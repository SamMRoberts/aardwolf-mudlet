<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE MudletPackage>
<MudletPackage version="1.001"><AliasPackage><Alias isActive="yes" isFolder="no"><name>aardwolf_mudlet.command</name><script>aardwolf_mudlet.commands.dispatch(matches[2])
</script><command></command><packageName></packageName><regex>^aard(?:\s+(.*))?$</regex></Alias><Alias isActive="yes" isFolder="no"><name>aardwolf_mudlet.legacy_group_on</name><script>aardwolf_mudlet.commands.set_tab("group")
</script><command></command><packageName></packageName><regex>^groupon$</regex></Alias><Alias isActive="yes" isFolder="no"><name>aardwolf_mudlet.legacy_group_off</name><script>aardwolf_mudlet.commands.set_tab("overview")
</script><command></command><packageName></packageName><regex>^groupoff$</regex></Alias><Alias isActive="yes" isFolder="no"><name>aardwolf_mudlet.legacy_chats_show</name><script>aardwolf_mudlet.commands.dispatch("chat show")
</script><command></command><packageName></packageName><regex>^chats show$</regex></Alias><Alias isActive="yes" isFolder="no"><name>aardwolf_mudlet.legacy_chats_hide</name><script>aardwolf_mudlet.commands.dispatch("chat hide")
</script><command></command><packageName></packageName><regex>^chats hide$</regex></Alias><Alias isActive="yes" isFolder="no"><name>aardwolf_mudlet.legacy_reset</name><script>aardwolf_mudlet.commands.repair()
</script><command></command><packageName></packageName><regex>^resetaard$</regex></Alias><Alias isActive="yes" isFolder="no"><name>aardwolf_mudlet.legacy_tick</name><script>aardwolf_mudlet.commands.tick_status()
</script><command></command><packageName></packageName><regex>^aard tick(?: (?:help|miniwin|status|reset))?$</regex></Alias><Alias isActive="yes" isFolder="no"><name>aardwolf_mudlet.legacy_tts</name><script>aardwolf_mudlet.commands.legacy_tts(matches[2])
</script><command></command><packageName></packageName><regex>^(sapi(?: .*)?|tts(?: .*)?|tts_interrupt(?: .*)?|tts_note(?: .*)?|tts_stop)$</regex></Alias></AliasPackage><TriggerPackage><Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no"><name>aardwolf_mudlet.command_tag</name><script>if aardwolf_mudlet.capture.on_tag(matches[2], matches[3]) and type(deleteLine) == "function" then deleteLine() end
</script><triggerType>0</triggerType><conditonLineDelta>0</conditonLineDelta><mStayOpen>0</mStayOpen><mCommand></mCommand><packageName></packageName><path></path><mFgColor>#ff0000</mFgColor><mBgColor>#ffff00</mBgColor><mSoundFile></mSoundFile><colorTriggerFgColor>#000000</colorTriggerFgColor><colorTriggerBgColor>#000000</colorTriggerBgColor><regexCodeList><string>^\{Command:([A-Za-z0-9_]+) (Start|End)\}$</string></regexCodeList><regexCodePropertyList><integer>1</integer></regexCodePropertyList></Trigger><Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no"><name>aardwolf_mudlet.capture_line</name><script>aardwolf_mudlet.capture.on_line(line or (getCurrentLine and getCurrentLine()) or "")
</script><triggerType>0</triggerType><conditonLineDelta>0</conditonLineDelta><mStayOpen>0</mStayOpen><mCommand></mCommand><packageName></packageName><path></path><mFgColor>#ff0000</mFgColor><mBgColor>#ffff00</mBgColor><mSoundFile></mSoundFile><colorTriggerFgColor>#000000</colorTriggerFgColor><colorTriggerBgColor>#000000</colorTriggerBgColor><regexCodeList><string>^.*$</string></regexCodeList><regexCodePropertyList><integer>1</integer></regexCodePropertyList></Trigger><Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no"><name>aardwolf_mudlet.system_line</name><script>aardwolf_mudlet.chat.capture_system_line(line or (getCurrentLine and getCurrentLine()) or "")
</script><triggerType>0</triggerType><conditonLineDelta>0</conditonLineDelta><mStayOpen>0</mStayOpen><mCommand></mCommand><packageName></packageName><path></path><mFgColor>#ff0000</mFgColor><mBgColor>#ffff00</mBgColor><mSoundFile></mSoundFile><colorTriggerFgColor>#000000</colorTriggerFgColor><colorTriggerBgColor>#000000</colorTriggerBgColor><regexCodeList><string>^[A-Z][A-Z _]+:.*$</string></regexCodeList><regexCodePropertyList><integer>1</integer></regexCodePropertyList></Trigger><Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no"><name>aardwolf_mudlet.group_invite</name><script>aardwolf_mudlet.protocol.group_invite(matches[2], matches[3])
</script><triggerType>0</triggerType><conditonLineDelta>0</conditonLineDelta><mStayOpen>0</mStayOpen><mCommand></mCommand><packageName></packageName><path></path><mFgColor>#ff0000</mFgColor><mBgColor>#ffff00</mBgColor><mSoundFile></mSoundFile><colorTriggerFgColor>#000000</colorTriggerFgColor><colorTriggerBgColor>#000000</colorTriggerBgColor><regexCodeList><string>^(\w+) has invited you to join group: (.*)\.$</string></regexCodeList><regexCodePropertyList><integer>1</integer></regexCodePropertyList></Trigger><Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no"><name>aardwolf_mudlet.group_cancelled_by_player</name><script>aardwolf_mudlet.protocol.group_cancel(matches[2], matches[3])
</script><triggerType>0</triggerType><conditonLineDelta>0</conditonLineDelta><mStayOpen>0</mStayOpen><mCommand></mCommand><packageName></packageName><path></path><mFgColor>#ff0000</mFgColor><mBgColor>#ffff00</mBgColor><mSoundFile></mSoundFile><colorTriggerFgColor>#000000</colorTriggerFgColor><colorTriggerBgColor>#000000</colorTriggerBgColor><regexCodeList><string>^(\w+) has cancelled your invitation to join group: (.*)\.$</string></regexCodeList><regexCodePropertyList><integer>1</integer></regexCodePropertyList></Trigger><Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no"><name>aardwolf_mudlet.group_declined</name><script>aardwolf_mudlet.protocol.group_cancel(matches[2], "Invitation declined")
</script><triggerType>0</triggerType><conditonLineDelta>0</conditonLineDelta><mStayOpen>0</mStayOpen><mCommand></mCommand><packageName></packageName><path></path><mFgColor>#ff0000</mFgColor><mBgColor>#ffff00</mBgColor><mSoundFile></mSoundFile><colorTriggerFgColor>#000000</colorTriggerFgColor><colorTriggerBgColor>#000000</colorTriggerBgColor><regexCodeList><string>^You have declined the group invitation from (\w+)\.$</string></regexCodeList><regexCodePropertyList><integer>1</integer></regexCodePropertyList></Trigger><Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no"><name>aardwolf_mudlet.group_no_invitation</name><script>aardwolf_mudlet.protocol.group_cancel(matches[2], "No invitation outstanding")
</script><triggerType>0</triggerType><conditonLineDelta>0</conditonLineDelta><mStayOpen>0</mStayOpen><mCommand></mCommand><packageName></packageName><path></path><mFgColor>#ff0000</mFgColor><mBgColor>#ffff00</mBgColor><mSoundFile></mSoundFile><colorTriggerFgColor>#000000</colorTriggerFgColor><colorTriggerBgColor>#000000</colorTriggerBgColor><regexCodeList><string>^You have no invitation outstanding from (\w+)\.$</string></regexCodeList><regexCodePropertyList><integer>1</integer></regexCodePropertyList></Trigger><Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no"><name>aardwolf_mudlet.group_player_left_group</name><script>aardwolf_mudlet.protocol.group_cancel(matches[2], matches[3] .. " left that group")
</script><triggerType>0</triggerType><conditonLineDelta>0</conditonLineDelta><mStayOpen>0</mStayOpen><mCommand></mCommand><packageName></packageName><path></path><mFgColor>#ff0000</mFgColor><mBgColor>#ffff00</mBgColor><mSoundFile></mSoundFile><colorTriggerFgColor>#000000</colorTriggerFgColor><colorTriggerBgColor>#000000</colorTriggerBgColor><regexCodeList><string>^Your group invitation from (\w+) is cancelled because (\w+) has left that group\.$</string></regexCodeList><regexCodePropertyList><integer>1</integer></regexCodePropertyList></Trigger><Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no"><name>aardwolf_mudlet.group_player_left_game</name><script>aardwolf_mudlet.protocol.group_cancel(matches[2], matches[3] .. " left the game")
</script><triggerType>0</triggerType><conditonLineDelta>0</conditonLineDelta><mStayOpen>0</mStayOpen><mCommand></mCommand><packageName></packageName><path></path><mFgColor>#ff0000</mFgColor><mBgColor>#ffff00</mBgColor><mSoundFile></mSoundFile><colorTriggerFgColor>#000000</colorTriggerFgColor><colorTriggerBgColor>#000000</colorTriggerBgColor><regexCodeList><string>^Your group invitation from (\w+) is cancelled because (\w+) has left the game\.$</string></regexCodeList><regexCodePropertyList><integer>1</integer></regexCodePropertyList></Trigger><Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no"><name>aardwolf_mudlet.group_disbanded</name><script>aardwolf_mudlet.protocol.group_cancel(matches[2], "Group disbanded")
</script><triggerType>0</triggerType><conditonLineDelta>0</conditonLineDelta><mStayOpen>0</mStayOpen><mCommand></mCommand><packageName></packageName><path></path><mFgColor>#ff0000</mFgColor><mBgColor>#ffff00</mBgColor><mSoundFile></mSoundFile><colorTriggerFgColor>#000000</colorTriggerFgColor><colorTriggerBgColor>#000000</colorTriggerBgColor><regexCodeList><string>^Your group invite from (\w+) is cancelled because the group has been disbanded\.$</string></regexCodeList><regexCodePropertyList><integer>1</integer></regexCodePropertyList></Trigger></TriggerPackage><TimerPackage></TimerPackage><ScriptPackage><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.state</name><script>aardwolf_mudlet = aardwolf_mudlet or {}

aardwolf_mudlet.util = aardwolf_mudlet.util or {}
aardwolf_mudlet.constants = aardwolf_mudlet.constants or {}
aardwolf_mudlet.state = aardwolf_mudlet.state or {}

function aardwolf_mudlet.util.finite(value)
  local number = tonumber(value)
  if not number or number ~= number or number == math.huge or number == -math.huge then
    return nil
  end
  return number
end

function aardwolf_mudlet.util.bounded(value, minimum, maximum)
  local number = aardwolf_mudlet.util.finite(value)
  if not number then return nil end
  return math.max(minimum, math.min(maximum, number))
end

function aardwolf_mudlet.util.text(value, maximum_length)
  if type(value) ~= "string" and type(value) ~= "number" then return nil end
  local cleaned = tostring(value):gsub("[%z\1-\8\11\12\14-\31]", "")
  local limit = maximum_length or 160
  if #cleaned &gt; limit then return cleaned:sub(1, limit) .. "..." end
  return cleaned
end

function aardwolf_mudlet.util.escape(value)
  local text = aardwolf_mudlet.util.text(value, 2000) or ""
  return text:gsub("&amp;", "&amp;amp;"):gsub("&lt;", "&amp;lt;"):gsub("&gt;", "&amp;gt;")
    :gsub('"', "&amp;quot;"):gsub("'", "&amp;#39;")
end

function aardwolf_mudlet.util.display(value)
  local number = aardwolf_mudlet.util.finite(value)
  if number then
    if number == math.floor(number) then return string.format("%d", number) end
    return string.format("%.1f", number)
  end
  -- UI rows also pass validated protocol strings and composed values such as
  -- "204 / 204" through this formatter. Treating every non-number as missing
  -- made populated identity, class, position, and attribute rows render as
  -- "--". Preserve printable text while keeping empty values explicit.
  local text = aardwolf_mudlet.util.text(value, 160)
  if not text or text == "" then return "--" end
  return text
end

function aardwolf_mudlet.util.safe_call(object, method, ...)
  if type(object) ~= "table" or type(object[method]) ~= "function" then return false end
  return pcall(object[method], object, ...)
end

function aardwolf_mudlet.util.copy(value, depth)
  if type(value) ~= "table" then return value end
  local remaining = depth == nil and 4 or depth
  if remaining &lt;= 0 then return {} end
  local output, count = {}, 0
  for key, item in pairs(value) do
    count = count + 1
    if count &gt; 200 then break end
    if type(key) == "string" or type(key) == "number" then
      output[key] = aardwolf_mudlet.util.copy(item, remaining - 1)
    end
  end
  return output
end

aardwolf_mudlet.constants.wear_locations = {
  [0] = "Light", [1] = "Head", [2] = "Eyes", [3] = "Left ear", [4] = "Right ear",
  [5] = "Neck 1", [6] = "Neck 2", [7] = "Back", [8] = "Medal 1", [9] = "Medal 2",
  [10] = "Medal 3", [11] = "Medal 4", [12] = "Torso", [13] = "Body", [14] = "Waist",
  [15] = "Arms", [16] = "Left wrist", [17] = "Right wrist", [18] = "Hands",
  [19] = "Left finger", [20] = "Right finger", [21] = "Legs", [22] = "Feet",
  [23] = "Shield", [24] = "Wielded", [25] = "Second", [26] = "Hold", [27] = "Float",
  [28] = "Tattoo 1", [29] = "Tattoo 2", [30] = "Above", [31] = "Portal", [32] = "Sleeping",
}

aardwolf_mudlet.constants.character_states = {
  [1] = "Login", [2] = "Login sequence", [3] = "Active", [4] = "AFK", [5] = "Note",
  [6] = "Editing", [7] = "Pager", [8] = "Combat", [9] = "Sleeping", [11] = "Resting", [12] = "Running",
}

aardwolf_mudlet.constants.themes = {
  obsidian = {
    panel = "#090d13", surface = "#111823", raised = "#182231", border = "#34465d",
    text = "#f3f7fb", muted = "#aeb9c8", accent = "#4cc9f0", emerald = "#38d996",
    gold = "#f4c95d", crimson = "#ff6673", violet = "#b98cff", track = "#263244",
    hp = "#38d996", mana = "#4c9cff", moves = "#f4c95d", enemy = "#ff6673",
    hunger = "#ef9b49", thirst = "#4cc9f0",
  },
  ["high-contrast"] = {
    panel = "#000000", surface = "#080808", raised = "#151515", border = "#ffffff",
    text = "#ffffff", muted = "#e6e6e6", accent = "#00ffff", emerald = "#00ff66",
    gold = "#ffff00", crimson = "#ff4a4a", violet = "#e080ff", track = "#303030",
    hp = "#00ff66", mana = "#00aaff", moves = "#ffff00", enemy = "#ff4a4a",
    hunger = "#ff9900", thirst = "#00ffff",
  },
}

local function empty_details()
  return {
    equipment = {}, inventory = {}, bags = {}, generation = 0, refreshing = false, stale = true,
    last_updated = nil, error = nil, errors = {}, overflow = false,
    sections = {equipment = {status = "unavailable"}, bags = {status = "unavailable"}},
  }
end

function aardwolf_mudlet.state.reset_session(reason)
  local state = aardwolf_mudlet.state
  state.session_generation = (state.session_generation or 0) + 1
  state.connection = reason == "connected" and "connected" or "disconnected"
  state.character_key = nil
  state.sections = {}
  for _, name in ipairs({"base", "vitals", "maxstats", "status", "stats", "worth", "room", "group", "quest", "tick", "map", "capture"}) do
    state.sections[name] = {
      status = "unavailable", value = name == "group" and {members = {}} or {},
      received_at = nil, session_generation = state.session_generation, error = nil,
    }
  end
  state.sections.details = {
    status = "stale", value = empty_details(), received_at = nil,
    session_generation = state.session_generation, error = nil,
  }
  state.render_pending = false
end

function aardwolf_mudlet.state.record(section, value, status, error_message, received_at)
  if type(section) ~= "string" or type(value) ~= "table" then return false end
  if not aardwolf_mudlet.state.sections then aardwolf_mudlet.state.reset_session("disconnected") end
  aardwolf_mudlet.state.sections[section] = {
    status = status or "current",
    value = aardwolf_mudlet.util.copy(value),
    received_at = received_at or os.time(),
    session_generation = aardwolf_mudlet.state.session_generation,
    error = aardwolf_mudlet.util.text(error_message, 200),
  }
  return true
end

function aardwolf_mudlet.state.envelope(section)
  if not aardwolf_mudlet.state.sections then aardwolf_mudlet.state.reset_session("disconnected") end
  return aardwolf_mudlet.state.sections[section] or {
    status = "unavailable", value = {}, session_generation = aardwolf_mudlet.state.session_generation,
  }
end

function aardwolf_mudlet.state.value(section)
  return aardwolf_mudlet.state.envelope(section).value or {}
end

function aardwolf_mudlet.state.set_details(details, status, error_message)
  return aardwolf_mudlet.state.record("details", details, status or (details.stale and "stale" or "current"), error_message)
end

function aardwolf_mudlet.state.mark_stale(reason)
  if not aardwolf_mudlet.state.sections then return end
  for name, envelope in pairs(aardwolf_mudlet.state.sections) do
    if name ~= "details" and envelope.status == "current" then
      envelope.status = "stale"
      envelope.error = aardwolf_mudlet.util.text(reason, 200)
    end
  end
end

function aardwolf_mudlet.state.tick_remaining(now)
  local tick = aardwolf_mudlet.state.value("tick")
  local last_seen = aardwolf_mudlet.util.finite(tick.last_seen)
  local duration = aardwolf_mudlet.util.finite(tick.duration)
  if not last_seen or not duration or duration &lt;= 0 then return nil end
  return math.max(0, math.min(duration, math.ceil(duration - ((now or os.time()) - last_seen))))
end

aardwolf_mudlet.state.reset_session("disconnected")
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.chat</name><script>aardwolf_mudlet = aardwolf_mudlet or {}
aardwolf_mudlet.chat = aardwolf_mudlet.chat or {}

local chat = aardwolf_mudlet.chat
local DEFAULT_LIMIT = 500
local MAX_LIMIT = 5000
local DEFAULT_TABS = {
  {name = "All", filters = {"*"}},
  {name = "Tells", filters = {"tell", "ptell", "dtell"}},
  {name = "Group", filters = {"group", "gtell"}},
  {name = "Clan", filters = {"claninfo", "clantalk", "gclan"}},
  {name = "System", filters = {"info", "raidinfo", "global_quest", "warfare", "remort_auction", "remote_socials"}},
}

local function clean(value, limit)
  return aardwolf_mudlet.util.text(value, limit or 200)
end

local function normalize_filter(value)
  local result = clean(value, 32)
  if not result then return nil end
  result = result:lower():match("^%s*(.-)%s*$")
  if result == "*" or result:match("^[a-z0-9_.%-]+$") then return result end
  return nil
end

local function copy_tabs(source)
  local result, seen = {}, {}
  for _, candidate in ipairs(type(source) == "table" and source or {}) do
    if #result &gt;= 12 then break end
    if type(candidate) == "table" then
      local name = clean(candidate.name, 24)
      if name then name = name:match("^%s*(.-)%s*$") end
      local key = name and name:lower() or nil
      if key and name ~= "" and not seen[key] then
        local filters, filter_seen = {}, {}
        for _, value in ipairs(type(candidate.filters) == "table" and candidate.filters or {}) do
          local filter = normalize_filter(value)
          if filter and not filter_seen[filter] and #filters &lt; 64 then
            filter_seen[filter] = true
            filters[#filters + 1] = filter
          end
        end
        if #filters == 0 then filters[1] = "*" end
        seen[key] = true
        result[#result + 1] = {name = name, filters = filters}
      end
    end
  end
  return result
end

function chat.validate_settings(candidate, fallback)
  local base = type(fallback) == "table" and fallback or {
    visible = true, preserve_main = true, logging = false,
    max_messages = DEFAULT_LIMIT, active_tab = "All", tabs = DEFAULT_TABS,
  }
  local output = {
    visible = base.visible ~= false,
    preserve_main = base.preserve_main ~= false,
    logging = base.logging == true,
    max_messages = math.floor(tonumber(base.max_messages) or DEFAULT_LIMIT),
    active_tab = clean(base.active_tab, 24) or "All",
    tabs = copy_tabs(base.tabs),
  }
  if type(candidate) == "table" then
    if type(candidate.visible) == "boolean" then output.visible = candidate.visible end
    if type(candidate.preserve_main) == "boolean" then output.preserve_main = candidate.preserve_main end
    if type(candidate.logging) == "boolean" then output.logging = candidate.logging end
    output.max_messages = math.floor(aardwolf_mudlet.util.bounded(candidate.max_messages, 1, MAX_LIMIT) or output.max_messages)
    local tabs = copy_tabs(candidate.tabs)
    if #tabs &gt; 0 then output.tabs = tabs end
    local active = clean(candidate.active_tab, 24)
    if active then output.active_tab = active end
  end
  if #output.tabs == 0 then output.tabs = copy_tabs(DEFAULT_TABS) end
  local active_found = false
  for _, tab in ipairs(output.tabs) do if tab.name == output.active_tab then active_found = true end end
  if not active_found then output.active_tab = output.tabs[1].name end
  return output
end

local function settings()
  local data = aardwolf_mudlet.settings and aardwolf_mudlet.settings.data
  if not data then return chat.validate_settings(nil) end
  data.chat = chat.validate_settings(data.chat)
  return data.chat
end

local function tab_matches(tab, channel)
  for _, filter in ipairs(tab.filters or {}) do
    if filter == "*" or filter == channel then return true end
  end
  return false
end

local function buffer_for(name)
  chat.buffers = chat.buffers or {}
  chat.buffers[name] = chat.buffers[name] or {messages = {}, unread = 0}
  return chat.buffers[name]
end

local function trim(buffer, maximum)
  while #buffer.messages &gt; maximum do table.remove(buffer.messages, 1) end
end

local function log_path()
  return aardwolf_mudlet.settings.directory() .. "/chat.log"
end

local function rotate_log(path)
  if type(lfs) ~= "table" or type(lfs.attributes) ~= "function" then return end
  local size = tonumber(lfs.attributes(path, "size")) or 0
  if size &lt; 5 * 1024 * 1024 then return end
  for index = 4, 1, -1 do
    local old = path .. "." .. index
    local next_name = path .. "." .. (index + 1)
    if lfs.attributes(old, "mode") == "file" then os.rename(old, next_name) end
  end
  os.rename(path, path .. ".1")
end

local function write_log(message)
  if not settings().logging or not aardwolf_mudlet.settings.ensure_directory() then return false end
  local path = log_path()
  rotate_log(path)
  local file = io.open(path, "a")
  if not file then return false end
  local line = string.format("[%s] [%s] %s%s\n", os.date("%Y-%m-%d %H:%M:%S", message.time), message.channel,
    message.player ~= "" and (message.player .. ": ") or "", message.plain)
  file:write(line)
  file:close()
  return true
end

function chat.initialize()
  chat.buffers = chat.buffers or {}
  chat.active = true
  local valid = {}
  for _, tab in ipairs(settings().tabs) do valid[tab.name] = true; buffer_for(tab.name) end
  for name in pairs(chat.buffers) do if not valid[name] then chat.buffers[name] = nil end end
end

function chat.shutdown()
  chat.active = false
end

function chat.record(channel, player, message, raw)
  if not chat.active then chat.initialize() end
  channel = normalize_filter(channel or "system") or "system"
  player = clean(player, 80) or ""
  message = clean(message, 2000)
  if not message or message == "" then return false end
  local entry = {channel = channel, player = player, plain = message, raw = clean(raw, 2400) or message, time = os.time()}
  local config = settings()
  for _, tab in ipairs(config.tabs) do
    if tab_matches(tab, channel) then
      local buffer = buffer_for(tab.name)
      buffer.messages[#buffer.messages + 1] = entry
      trim(buffer, config.max_messages)
      if tab.name ~= config.active_tab or aardwolf_mudlet.settings.data.active_tab ~= "chat" then
        buffer.unread = math.min(999, (buffer.unread or 0) + 1)
      end
    end
  end
  write_log(entry)
  if aardwolf_mudlet.accessibility and aardwolf_mudlet.accessibility.on_chat then
    aardwolf_mudlet.accessibility.on_chat(entry)
  end
  if aardwolf_mudlet.sound and aardwolf_mudlet.sound.on_event then
    aardwolf_mudlet.sound.on_event("chat." .. channel)
  end
  if aardwolf_mudlet.ui and aardwolf_mudlet.ui.request_render then aardwolf_mudlet.ui.request_render() end
  return true
end

function chat.on_gmcp()
  local payload = gmcp and gmcp.comm and gmcp.comm.channel
  if type(payload) ~= "table" then return false end
  return chat.record(payload.chan or payload.channel or payload.name or "channel",
    payload.player or payload.from or payload.sender or "", payload.msg or payload.message or payload.text, payload.msg or payload.message or payload.text)
end

function chat.capture_system_line(line)
  local prefix, body = tostring(line or ""):match("^([A-Z][A-Z _]+):%s*(.*)$")
  if not prefix then return false end
  local tags = {INFO="info", RAIDINFO="raidinfo", CLANINFO="claninfo", WARFARE="warfare", ["GLOBAL QUEST"]="global_quest", ["CLAN DONATIONS"]="clan_donations"}
  local tag = tags[prefix]
  if not tag then return false end
  return chat.record(tag, "", body, line)
end

function chat.tabs() return settings().tabs end
function chat.active_name() return settings().active_tab end
function chat.active_buffer() return buffer_for(chat.active_name()) end

function chat.select(name_or_index)
  local tabs = settings().tabs
  local selected = type(name_or_index) == "number" and tabs[name_or_index] or nil
  if not selected then
    for _, tab in ipairs(tabs) do if tab.name:lower() == tostring(name_or_index or ""):lower() then selected = tab end end
  end
  if not selected then return false, "Unknown chat tab" end
  settings().active_tab = selected.name
  buffer_for(selected.name).unread = 0
  aardwolf_mudlet.settings.save()
  if aardwolf_mudlet.ui and aardwolf_mudlet.ui.request_render then aardwolf_mudlet.ui.request_render() end
  return true
end

function chat.add_tab(name, filters)
  name = clean(name, 24)
  local config = settings()
  if not name or name == "" or #config.tabs &gt;= 12 then return false, "Tab name is invalid or the 12-tab limit was reached" end
  for _, tab in ipairs(config.tabs) do if tab.name:lower() == name:lower() then return false, "Chat tab already exists" end end
  local values = {}
  for value in tostring(filters or "*"):gmatch("[^,]+") do values[#values + 1] = value end
  local normalized = copy_tabs({{name = name, filters = values}})[1]
  if not normalized then return false, "Chat filters are invalid" end
  config.tabs[#config.tabs + 1] = normalized
  buffer_for(name)
  aardwolf_mudlet.settings.save()
  return true
end

function chat.rename_tab(old_name, new_name)
  local config = settings()
  new_name = clean(new_name, 24)
  if not new_name or new_name == "" then return false, "New tab name is invalid" end
  for _, tab in ipairs(config.tabs) do if tab.name:lower() == new_name:lower() then return false, "Chat tab already exists" end end
  for _, tab in ipairs(config.tabs) do
    if tab.name:lower() == tostring(old_name or ""):lower() then
      local old = tab.name; tab.name = new_name
      chat.buffers[new_name] = chat.buffers[old] or {messages = {}, unread = 0}; chat.buffers[old] = nil
      if config.active_tab == old then config.active_tab = new_name end
      aardwolf_mudlet.settings.save(); return true
    end
  end
  return false, "Unknown chat tab"
end

function chat.remove_tab(name)
  local config = settings()
  if #config.tabs &lt;= 1 then return false, "At least one chat tab is required" end
  for index, tab in ipairs(config.tabs) do
    if tab.name:lower() == tostring(name or ""):lower() then
      table.remove(config.tabs, index); chat.buffers[tab.name] = nil
      if config.active_tab == tab.name then config.active_tab = config.tabs[1].name end
      aardwolf_mudlet.settings.save(); return true
    end
  end
  return false, "Unknown chat tab"
end

function chat.move_tab(name, position)
  position = math.floor(tonumber(position) or 0)
  local tabs = settings().tabs
  if position &lt; 1 or position &gt; #tabs then return false, "Chat tab position is out of range" end
  for index, tab in ipairs(tabs) do
    if tab.name:lower() == tostring(name or ""):lower() then
      table.remove(tabs, index); table.insert(tabs, position, tab)
      aardwolf_mudlet.settings.save(); return true
    end
  end
  return false, "Unknown chat tab"
end

function chat.set_limit(value)
  local limit = math.floor(tonumber(value) or 0)
  if limit &lt; 1 or limit &gt; MAX_LIMIT then return false, "Chat limit must be between 1 and 5000" end
  settings().max_messages = limit
  for _, buffer in pairs(chat.buffers or {}) do trim(buffer, limit) end
  aardwolf_mudlet.settings.save(); return true
end

function chat.set_filters(name, filters)
  local config = settings()
  for _, tab in ipairs(config.tabs) do
    if tab.name:lower() == tostring(name or ""):lower() then
      local values = {}; for value in tostring(filters or ""):gmatch("[^,]+") do values[#values + 1] = value end
      local normalized = copy_tabs({{name = tab.name, filters = values}})[1]
      if not normalized then return false, "Chat filters are invalid" end
      tab.filters = normalized.filters; aardwolf_mudlet.settings.save(); return true
    end
  end
  return false, "Unknown chat tab"
end

function chat.clear(name)
  local target = name or chat.active_name()
  local buffer = chat.buffers and chat.buffers[target]
  if not buffer then return false end
  buffer.messages, buffer.unread = {}, 0
  return true
end

function chat.set_preserve(value) settings().preserve_main = value == true; aardwolf_mudlet.settings.save(); return true end
function chat.set_logging(value) settings().logging = value == true; aardwolf_mudlet.settings.save(); return true end

function chat.render(limit)
  local buffer = chat.active_buffer()
  local output = {}
  local first = math.max(1, #buffer.messages - (limit or 80) + 1)
  for index = first, #buffer.messages do
    local entry = buffer.messages[index]
    local player = entry.player ~= "" and ("&lt;b&gt;" .. aardwolf_mudlet.util.escape(entry.player) .. ":&lt;/b&gt; ") or ""
    output[#output + 1] = string.format("&lt;font color='#8fa3bc'&gt;[%s] [%s]&lt;/font&gt; %s%s", os.date("%H:%M:%S", entry.time),
      aardwolf_mudlet.util.escape(entry.channel), player, aardwolf_mudlet.util.escape(entry.plain))
  end
  if #output == 0 then output[1] = "&lt;font color='#8fa3bc'&gt;No messages captured for this tab.&lt;/font&gt;" end
  return table.concat(output, "&lt;br&gt;")
end

function chat.copy_active(raw)
  if type(setClipboardText) ~= "function" then return false, "Clipboard support is unavailable" end
  local rows = {}
  for _, entry in ipairs(chat.active_buffer().messages) do rows[#rows + 1] = raw and entry.raw or entry.plain end
  setClipboardText(table.concat(rows, "\n"))
  return true
end

function chat.open_url(url)
  url = clean(url, 500)
  if not url or not url:match("^https?://[%w%-%._~:/%?#%[%]@!$&amp;'()%*+,;=%%]+$") then return false, "Only valid HTTP or HTTPS URLs are allowed" end
  if type(openUrl) ~= "function" then return false, "URL support is unavailable" end
  openUrl(url); return true
end
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.accessibility</name><script>aardwolf_mudlet = aardwolf_mudlet or {}
aardwolf_mudlet.accessibility = aardwolf_mudlet.accessibility or {}

local accessibility = aardwolf_mudlet.accessibility

function accessibility.validate_settings(candidate, fallback)
  local base = type(fallback) == "table" and fallback or {enabled = false, rate = 0, volume = 1, voice = nil}
  local output = {enabled = base.enabled == true, rate = tonumber(base.rate) or 0, volume = tonumber(base.volume) or 1, voice = base.voice}
  if type(candidate) == "table" then
    output.enabled = candidate.enabled == true
    output.rate = aardwolf_mudlet.util.bounded(candidate.rate, -1, 1) or output.rate
    output.volume = aardwolf_mudlet.util.bounded(candidate.volume, 0, 1) or output.volume
    output.voice = aardwolf_mudlet.util.text(candidate.voice, 100)
  end
  return output
end

local function config()
  local data = aardwolf_mudlet.settings.data
  data.accessibility = accessibility.validate_settings(data.accessibility)
  return data.accessibility
end

function accessibility.available() return type(ttsSpeak) == "function" or type(ttsQueue) == "function" end

function accessibility.apply()
  local value = config()
  if type(ttsSetRate) == "function" then pcall(ttsSetRate, value.rate) end
  if type(ttsSetVolume) == "function" then pcall(ttsSetVolume, value.volume) end
  if value.voice and type(ttsSetVoiceByName) == "function" then pcall(ttsSetVoiceByName, value.voice) end
end

function accessibility.set_enabled(value)
  if value and not accessibility.available() then return false, "Mudlet text-to-speech is unavailable" end
  config().enabled = value == true
  aardwolf_mudlet.settings.save()
  accessibility.apply()
  return true
end

function accessibility.say(text, interrupt)
  text = aardwolf_mudlet.util.text(text, 1000)
  if not text or text == "" then return false, "Text is required" end
  if not accessibility.available() then return false, "Mudlet text-to-speech is unavailable" end
  accessibility.apply()
  if interrupt and type(ttsSpeak) == "function" then ttsSpeak(text)
  elseif type(ttsQueue) == "function" then ttsQueue(text)
  else ttsSpeak(text) end
  return true
end

function accessibility.clear()
  if type(ttsClearQueue) == "function" then ttsClearQueue(); return true end
  if type(ttsStop) == "function" then ttsStop(); return true end
  return false, "Mudlet text-to-speech queue controls are unavailable"
end

function accessibility.set_rate(value)
  value = aardwolf_mudlet.util.bounded(value, -1, 1)
  if not value then return false, "Rate must be between -1 and 1" end
  config().rate = value; aardwolf_mudlet.settings.save(); accessibility.apply(); return true
end

function accessibility.set_voice(value)
  value = aardwolf_mudlet.util.text(value, 100)
  if not value or value == "" then return false, "Voice name is required" end
  if type(ttsSetVoiceByName) ~= "function" then return false, "Voice selection is unavailable" end
  local ok = ttsSetVoiceByName(value)
  if ok == false then return false, "Unknown voice" end
  config().voice = value; aardwolf_mudlet.settings.save(); return true
end

function accessibility.voices()
  if type(ttsGetVoices) ~= "function" then return {} end
  local ok, value = pcall(ttsGetVoices)
  return ok and type(value) == "table" and value or {}
end

function accessibility.on_chat(entry)
  if config().enabled and (entry.channel == "tell" or entry.channel == "ptell" or entry.channel == "dtell") then
    accessibility.say((entry.player ~= "" and entry.player .. " says " or "") .. entry.plain, false)
  end
end

function accessibility.status()
  local value = config()
  return string.format("available=%s enabled=%s rate=%s voice=%s", tostring(accessibility.available()), tostring(value.enabled), tostring(value.rate), tostring(value.voice or "system"))
end
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.sound</name><script>aardwolf_mudlet = aardwolf_mudlet or {}
aardwolf_mudlet.sound = aardwolf_mudlet.sound or {}

local sound = aardwolf_mudlet.sound

local function valid_relative_file(value)
  if type(value) ~= "string" or value == "" or #value &gt; 200 then return nil end
  if value:sub(1, 1) == "/" or value:find("\\") or value:find("..", 1, true) then return nil end
  if not value:match("^[%w%._%-%/ ]+%.[Ww][Aa][Vv]$") and not value:match("^[%w%._%-%/ ]+%.[Mm][Pp]3$") and not value:match("^[%w%._%-%/ ]+%.[Oo][Gg][Gg]$") then return nil end
  return value
end

local function valid_event(value)
  value = aardwolf_mudlet.util.text(value, 64)
  if value and value:match("^[a-z0-9_.%-]+$") then return value end
end

function sound.validate_settings(candidate, fallback)
  local output = {enabled = type(fallback) == "table" and fallback.enabled == true or false, mappings = {}}
  local source = type(candidate) == "table" and candidate or fallback
  if type(source) ~= "table" then return output end
  output.enabled = source.enabled == true
  local count = 0
  for event, mapping in pairs(type(source.mappings) == "table" and source.mappings or {}) do
    local event_name = valid_event(event)
    local file, volume
    if type(mapping) == "table" then file = valid_relative_file(mapping.file); volume = aardwolf_mudlet.util.bounded(mapping.volume, 1, 100) or 50 end
    if event_name and file and count &lt; 64 then output.mappings[event_name] = {file = file, volume = volume}; count = count + 1 end
  end
  return output
end

local function config()
  local data = aardwolf_mudlet.settings.data
  data.sounds = sound.validate_settings(data.sounds)
  return data.sounds
end

function sound.available() return type(playSoundFile) == "function" end
function sound.media_root() return getMudletHomeDir() .. "/media/" end

function sound.set_enabled(value)
  if value and not sound.available() then return false, "Local sound playback is unavailable in this Mudlet build" end
  config().enabled = value == true; aardwolf_mudlet.settings.save(); return true
end

function sound.map(event, file, volume)
  event, file = valid_event(event), valid_relative_file(file)
  if not event or not file then return false, "Use a simple event name and a relative .wav, .mp3, or .ogg file under the profile media directory" end
  config().mappings[event] = {file = file, volume = aardwolf_mudlet.util.bounded(volume, 1, 100) or 50}
  aardwolf_mudlet.settings.save(); return true
end

function sound.remove(event)
  event = valid_event(event)
  if not event or not config().mappings[event] then return false, "Unknown sound event" end
  config().mappings[event] = nil; aardwolf_mudlet.settings.save(); return true
end

function sound.on_event(event)
  local value = config()
  local mapping = value.mappings[event]
  if not value.enabled or not mapping or not sound.available() then return false end
  local ok = pcall(playSoundFile, {name = sound.media_root() .. mapping.file, volume = mapping.volume, key = "aardwolf-mudlet-" .. event, tag = "aardwolf-mudlet"})
  return ok
end

function sound.status()
  local count = 0; for _ in pairs(config().mappings) do count = count + 1 end
  return string.format("available=%s enabled=%s mappings=%d media=%s", tostring(sound.available()), tostring(config().enabled), count, sound.media_root())
end

function sound.shutdown()
  if type(stopSounds) == "function" then pcall(stopSounds,{tag="aardwolf-mudlet"}) end
end
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.settings</name><script>aardwolf_mudlet.settings = aardwolf_mudlet.settings or {}

local SCHEMA_VERSION = 6
local VALID_TABS = {overview = true, character = true, group = true, inventory = true, chat = true}
local VALID_SCALES = {[90] = true, [100] = true, [115] = true, [130] = true}

local function defaults()
  return {
    schema_version = SCHEMA_VERSION,
    visible = true,
    details_visible = false,
    theme = "obsidian",
    density = "comfortable",
    text_scale = 100,
    workspace_width = 440,
    active_tab = "overview",
    inspector_pinned = false,
    palette_open = false,
    inventory_tab = "equipment",
    show_empty_slots = false,
    collapsed_by_user = false,
    sync_map_theme = true,
    custom_actions = {},
    next_action_id = 1,
    chat = {
      visible = true, preserve_main = true, logging = false, max_messages = 500,
      active_tab = "All",
      tabs = {
        {name = "All", filters = {"*"}},
        {name = "Tells", filters = {"tell", "ptell", "dtell"}},
        {name = "Group", filters = {"group", "gtell"}},
        {name = "Clan", filters = {"claninfo", "clantalk", "gclan"}},
        {name = "System", filters = {"info", "raidinfo", "global_quest", "warfare", "remort_auction", "remote_socials"}},
      },
    },
    accessibility = {enabled = false, rate = 0, volume = 1, voice = nil},
    sounds = {enabled = false, mappings = {}},
    data = {auto_backup = false, retain_backups = 30},
    border_claim = nil,
    bottom_border_claim = nil,
  }
end

local function normalized_tab(value)
  if value == "map" then return "overview" end
  return VALID_TABS[value] and value or nil
end

local function border_claim(value, size_key, maximum)
  if type(value) ~= "table" then return nil end
  local base = aardwolf_mudlet.util.bounded(value.base, 0, 10000)
  local size = aardwolf_mudlet.util.bounded(value[size_key], 0, maximum)
  local applied = aardwolf_mudlet.util.bounded(value.applied, 0, 12000)
  if not base or not size or not applied or applied ~= base + size then return nil end
  return {base = base, [size_key] = size, applied = applied}
end

local function custom_action(value)
  if type(value) ~= "table" then return nil end
  local id = aardwolf_mudlet.util.text(value.id, 48)
  local label = aardwolf_mudlet.util.text(value.label, 32)
  local command = aardwolf_mudlet.util.text(value.command, 200)
  local category = aardwolf_mudlet.util.text(value.category, 24) or "Custom"
  local order = aardwolf_mudlet.util.bounded(value.order, 1, 24)
  if not id or id == "" or not label or label == "" or not command or command == "" then return nil end
  if label:find("[%c]") or category:find("[%c]") or command:find("[%c]") then return nil end
  return {id = id, label = label, command = command, category = category, order = order or 24}
end

function aardwolf_mudlet.settings.validate(candidate)
  local output = defaults()
  if type(candidate) ~= "table" then return output end
  local version = tonumber(candidate.schema_version)
  if not version or version &lt; 1 or version &gt; SCHEMA_VERSION then return output end
  if type(candidate.visible) == "boolean" then output.visible = candidate.visible end
  if type(candidate.details_visible) == "boolean" then output.details_visible = candidate.details_visible end
  if candidate.theme == "dark" then output.theme = "obsidian"
  elseif candidate.theme == "obsidian" or candidate.theme == "high-contrast" then output.theme = candidate.theme end
  if candidate.density == "compact" or candidate.density == "comfortable" then output.density = candidate.density end
  local scale = tonumber(candidate.text_scale)
  if VALID_SCALES[scale] then output.text_scale = scale end
  output.workspace_width = aardwolf_mudlet.util.bounded(candidate.workspace_width, 360, 520) or output.workspace_width
  local active_tab = normalized_tab(candidate.active_tab)
  -- Schema 4 could retain a separate selected inspector view. When that
  -- inspector was pinned it is the view the player was actually using, so it
  -- becomes the schema-5 dock selection. Otherwise the former Map tab maps to
  -- Overview as expected.
  if version &lt; 5 and candidate.inspector_pinned == true then
    active_tab = normalized_tab(candidate.inspector_tab) or active_tab
  end
  if active_tab then output.active_tab = active_tab end
  if candidate.inventory_tab == "equipment" or candidate.inventory_tab == "bags" then output.inventory_tab = candidate.inventory_tab end
  for _, name in ipairs({"inspector_pinned", "palette_open", "show_empty_slots", "collapsed_by_user", "sync_map_theme"}) do
    if type(candidate[name]) == "boolean" then output[name] = candidate[name] end
  end
  if version &lt; 4 and output.details_visible then
    output.inspector_pinned = true
    output.active_tab = "inventory"
  end
  local seen = {}
  if type(candidate.custom_actions) == "table" then
    for _, value in ipairs(candidate.custom_actions) do
      if #output.custom_actions &gt;= 24 then break end
      local action = custom_action(value)
      if action and not seen[action.id] then
        seen[action.id] = true
        output.custom_actions[#output.custom_actions + 1] = action
      end
    end
  end
  table.sort(output.custom_actions, function(left, right)
    if left.order == right.order then return left.id &lt; right.id end
    return left.order &lt; right.order
  end)
  output.next_action_id = math.max(1, math.floor(tonumber(candidate.next_action_id) or 1))
  if aardwolf_mudlet.chat and type(aardwolf_mudlet.chat.validate_settings) == "function" then
    output.chat = aardwolf_mudlet.chat.validate_settings(candidate.chat, output.chat)
  end
  if aardwolf_mudlet.accessibility and type(aardwolf_mudlet.accessibility.validate_settings) == "function" then
    output.accessibility = aardwolf_mudlet.accessibility.validate_settings(candidate.accessibility, output.accessibility)
  end
  if aardwolf_mudlet.sound and type(aardwolf_mudlet.sound.validate_settings) == "function" then
    output.sounds = aardwolf_mudlet.sound.validate_settings(candidate.sounds, output.sounds)
  end
  if type(candidate.data) == "table" then
    output.data.auto_backup = candidate.data.auto_backup == true
    output.data.retain_backups = math.floor(aardwolf_mudlet.util.bounded(candidate.data.retain_backups, 1, 30) or 30)
  end
  output.border_claim = border_claim(candidate.border_claim, "width", 2000)
  output.bottom_border_claim = border_claim(candidate.bottom_border_claim, "height", 1000)
  return output
end

function aardwolf_mudlet.settings.directory()
  return getMudletHomeDir() .. "/aardwolf-mudlet"
end

function aardwolf_mudlet.settings.path()
  return aardwolf_mudlet.settings.directory() .. "/settings.json"
end

function aardwolf_mudlet.settings.ensure_directory()
  local directory = aardwolf_mudlet.settings.directory()
  if type(lfs) == "table" and type(lfs.attributes) == "function" and lfs.attributes(directory, "mode") == "directory" then
    return true
  end
  if type(lfs) ~= "table" or type(lfs.mkdir) ~= "function" then return false end
  local ok = pcall(lfs.mkdir, directory)
  return ok and (type(lfs.attributes) ~= "function" or lfs.attributes(directory, "mode") == "directory")
end

function aardwolf_mudlet.settings.load()
  local file = io.open(aardwolf_mudlet.settings.path(), "r")
  local candidate
  if file then
    local contents = file:read("*a")
    file:close()
    if type(yajl) == "table" and type(yajl.to_value) == "function" then
      local ok, decoded = pcall(yajl.to_value, contents)
      if ok then candidate = decoded end
    end
  end
  aardwolf_mudlet.settings.data = aardwolf_mudlet.settings.validate(candidate)
  return aardwolf_mudlet.settings.data
end

function aardwolf_mudlet.settings.save()
  aardwolf_mudlet.settings.data = aardwolf_mudlet.settings.validate(aardwolf_mudlet.settings.data)
  if type(yajl) ~= "table" or type(yajl.to_string) ~= "function" then return false, "Mudlet JSON support is unavailable" end
  if not aardwolf_mudlet.settings.ensure_directory() then return false, "Unable to create settings directory" end
  local ok, encoded = pcall(yajl.to_string, aardwolf_mudlet.settings.data)
  if not ok then return false, encoded end
  local temporary = aardwolf_mudlet.settings.path() .. ".tmp"
  local file, error_message = io.open(temporary, "w")
  if not file then return false, error_message end
  file:write(encoded)
  file:close()
  local renamed, rename_error = os.rename(temporary, aardwolf_mudlet.settings.path())
  if not renamed then return false, rename_error end
  return true
end

function aardwolf_mudlet.settings.update(name, value)
  aardwolf_mudlet.settings.data = aardwolf_mudlet.settings.data or defaults()
  aardwolf_mudlet.settings.data[name] = value
  return aardwolf_mudlet.settings.save()
end

function aardwolf_mudlet.settings.is_visible()
  return not aardwolf_mudlet.settings.data or aardwolf_mudlet.settings.data.visible ~= false
end

function aardwolf_mudlet.settings.details_are_visible()
  local data = aardwolf_mudlet.settings.data or defaults()
  return data.active_tab == "inventory"
end
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.data</name><script>aardwolf_mudlet = aardwolf_mudlet or {}
aardwolf_mudlet.data = aardwolf_mudlet.data or {}

local data_api = aardwolf_mudlet.data

local function prune_backups()
  if type(lfs) ~= "table" or type(lfs.dir) ~= "function" then return end
  local directory = aardwolf_mudlet.settings.directory()
  local names = {}
  for name in lfs.dir(directory) do
    if name:match("^settings%-backup%-%d%d%d%d%d%d%d%d%-%d%d%d%d%d%d%.json$") then names[#names + 1] = name end
  end
  table.sort(names, function(left,right) return left&gt;right end)
  local retain = math.floor(aardwolf_mudlet.util.bounded(aardwolf_mudlet.settings.data.data.retain_backups,1,30) or 30)
  for index=retain+1,#names do os.remove(directory .. "/" .. names[index]) end
end

local function encode_settings()
  if type(yajl) ~= "table" or type(yajl.to_string) ~= "function" then return nil, "Mudlet JSON support is unavailable" end
  local ok, encoded = pcall(yajl.to_string, aardwolf_mudlet.settings.validate(aardwolf_mudlet.settings.data))
  return ok and encoded or nil, ok and nil or encoded
end

local function write_fixed(name, contents)
  if not aardwolf_mudlet.settings.ensure_directory() then return false, "Unable to create the package data directory" end
  local path = aardwolf_mudlet.settings.directory() .. "/" .. name
  local temporary = path .. ".tmp"
  local file, error_message = io.open(temporary, "w")
  if not file then return false, error_message end
  file:write(contents); file:close()
  local ok, rename_error = os.rename(temporary, path)
  return ok == true, ok and path or rename_error
end

function data_api.export()
  local encoded, error_message = encode_settings()
  if not encoded then return false, error_message end
  return write_fixed("settings-export.json", encoded)
end

function data_api.backup()
  local encoded, error_message = encode_settings()
  if not encoded then return false, error_message end
  local ok,value=write_fixed("settings-backup-" .. os.date("%Y%m%d-%H%M%S") .. ".json", encoded)
  if ok then prune_backups() end
  return ok,value
end

function data_api.set_auto_backup(value)
  aardwolf_mudlet.settings.data.data.auto_backup=value==true
  return aardwolf_mudlet.settings.save()
end

function data_api.import()
  local path = aardwolf_mudlet.settings.directory() .. "/settings-export.json"
  local file = io.open(path, "r")
  if not file then return false, "No settings-export.json exists in the package data directory" end
  local contents = file:read("*a"); file:close()
  if #contents &gt; 1024 * 1024 then return false, "Settings export is too large" end
  if type(yajl) ~= "table" or type(yajl.to_value) ~= "function" then return false, "Mudlet JSON support is unavailable" end
  local ok, decoded = pcall(yajl.to_value, contents)
  if not ok or type(decoded) ~= "table" then return false, "Settings export is invalid JSON" end
  aardwolf_mudlet.settings.data = aardwolf_mudlet.settings.validate(decoded)
  local saved, save_error = aardwolf_mudlet.settings.save()
  if saved and aardwolf_mudlet.ui then aardwolf_mudlet.ui.destroy(); aardwolf_mudlet.ui.build(); aardwolf_mudlet.ui.show() end
  return saved, save_error or path
end

function data_api.on_profile_save()
  local config = aardwolf_mudlet.settings.data and aardwolf_mudlet.settings.data.data
  if config and config.auto_backup then data_api.backup() end
end

function data_api.status()
  local config = aardwolf_mudlet.settings.data and aardwolf_mudlet.settings.data.data or {}
  return string.format("directory=%s auto-backup=%s retain=%s", aardwolf_mudlet.settings.directory(), tostring(config.auto_backup == true), tostring(config.retain_backups or 30))
end
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.capture</name><script>aardwolf_mudlet = aardwolf_mudlet or {}
aardwolf_mudlet.capture = aardwolf_mudlet.capture or {}

local capture = aardwolf_mudlet.capture

function capture.start(command)
  command = aardwolf_mudlet.util.text(command, 200)
  if not command or command == "" or command:find("[%c]") then return false, "Capture command must be one printable line" end
  if aardwolf_mudlet.state.connection ~= "connected" then return false, "Capture requires an active connection" end
  capture.active = {command = command, token = command:match("^%s*([%w_]+)"), lines = {}, started = false, deadline = os.time() + 30, overflow = false}
  send(command, false)
  return true
end

function capture.on_tag(token, phase)
  local active = capture.active
  if not active or not active.token or tostring(token or ""):lower() ~= active.token:lower() then return false end
  if phase == "Start" then active.started = true; return true end
  if phase == "End" and active.started then
    aardwolf_mudlet.state.record("capture", {command = active.command, lines = active.lines, overflow = active.overflow}, "current")
    capture.active = nil
    if aardwolf_mudlet.ui and aardwolf_mudlet.ui.request_render then aardwolf_mudlet.ui.request_render() end
    return true
  end
  return false
end

function capture.on_line(line)
  local active = capture.active
  if not active or not active.started then return false end
  line = aardwolf_mudlet.util.text(line, 2000)
  if not line or line:match("^%{Command:[%w_]+ (Start|End)%}$") then return false end
  if #active.lines &gt;= 500 then active.overflow = true; return false end
  active.lines[#active.lines + 1] = line
  return true
end

function capture.expire()
  if capture.active and os.time() &gt; capture.active.deadline then
    aardwolf_mudlet.state.record("capture", {command = capture.active.command, lines = capture.active.lines, overflow = capture.active.overflow}, "error", "Capture timed out after 30 seconds")
    capture.active = nil
    return true
  end
  return false
end

function capture.cancel() capture.active = nil end

function capture.copy()
  if type(setClipboardText) ~= "function" then return false, "Clipboard support is unavailable" end
  local value = aardwolf_mudlet.state.value("capture")
  setClipboardText(table.concat(type(value.lines) == "table" and value.lines or {}, "\n"))
  return true
end
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.details</name><script>aardwolf_mudlet.details = aardwolf_mudlet.details or {runtime={generation=0,queue={},capture=nil,trigger_id=nil}}
local LIMIT, TIMER = 100, "aardwolf-mudlet::details-timeout"
local QUEUE_TIMER = "aardwolf-mudlet::details-queue"

local function details() return aardwolf_mudlet.state.value("details") end
local function section_for(kind) return kind == "eqdata" and "equipment" or "bags" end
local function finish(success, message)
  local runtime, capture = aardwolf_mudlet.details.runtime, aardwolf_mudlet.details.runtime.capture
  if not capture then return end
  runtime.capture=nil
  if deleteNamedTimer then pcall(deleteNamedTimer, "aardwolf-mudlet", TIMER) end
  if not success then
    runtime.queue={}
    local previous=aardwolf_mudlet.util.copy(runtime.previous or details())
    previous.refreshing=false; previous.stale=true; previous.error=message
    previous.errors=previous.errors or {}; previous.errors[#previous.errors+1]=message
    previous.sections=previous.sections or {}
    previous.sections[section_for(capture.kind)]={status="error",received_at=os.time(),error=message}
    runtime.previous=nil
    aardwolf_mudlet.state.set_details(previous,"stale",message)
    if aardwolf_mudlet.ui then aardwolf_mudlet.ui.request_render() end
    return
  end
  local value=details()
  value.refreshing=#runtime.queue&gt;0
  value.last_updated=os.time()
  value.stale=false
  value.sections[section_for(capture.kind)]={status="current",received_at=os.time(),error=nil}
  if #runtime.queue==0 then value.error=nil; runtime.previous=nil end
  aardwolf_mudlet.state.set_details(value,#runtime.queue&gt;0 and "partial" or "current")
  if aardwolf_mudlet.ui then aardwolf_mudlet.ui.request_render() end
  if #runtime.queue&gt;0 then
    if deleteNamedTimer then pcall(deleteNamedTimer,"aardwolf-mudlet",QUEUE_TIMER) end
    registerNamedTimer("aardwolf-mudlet",QUEUE_TIMER,(capture.kind=="invdetails" or capture.kind=="bagdata") and 1 or 0.1,aardwolf_mudlet.details.next,false)
  end
end

local function fields(csv)
  local result={}; for field in (csv..","):gmatch("(.-),") do result[#result+1]=field end; return result
end
local function pipe_fields(value) local result={}; for field in (value.."|"):gmatch("(.-)|") do result[#result+1]=field end; return result end
local function item(payload)
  local f=fields(payload)
  if #f~=8 or not tonumber(f[1]) or not f[3] or f[3]=="" or not tonumber(f[4]) or not tonumber(f[5])
      or (f[6]~="0" and f[6]~="1") or not tonumber(f[7]) or not tonumber(f[8]) then return nil end
  return {id=tonumber(f[1]),flags=f[2],name=f[3]:gsub("@x%d%d%d",""):gsub("@.",""),level=tonumber(f[4]),type=tonumber(f[5]),unique=f[6]=="1",slot=tonumber(f[7]),timer=tonumber(f[8])}
end
local function tagged_payload(line,name) return line:match("^%{"..name.."%}%s*(.-)%s*$") end
local function opening_argument(line,name)
  local argument=line:match("^%{"..name.."%s*([^}]*)%}%s*$")
  if argument==nil then return nil end
  return argument:match("^%s*(.-)%s*$")
end
local function find_bag(value,id)
  for _,bag in ipairs(value.bags or {}) do if bag.id==id then return bag end end
end
local function append(capture,parsed)
  if #capture.rows&gt;=LIMIT then finish(false,"Tagged response exceeded record limit"); return false end
  capture.rows[#capture.rows+1]=parsed
  return true
end

function aardwolf_mudlet.details.capture_timeout() finish(false,"Tagged response timed out; previous valid data retained") end
function aardwolf_mudlet.details.next()
  local runtime=aardwolf_mudlet.details.runtime
  if runtime.capture or #runtime.queue==0 or aardwolf_mudlet.state.connection~="connected" then return end
  local request=table.remove(runtime.queue,1)
  runtime.capture={kind=request.kind,id=request.id,rows={},generation=runtime.generation,invalid=0,opened=false}
  send(request.command,false)
  registerNamedTimer("aardwolf-mudlet",TIMER,3,aardwolf_mudlet.details.capture_timeout,false)
end

local function capture_item_row(capture,line,predicate)
  if not capture.opened then return false end
  local parsed=item(line)
  if not parsed then
    if line:match("^%d+,[^,]*,") then capture.invalid=capture.invalid+1 end
    return false
  end
  if predicate and not predicate(parsed) then capture.invalid=capture.invalid+1; return true end
  append(capture,parsed)
  return true
end

function aardwolf_mudlet.details.capture_line(raw)
  local line=tostring(raw or ""):gsub("\27%[[0-9;]*m",""):match("^%s*(.-)%s*$")
  local capture=aardwolf_mudlet.details.runtime.capture
  if not capture then return false end

  if capture.kind=="eqdata" then
    local argument=opening_argument(line,"eqdata")
    if argument~=nil then
      if argument~="" then finish(false,"Equipment opening tag was malformed"); return true end
      capture.opened=true; return true
    end
    if line=="{/eqdata}" then
      if not capture.opened then finish(false,"Equipment response ended before its opening tag"); return true end
      if capture.invalid&gt;0 then finish(false,"Malformed equipment response; previous valid snapshot retained"); return true end
      local replacement={}; for _,record in ipairs(capture.rows) do replacement[record.slot]=record end
      details().equipment=replacement
      finish(true)
      return true
    end
    return capture_item_row(capture,line,function(record) return record.slot&gt;=0 and record.slot&lt;=32 end)
  end

  if capture.kind=="invdata" or capture.kind=="bagdata" then
    local argument=opening_argument(line,"invdata")
    if argument~=nil then
      if capture.kind=="bagdata" then
        local response_id=tonumber(argument)
        if not response_id or response_id~=capture.id then finish(false,"Container ID did not match invdata request"); return true end
        capture.verified_id=true
      elseif argument~="" then finish(false,"Top-level inventory returned an unexpected container ID"); return true end
      capture.opened=true
      return true
    end
    if line=="{/invdata}" then
      if not capture.opened or (capture.kind=="bagdata" and not capture.verified_id) then finish(false,"Inventory response was not verified"); return true end
      if capture.invalid&gt;0 then finish(false,"Malformed inventory response; previous valid snapshot retained"); return true end
      local value=details()
      if capture.kind=="invdata" then
        value.inventory=capture.rows
        value.bags={}
        for _,record in ipairs(capture.rows) do
          if record.type==11 then
            value.bags[#value.bags+1]={id=record.id,name=record.name,level=record.level,type=record.type,flags=record.flags,unique=record.unique,timer=record.timer,items={},pending=true,status="partial"}
          end
        end
        for _,bag in ipairs(value.bags) do
          aardwolf_mudlet.details.runtime.queue[#aardwolf_mudlet.details.runtime.queue+1]={kind="invdetails",command="invdetails "..bag.id,id=bag.id}
          aardwolf_mudlet.details.runtime.queue[#aardwolf_mudlet.details.runtime.queue+1]={kind="bagdata",command="invdata "..bag.id,id=bag.id}
        end
      else
        local bag=find_bag(value,capture.id)
        if bag then bag.items=capture.rows; bag.pending=false; bag.status="current" end
      end
      finish(true)
      return true
    end
    return capture_item_row(capture,line)
  end

  if capture.kind=="invdetails" then
    local argument=opening_argument(line,"invdetails")
    if argument~=nil then
      if argument~="" then finish(false,"Container detail opening tag was malformed"); return true end
      capture.opened=true; return true
    end
    if line=="{/invdetails}" then
      if not capture.opened or not capture.verified_id then finish(false,"Container ID was not verified"); return true end
      if capture.max_weight==nil or capture.used_weight==nil then finish(false,"Container weights were malformed; previous valid snapshot retained"); return true end
      local value=details(); local bag=find_bag(value,capture.id)
      if bag then
        bag.own_weight=capture.header_weight
        bag.max_weight=capture.max_weight
        bag.used_weight=capture.used_weight or capture.header_weight
        bag.pending=true
        bag.status="partial"
      end
      finish(true)
      return true
    end
    if not capture.opened then return false end
    local header=tagged_payload(line,"invheader")
    if header then
      local f=pipe_fields(header); local response_id=tonumber(f[1])
      if not response_id or response_id~=capture.id then finish(false,"Container ID did not match request"); return true end
      capture.verified_id=true; capture.header_weight=tonumber(f[5]); return true
    end
    local container=tagged_payload(line,"container")
    if container then
      if not capture.verified_id then finish(false,"Container details arrived before verified header"); return true end
      local f=pipe_fields(container); capture.max_weight=tonumber(f[1]); capture.used_weight=tonumber(f[3]); return true
    end
    return false
  end
  return false
end

function aardwolf_mudlet.details.on_line()
  local line=line or (getCurrentLine and getCurrentLine()) or ""
  if aardwolf_mudlet.details.capture_line(line) and deleteLine then deleteLine() end
end
function aardwolf_mudlet.details.refresh()
  if aardwolf_mudlet.state.connection~="connected" then return false,"Disconnected" end
  aardwolf_mudlet.details.start()
  local runtime=aardwolf_mudlet.details.runtime; runtime.generation=runtime.generation+1; runtime.queue={{kind="eqdata",command="eqdata"},{kind="invdata",command="invdata"}}
  local value=details(); runtime.previous=aardwolf_mudlet.util.copy(value); value.refreshing=true; value.error=nil; value.errors={}; value.generation=runtime.generation; aardwolf_mudlet.state.set_details(value,"partial")
  aardwolf_mudlet.details.next()
  return true
end
function aardwolf_mudlet.details.start()
  if not aardwolf_mudlet.details.runtime.trigger_id and tempRegexTrigger then
    aardwolf_mudlet.details.runtime.trigger_id=tempRegexTrigger("^.*$",aardwolf_mudlet.details.on_line)
  end
end
function aardwolf_mudlet.details.stop(reason)
  if deleteNamedTimer then pcall(deleteNamedTimer,"aardwolf-mudlet",TIMER); pcall(deleteNamedTimer,"aardwolf-mudlet",QUEUE_TIMER) end
  if aardwolf_mudlet.details.runtime.trigger_id and killTrigger then pcall(killTrigger,aardwolf_mudlet.details.runtime.trigger_id) end
  aardwolf_mudlet.details.runtime.trigger_id=nil
  aardwolf_mudlet.details.runtime.capture=nil; aardwolf_mudlet.details.runtime.queue={}; aardwolf_mudlet.details.runtime.previous=nil
  if type(reason)=="string" then local value=details(); value.refreshing=false; value.stale=true; value.error=reason; aardwolf_mudlet.state.set_details(value,"stale",reason) end
end
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.actions</name><script>aardwolf_mudlet.actions = aardwolf_mudlet.actions or {}
aardwolf_mudlet.actions.pending_action = nil

local curated = {
  look={id="look",label="Look",category="Room",command="look"}, exits={id="exits",label="Exits",category="Room",command="exits"},
  scan={id="scan",label="Scan",category="Room",command="scan"}, score={id="score",label="Score",category="Character",command="score"},
  affects={id="affects",label="Affects",category="Character",command="aff"}, attributes={id="attributes",label="Attributes",category="Character",command="attr"},
  resists={id="resists",label="Resists",category="Character",command="resists"},
  character_refresh={id="character-refresh",label="Refresh Character",category="Refresh",local_action="character"},
  quest_refresh={id="quest-refresh",label="Refresh Quest",category="Refresh",local_action="quest"},
  details_refresh={id="details-refresh",label="Refresh Inventory",category="Refresh",local_action="details"},
  map_import={id="map-import",label="Import Map",category="Map",local_action="import"},
  map_center={id="map-center",label="Center Map",category="Map",local_action="center"},
  map_zoom_in={id="map-zoom-in",label="Zoom In",category="Map",local_action="zoom-in"},
  map_zoom_out={id="map-zoom-out",label="Zoom Out",category="Map",local_action="zoom-out"},
}
function aardwolf_mudlet.actions.curated()
  local result={}
  for _,action in pairs(curated) do result[#result+1]=action end
  table.sort(result,function(left,right) if left.category==right.category then return left.label&lt;right.label end return left.category&lt;right.category end)
  return result
end

function aardwolf_mudlet.actions.gameplay_available()
  if aardwolf_mudlet.state.connection ~= "connected" then return false end
  local state = tonumber(aardwolf_mudlet.state.value("status").state)
  return not ({[1]=true,[2]=true,[5]=true,[6]=true,[7]=true})[state]
end

function aardwolf_mudlet.actions.context()
  local result = {curated.look, curated.exits, curated.scan}
  local exits = aardwolf_mudlet.state.value("room").exits or {}
  for _, direction in ipairs({"n","e","s","w","u","d"}) do
    if exits[direction] ~= nil then
      result[#result + 1] = {id="exit-"..direction,label=direction:upper(),category="Exits",command=direction}
      break
    end
  end
  if #result &lt; 4 then result[#result + 1] = curated.score end
  return result
end

local function safe_curated(action)
  if not action or type(action.command) ~= "string" then return false end
  if action.id:match("^exit%-[neswud]$") then return action.command == action.id:sub(-1) end
  return curated[action.id] == action
end

function aardwolf_mudlet.actions.execute(id)
  if id=="character-refresh" then return aardwolf_mudlet.lifecycle.request_character(true) end
  if id=="quest-refresh" then return aardwolf_mudlet.lifecycle.request_quest(true) end
  if id=="details-refresh" then aardwolf_mudlet.details.refresh(); return true end
  if id=="map-import" then
    if aardwolf_mudlet.map and aardwolf_mudlet.map.start_import then aardwolf_mudlet.map.start_import(); return true end
    return false, "The packaged map importer is unavailable"
  end
  if id=="map-center" then aardwolf_mudlet.lifecycle.center_map(); return true end
  if id=="map-zoom-in" or id=="map-zoom-out" then
    local mapper=aardwolf_mudlet.ui and aardwolf_mudlet.ui.mapper
    local method=id=="map-zoom-in" and "zoomIn" or "zoomOut"
    return aardwolf_mudlet.util.safe_call(mapper,method)
  end
  if not aardwolf_mudlet.actions.gameplay_available() then return false, "Gameplay actions are unavailable in the current connection state" end
  local action = curated[id]
  if not action and type(id) == "string" and id:match("^exit%-[neswud]$") then
    local direction = id:sub(-1)
    if aardwolf_mudlet.state.value("room").exits[direction] ~= nil then action={id=id,command=direction} end
  end
  if safe_curated(action) then send(action.command, false); return true end
  for _, custom in ipairs(aardwolf_mudlet.settings.data.custom_actions or {}) do
    if custom.id == id then send(custom.command, false); return true end
  end
  return false, "Unknown action"
end

function aardwolf_mudlet.actions.confirm()
  local pending = aardwolf_mudlet.actions.pending_action
  aardwolf_mudlet.actions.pending_action = nil
  if not pending or not aardwolf_mudlet.actions.gameplay_available() then return false end
  send(pending.command, false)
  return true
end
function aardwolf_mudlet.actions.cancel() aardwolf_mudlet.actions.pending_action = nil end
function aardwolf_mudlet.actions.pending() return aardwolf_mudlet.actions.pending_action end
aardwolf_mudlet.actions.confirm_pending=aardwolf_mudlet.actions.confirm
aardwolf_mudlet.actions.cancel_pending=aardwolf_mudlet.actions.cancel

local function valid(label, command, category)
  category=category or "Custom"
  return type(label)=="string" and label:match("%S") and #label&lt;=32 and not label:find("[%c]")
    and type(category)=="string" and category:match("%S") and #category&lt;=24 and not category:find("[%c]")
    and type(command)=="string" and command:match("%S") and #command&lt;=200 and not command:find("[%c]")
end
function aardwolf_mudlet.actions.add(label, command, category)
  local list = aardwolf_mudlet.settings.data.custom_actions
  if #list &gt;= 24 or not valid(label, command, category) then return false, "Action requires a printable 1-32 character label, 1-24 character category, and 1-200 character command" end
  local numeric = aardwolf_mudlet.settings.data.next_action_id or 1
  list[#list+1] = {id="custom-"..numeric,label=label,command=command,category=category or "Custom",order=#list+1}
  aardwolf_mudlet.settings.data.next_action_id = numeric + 1
  aardwolf_mudlet.settings.save(); return true
end
function aardwolf_mudlet.actions.update(id, label, command, category)
  if not valid(label, command, category) then return false end
  for _, action in ipairs(aardwolf_mudlet.settings.data.custom_actions) do if action.id==id then action.label=label; action.command=command; action.category=category or action.category; aardwolf_mudlet.settings.save(); return true end end
  return false
end
function aardwolf_mudlet.actions.remove(id)
  for index, action in ipairs(aardwolf_mudlet.settings.data.custom_actions) do if action.id==id then table.remove(aardwolf_mudlet.settings.data.custom_actions,index); aardwolf_mudlet.settings.save(); return true end end
  return false
end
function aardwolf_mudlet.actions.move(id, order)
  order=math.floor(tonumber(order) or 0); local list=aardwolf_mudlet.settings.data.custom_actions
  if order&lt;1 or order&gt;#list then return false end
  for index, action in ipairs(list) do if action.id==id then table.remove(list,index); table.insert(list,order,action); for position,item in ipairs(list) do item.order=position end; aardwolf_mudlet.settings.save(); return true end end
  return false
end
function aardwolf_mudlet.actions.custom() return aardwolf_mudlet.settings.data.custom_actions or {} end
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.map</name><script>-- Merge-safe Aardwolf.db/v11 importer for the monolithic aardwolf-mudlet package.
-- The packaged JSON is data, never a Mudlet map backup: this module deliberately
-- does not load, clear, close, or replace the profile map.
aardwolf_mudlet = aardwolf_mudlet or {}
aardwolf_mudlet.map = aardwolf_mudlet.map or {}

local map = aardwolf_mudlet.map
map.runtime = map.runtime or {}
local unpack_values = unpack or table.unpack

local OWNER_KEY = "aardwolf_map.owner"
local OWNER_VALUE = "Aardwolf.db/v11"
local KEY_PREFIX = "aardwolf_map."
local TIMER_USER = "aardwolf_mudlet"
local TIMER_NAME = "aardwolf-mudlet::map::import"
local ROOM_HANDLER = "aardwolf-mudlet::map::room-info"
local EXIT_HANDLER = "aardwolf-mudlet::map::exit"
local UNINSTALL_HANDLER = "aardwolf-mudlet::map::uninstall"
local STATUS_EVENT = "aardwolf-mudlet::map-status-changed"
local FINISHED_EVENT = "aardwolf-mudlet::map-import-finished"
local BATCH_SIZE = 100
local ENVIRONMENT_ID_START = 1000
local ENVIRONMENT_ID_LIMIT = 65535

local palettes = {
  source = {
    [0] = {0, 0, 0}, [1] = {128, 0, 0}, [2] = {0, 128, 0}, [3] = {128, 128, 0},
    [4] = {0, 0, 128}, [5] = {128, 0, 128}, [6] = {0, 128, 128}, [7] = {192, 192, 192},
    [8] = {128, 128, 128}, [9] = {255, 0, 0}, [10] = {0, 255, 0}, [11] = {255, 255, 0},
    [12] = {0, 0, 255}, [13] = {255, 0, 255}, [14] = {0, 255, 255}, [15] = {255, 255, 255},
  },
  obsidian = {
    [0] = {13, 17, 23}, [1] = {134, 48, 72}, [2] = {46, 139, 87}, [3] = {184, 134, 11},
    [4] = {45, 87, 154}, [5] = {125, 70, 152}, [6] = {25, 135, 143}, [7] = {184, 192, 204},
    [8] = {91, 99, 112}, [9] = {232, 85, 106}, [10] = {75, 210, 130}, [11] = {245, 194, 66},
    [12] = {82, 143, 255}, [13] = {190, 111, 229}, [14] = {65, 208, 218}, [15] = {245, 247, 250},
  },
  ["high-contrast"] = {
    [0] = {0, 0, 0}, [1] = {180, 0, 0}, [2] = {0, 150, 0}, [3] = {180, 120, 0},
    [4] = {0, 70, 210}, [5] = {160, 0, 180}, [6] = {0, 150, 170}, [7] = {210, 210, 210},
    [8] = {110, 110, 110}, [9] = {255, 70, 70}, [10] = {60, 255, 90}, [11] = {255, 230, 0},
    [12] = {80, 150, 255}, [13] = {255, 80, 255}, [14] = {50, 240, 255}, [15] = {255, 255, 255},
  },
}

local long_direction = {n = "north", e = "east", s = "south", w = "west", u = "up", d = "down"}
local required_mapper_apis = {
  "addAreaName", "addRoom", "clearRoomUserDataItem", "createRoomID", "getAreaTable", "getAreaUserData", "getCustomEnvColorTable", "getMapUserData",
  "getRoomEnv", "getRoomExits", "getRoomIDbyHash", "getRooms", "getRoomUserData", "registerNamedTimer",
  "setAreaUserData", "setCustomEnvColor", "setExit", "setMapUserData", "setRoomArea", "setRoomCoordinates",
  "setRoomEnv", "setRoomIDbyHash", "setRoomName", "setRoomUserData",
}

local function message(text)
  if type(echo) == "function" then echo("\n[aardwolf-mudlet map] " .. tostring(text) .. "\n") end
end

local function safe_call(fn, ...)
  if type(fn) ~= "function" then return false, "Mudlet mapper API is unavailable." end
  return pcall(fn, ...)
end

local function encode(value)
  if type(yajl) ~= "table" or type(yajl.to_string) ~= "function" then return nil end
  local ok, result = pcall(yajl.to_string, value)
  if ok and type(result) == "string" then return result end
  return nil
end

local function decode(value)
  if type(value) ~= "string" or value == "" or type(yajl) ~= "table" or type(yajl.to_value) ~= "function" then return nil end
  local ok, result = pcall(yajl.to_value, value)
  if ok then return result end
  return nil
end

local function map_key(name) return KEY_PREFIX .. name end

local function get_map_value(name)
  if type(getMapUserData) ~= "function" then return nil end
  local ok, value = pcall(getMapUserData, map_key(name))
  if ok then return value end
  return nil
end

local function set_map_value(name, value)
  if type(setMapUserData) ~= "function" then return false end
  local ok, result = pcall(setMapUserData, map_key(name), tostring(value or ""))
  return ok and result ~= false
end

local function get_map_table(name)
  local value = decode(get_map_value(name))
  return type(value) == "table" and value or {}
end

local function palette_setting()
  local value = get_map_value("palette")
  return palettes[value] and value or "source"
end

local function set_map_table(name, value)
  local encoded = encode(value)
  return encoded and set_map_value(name, encoded) or false
end

local function hash_for_vnum(vnum) return "aardwolf-map:vnum:" .. tostring(vnum) end

local function room_ids()
  if type(getRooms) ~= "function" then return {} end
  local ok, rooms = pcall(getRooms)
  if not ok or type(rooms) ~= "table" then return {} end
  local result, seen = {}, {}
  for key, value in pairs(rooms) do
    local room_id = tonumber(key)
    if not room_id or room_id ~= math.floor(room_id) then room_id = tonumber(value) end
    if room_id and room_id == math.floor(room_id) and room_id &gt;= 0 and not seen[room_id] then
      seen[room_id] = true
      result[#result + 1] = room_id
    end
  end
  table.sort(result)
  return result
end

local function room_data(room_id, key)
  if type(getRoomUserData) ~= "function" then return nil end
  local ok, value = pcall(getRoomUserData, room_id, map_key(key))
  return ok and value or nil
end

local function is_owned_room(room_id)
  if type(getRoomUserData) ~= "function" then return false end
  local ok, value = pcall(getRoomUserData, room_id, OWNER_KEY)
  return ok and value == OWNER_VALUE
end

local function is_owned_for_vnum(room_id, vnum)
  return is_owned_room(room_id) and tonumber(room_data(room_id, "vnum")) == tonumber(vnum)
end

local function write_room_data(room_id, key, value)
  if value == nil or value == "" then
    local ok, result = pcall(clearRoomUserDataItem, room_id, map_key(key))
    -- Mudlet returns false when the key was already absent. That is the
    -- desired idempotent state for an empty source value; nil indicates an
    -- invalid room or unavailable map and remains a real persistence error.
    return ok and result ~= nil
  end
  local ok, result = pcall(setRoomUserData, room_id, map_key(key), tostring(value))
  return ok and result ~= false
end

local function owned_room_count()
  local count = 0
  for _, room_id in ipairs(room_ids()) do if is_owned_room(room_id) then count = count + 1 end end
  return count
end

local function build_owned_vnum_index()
  local index = {}
  for _, room_id in ipairs(room_ids()) do
    if is_owned_room(room_id) then
      local vnum = tonumber(room_data(room_id, "vnum"))
      if vnum and vnum == math.floor(vnum) and not index[tostring(vnum)] then index[tostring(vnum)] = room_id end
    end
  end
  return index
end

local function room_id_for_vnum(vnum)
  local room_id
  if type(getRoomIDbyHash) == "function" then
    local ok, value = pcall(getRoomIDbyHash, hash_for_vnum(vnum))
    if ok then room_id = tonumber(value) end
  end
  if room_id and room_id &gt;= 0 then return room_id end
  return map.runtime.owned_by_vnum and map.runtime.owned_by_vnum[tostring(vnum)] or nil
end

local function copy_status()
  local current = map.runtime
  local room_total, exit_total = current.room_total or 0, current.exit_total or 0
  local completed = (current.room_index or 0) + (current.exit_index or 0) + (current.reconcile_index or 0)
  local total = room_total + exit_total + (current.write_exits and room_total or 0)
  return {
    phase = current.phase or "idle",
    source_hash = current.source_hash or get_map_value("completed_source_sha256"),
    completed_source_hash = get_map_value("completed_source_sha256"),
    room_index = current.room_index or 0, room_total = room_total,
    exit_index = current.exit_index or 0, exit_total = exit_total,
    reconcile_index = current.reconcile_index or 0,
    percent = total &gt; 0 and math.floor(completed * 1000 / total) / 10 or 0,
    created_rooms = current.created_rooms or 0, reused_rooms = current.reused_rooms or 0,
    skipped_rooms = current.skipped_rooms or 0, imported_exits = current.imported_exits or 0,
    skipped_exits = current.skipped_exits or 0, collision_count = current.collision_count or 0,
    owned_count = current.owned_count or 0, registered_environments = current.registered_environments or 0,
    palette = palettes[current.palette] and current.palette or palette_setting(),
    resume_pending = current.resume_pending == true,
    current_vnum = current.current_vnum, resolved_room_id = current.resolved_room_id,
    error = current.error_message or current.palette_error, updated_at = current.updated_at,
  }
end

local function emit_status()
  map.runtime.updated_at = os.time()
  if type(raiseEvent) == "function" then pcall(raiseEvent, STATUS_EVENT, copy_status()) end
end

local function delete_runtime_objects()
  if type(deleteNamedTimer) == "function" then pcall(deleteNamedTimer, TIMER_USER, TIMER_NAME) end
  if type(deleteNamedEventHandler) == "function" then
    pcall(deleteNamedEventHandler, TIMER_USER, ROOM_HANDLER)
    pcall(deleteNamedEventHandler, TIMER_USER, EXIT_HANDLER)
    pcall(deleteNamedEventHandler, TIMER_USER, UNINSTALL_HANDLER)
  end
end

local function resource_path()
  return getMudletHomeDir() .. "/aardwolf-mudlet/resources/aardwolf-map-v11.json"
end

local function valid_integer(value)
  local number = tonumber(value)
  return number and number == number and number ~= math.huge and number ~= -math.huge
    and number == math.floor(number) and number or nil
end

local function nonnegative_integer(value)
  local number = valid_integer(value)
  return number and number &gt;= 0 and number or nil
end

local function cursor(value, maximum)
  return math.max(0, math.min(nonnegative_integer(value) or 0, maximum))
end

local function validate_resource(data)
  if type(data) ~= "table" or data.schema_version ~= 1 or type(data.source) ~= "table"
      or type(data.source.sha256) ~= "string" or #data.source.sha256 ~= 64
      or not data.source.sha256:match("^[0-9a-f]+$") then
    return nil, "The packaged map resource has an unsupported schema."
  end
  if type(data.areas) ~= "table" or type(data.environments) ~= "table" or type(data.rooms) ~= "table"
      or type(data.exits) ~= "table" or type(data.import_area_uids) ~= "table" then
    return nil, "The packaged map resource is missing map records."
  end
  local areas, environments, rooms = {}, {}, {}
  for _, area in ipairs(data.areas) do
    if type(area) ~= "table" or type(area.uid) ~= "string" or area.uid == "" or type(area.name) ~= "string" then
      return nil, "The packaged map resource contains an invalid area."
    end
    areas[area.uid] = true
  end
  for _, environment in ipairs(data.environments) do
    local uid, color = valid_integer(environment and environment.uid), valid_integer(environment and environment.color)
    if not uid or not color or color &lt; 0 or color &gt; 15 or type(environment.name) ~= "string" then
      return nil, "The packaged map resource contains an invalid terrain record."
    end
    environments[tostring(uid)] = true
  end
  for _, room in ipairs(data.rooms) do
    local vnum = valid_integer(room and room.vnum)
    if not vnum or rooms[tostring(vnum)] or type(room.name) ~= "string" or not areas[room.area_uid]
        or not environments[tostring(valid_integer(room.terrain_uid))] or type(room.layout) ~= "table"
        or not valid_integer(room.layout[1]) or not valid_integer(room.layout[2]) or not valid_integer(room.layout[3]) then
      return nil, "The packaged map resource contains an invalid room record."
    end
    rooms[tostring(vnum)] = true
  end
  for _, exit_record in ipairs(data.exits) do
    if type(exit_record) ~= "table" or not long_direction[exit_record.direction]
        or not rooms[tostring(valid_integer(exit_record.from_vnum))]
        or not rooms[tostring(valid_integer(exit_record.to_vnum))] then
      return nil, "The packaged map resource contains an invalid exit record."
    end
  end
  if type(data.counts) == "table" then
    if tonumber(data.counts.rooms) ~= #data.rooms or tonumber(data.counts.standard_exits) ~= #data.exits then
      return nil, "The packaged map resource counts do not match its records."
    end
  end
  return data, nil
end

local function read_resource()
  if type(map._resource_override) == "table" then return validate_resource(map._resource_override) end
  local file, open_error = io.open(resource_path(), "rb")
  if not file then return nil, "The packaged map resource could not be opened: " .. tostring(open_error) end
  local contents = file:read("*a")
  file:close()
  if type(yajl) ~= "table" or type(yajl.to_value) ~= "function" then return nil, "Mudlet JSON support is unavailable." end
  local ok, value = pcall(yajl.to_value, contents)
  if not ok then return nil, "The packaged map resource is not valid JSON." end
  return validate_resource(value)
end

local function mapper_apis_available()
  for _, name in ipairs(required_mapper_apis) do
    if type(_G[name]) ~= "function" then return nil, "Required Mudlet mapper API is unavailable: " .. name .. "." end
  end
  return true, nil
end

local function is_reserved_environment_id(environment_id)
  return (environment_id &gt;= 1 and environment_id &lt;= 16) or (environment_id &gt;= 257 and environment_id &lt;= 272)
end

local function environment_usage()
  local usage = {}
  if type(getRoomEnv) ~= "function" then return usage end
  for _, room_id in ipairs(room_ids()) do
    local ok, value = pcall(getRoomEnv, room_id)
    local environment_id = ok and valid_integer(value) or nil
    if environment_id then
      usage[environment_id] = usage[environment_id] or {owned = {}, foreign = {}}
      local bucket = is_owned_room(room_id) and usage[environment_id].owned or usage[environment_id].foreign
      bucket[#bucket + 1] = room_id
    end
  end
  return usage
end

local function allocate_environment_id(unavailable)
  for environment_id = ENVIRONMENT_ID_START, ENVIRONMENT_ID_LIMIT do
    if not is_reserved_environment_id(environment_id) and not unavailable[environment_id] then return environment_id end
  end
  return nil
end

local function resolve_environments(data)
  local saved, usage, assigned = get_map_table("environment_ids"), environment_usage(), {}
  local unavailable = {}
  for id, buckets in pairs(usage) do if #buckets.foreign &gt; 0 then unavailable[id] = true end end
  local colors_ok, custom_colors = pcall(getCustomEnvColorTable)
  if colors_ok and type(custom_colors) == "table" then
    for id in pairs(custom_colors) do id=valid_integer(id); if id then unavailable[id]=true end end
  end
  local resolved, migrations = {}, {}
  for _, environment in ipairs(data.environments) do
    local source_uid = tostring(environment.uid)
    local candidate = valid_integer(saved[source_uid])
    local collision = candidate and (candidate &lt;= 0 or is_reserved_environment_id(candidate) or assigned[candidate]
      or (usage[candidate] and #usage[candidate].foreign &gt; 0))
    if not candidate then
      local legacy = valid_integer(environment.uid) and 1000 + tonumber(environment.uid) or nil
      local legacy_usage = legacy and usage[legacy]
      if legacy and not is_reserved_environment_id(legacy) and legacy_usage and #legacy_usage.owned &gt; 0
          and #legacy_usage.foreign == 0 and not assigned[legacy] then candidate = legacy end
    end
    if not candidate or collision then
      if candidate then unavailable[candidate] = true end
      local replacement = allocate_environment_id(unavailable)
      if not replacement then return nil, nil, "No collision-free Mudlet custom environment ID is available." end
      if candidate then migrations[candidate] = replacement end
      candidate = replacement
    end
    resolved[source_uid], assigned[candidate], unavailable[candidate] = candidate, true, true
  end
  if not set_map_table("environment_ids", resolved) then
    return nil, nil, "The terrain environment ownership mapping could not be persisted."
  end
  return resolved, migrations, nil
end

local function migrate_owned_environments(migrations)
  if type(getRoomEnv) ~= "function" or type(setRoomEnv) ~= "function" then return end
  for _, room_id in ipairs(room_ids()) do
    if is_owned_room(room_id) then
      local ok, old_id = pcall(getRoomEnv, room_id)
      local replacement = ok and migrations[tonumber(old_id)] or nil
      if replacement then pcall(setRoomEnv, room_id, replacement) end
    end
  end
end

local function register_environments(data)
  if type(setCustomEnvColor) ~= "function" then return nil, "Mudlet custom environment colors are unavailable." end
  for attempt = 1, 2 do
    local ids, migrations, allocation_error = resolve_environments(data)
    if not ids then return nil, allocation_error end
    migrate_owned_environments(migrations)
    local palette_name = palette_setting()
    local usage, retry, registered = environment_usage(), false, 0
    for _, environment in ipairs(data.environments) do
      local environment_id = ids[tostring(environment.uid)]
      if usage[environment_id] and #usage[environment_id].foreign &gt; 0 then
        retry = true
        break
      end
      local rgb = palettes[palette_name][tonumber(environment.color)]
      local ok, result = pcall(setCustomEnvColor, environment_id, rgb[1], rgb[2], rgb[3], 255)
      if not ok or result == false then return nil, "Could not register terrain color for " .. tostring(environment.name) .. "." end
      registered = registered + 1
    end
    if not retry then
      map.runtime.environment_ids = ids
      map.runtime.palette = palette_name
      map.runtime.registered_environments = registered
      set_map_value("registered_environment_count", registered)
      return registered, nil
    end
  end
  return nil, "A terrain environment was claimed by a foreign room during allocation; retry the import."
end

local function area_is_owned(area_id, source_uid)
  if type(getAreaUserData) ~= "function" then return false end
  local ok_owner, owner = pcall(getAreaUserData, area_id, OWNER_KEY)
  local ok_uid, uid = pcall(getAreaUserData, area_id, map_key("source_uid"))
  return ok_owner and ok_uid and owner == OWNER_VALUE and tostring(uid) == tostring(source_uid)
end

local function create_area(area)
  if type(addAreaName) ~= "function" then return nil, "Mudlet area APIs are unavailable." end
  local base = "Aardwolf (" .. tostring(area.uid) .. "): " .. tostring(area.name)
  for attempt = 1, 100 do
    local name = attempt == 1 and base or (base .. " [" .. tostring(attempt) .. "]")
    local ok, area_id = pcall(addAreaName, name)
    if ok and type(area_id) == "number" and area_id &gt;= 0 then return area_id, nil end
  end
  return nil, "Could not create a collision-free area for source area " .. tostring(area.uid) .. "."
end

local function find_owned_area(source_uid)
  if type(getAreaTable) ~= "function" then return nil end
  local ok, area_table = pcall(getAreaTable)
  if not ok or type(area_table) ~= "table" then return nil end
  for key, value in pairs(area_table) do
    local area_id = valid_integer(value) or valid_integer(key)
    if area_id and area_is_owned(area_id, source_uid) then return area_id end
  end
  return nil
end

local function ensure_areas(data)
  local saved, import_uids, areas = get_map_table("area_ids"), {}, {}
  for _, uid in ipairs(data.import_area_uids) do import_uids[tostring(uid)] = true end
  for _, area in ipairs(data.areas) do
    if import_uids[tostring(area.uid)] then
      local area_id = valid_integer(saved[area.uid])
      if not area_id or not area_is_owned(area_id, area.uid) then
        area_id = find_owned_area(area.uid)
      end
      if not area_id then
        local area_error
        area_id, area_error = create_area(area)
        if not area_id then return nil, area_error end
      end
      local area_values = {
        {OWNER_KEY, OWNER_VALUE}, {map_key("source_uid"), tostring(area.uid)},
        {map_key("source_sha256"), data.source.sha256}, {map_key("texture"), tostring(area.texture or "")},
        {map_key("source_color"), tostring(area.color or "")}, {map_key("flags"), tostring(area.flags or "")},
      }
      for _, pair in ipairs(area_values) do
        local ok, result = pcall(setAreaUserData, area_id, pair[1], pair[2])
        if not ok or result == false then return nil, "Could not mark source area " .. tostring(area.uid) .. " as package-owned." end
      end
      saved[area.uid], areas[area.uid] = area_id, area_id
    end
  end
  if not set_map_table("area_ids", saved) then return nil, "The source area ownership mapping could not be persisted." end
  return areas, nil
end

local function persist_progress()
  local current = map.runtime
  if not current.data or not (current.work_phase == "rooms" or current.work_phase == "exits" or current.work_phase == "reconcile") then return true end
  local progress = {
    source_hash = current.source_hash, phase = current.work_phase,
    room_total = current.room_total or 0, exit_total = current.exit_total or 0,
    room_index = current.room_index or 0, exit_index = current.exit_index or 0,
    reconcile_index = current.reconcile_index or 0, created_rooms = current.created_rooms or 0,
    reused_rooms = current.reused_rooms or 0, skipped_rooms = current.skipped_rooms or 0,
    imported_exits = current.imported_exits or 0, skipped_exits = current.skipped_exits or 0,
    collision_count = current.collision_count or 0, blocked_vnums = current.blocked_vnums or {},
    write_exits = current.write_exits == true,
  }
  if not set_map_table("import_progress", progress) then return false end
  return set_map_value("import_active_source_sha256", current.source_hash)
end

local function clear_transient_runtime()
  local current = map.runtime
  current.data, current.area_ids, current.environment_ids = nil, nil, nil
  current.owned_by_vnum, current.expected_exits, current.old_source_exits, current.blocked_vnums = nil, nil, nil, nil
  current.cancel_requested = false
end

local function set_error(error_message, skip_persist)
  if type(deleteNamedTimer) == "function" then pcall(deleteNamedTimer, TIMER_USER, TIMER_NAME) end
  map.runtime.phase, map.runtime.error_message, map.runtime.resume_pending = "error", error_message, true
  if not skip_persist then persist_progress() end
  emit_status()
  message(error_message)
end

local function apply_room_source(room_id, room, source_hash, area_ids, creating)
  local area_id, environment_id = area_ids[room.area_uid], map.runtime.environment_ids[tostring(room.terrain_uid)]
  if not area_id or not environment_id then return nil, "Source area or terrain is unavailable for vnum " .. tostring(room.vnum) .. "." end
  local calls = {
    {setRoomIDbyHash, room_id, hash_for_vnum(room.vnum)}, {setRoomName, room_id, room.name},
    {setRoomArea, room_id, area_id}, {setRoomCoordinates, room_id, room.layout[1], room.layout[2], room.layout[3]},
    {setRoomEnv, room_id, environment_id},
  }
  for _, call in ipairs(calls) do
    local ok, result = safe_call(call[1], unpack_values(call, 2))
    if not ok or result == false then return nil, "Mudlet could not update source vnum " .. tostring(room.vnum) .. "." end
  end
  local source_values = {
    {"area_uid", room.area_uid}, {"terrain", room.terrain}, {"terrain_uid", room.terrain_uid},
    {"info", room.info}, {"notes", room.notes}, {"building", room.building},
    {"norecall", room.norecall}, {"noportal", room.noportal},
    {"ignore_exits_mismatch", room.ignore_exits_mismatch},
    {"source_coordinates", encode(room.source_coordinates)}, {"source_sha256", source_hash},
  }
  for _, pair in ipairs(source_values) do
    if not write_room_data(room_id, pair[1], pair[2]) then
      return nil, "Mudlet could not persist source metadata for vnum " .. tostring(room.vnum) .. "."
    end
  end
  if creating then map.runtime.owned_by_vnum[tostring(room.vnum)] = room_id end
  return true, nil
end

local function create_source_room(room)
  if type(createRoomID) ~= "function" or type(addRoom) ~= "function" then return nil, "Mudlet room APIs are unavailable." end
  local ok_id, room_id = pcall(createRoomID)
  if not ok_id or type(room_id) ~= "number" or room_id &lt; 0 then return nil, "Mudlet could not allocate a room ID." end
  local ok_add, added = pcall(addRoom, room_id)
  if not ok_add or added == false then return nil, "Mudlet could not create a room for source vnum " .. tostring(room.vnum) .. "." end
  -- Mark immediately so a partially failed update remains safely resumable and
  -- can never be mistaken for foreign data.
  local owner_ok, owner_result = pcall(setRoomUserData, room_id, OWNER_KEY, OWNER_VALUE)
  if not owner_ok or owner_result == false or not write_room_data(room_id, "vnum", room.vnum) then
    return nil, "Mudlet could not mark source vnum " .. tostring(room.vnum) .. " as package-owned."
  end
  local applied, apply_error = apply_room_source(room_id, room, map.runtime.source_hash, map.runtime.area_ids, true)
  if not applied then return nil, apply_error end
  return room_id, nil
end

local function import_room(room)
  local current, vnum = map.runtime, room.vnum
  local room_id = room_id_for_vnum(vnum)
  if room_id then
    if not is_owned_for_vnum(room_id, vnum) then
      current.skipped_rooms = current.skipped_rooms + 1
      current.collision_count = current.collision_count + 1
      current.blocked_vnums[tostring(vnum)] = true
      return true
    end
    current.owned_by_vnum[tostring(vnum)] = room_id
    -- Trusted legacy rooms can predate the hash binding while still carrying
    -- the exact ownership and vnum markers. Rebind them instead of cloning.
    if type(setRoomIDbyHash) == "function" then pcall(setRoomIDbyHash, room_id, hash_for_vnum(vnum)) end
    if room_data(room_id, "source_sha256") ~= current.source_hash then
      local applied, apply_error = apply_room_source(room_id, room, current.source_hash, current.area_ids, false)
      if not applied then return nil, apply_error end
    end
    current.reused_rooms = current.reused_rooms + 1
    return true
  end
  local created_id, create_error = create_source_room(room)
  if not created_id then return nil, create_error end
  current.created_rooms, current.owned_count = current.created_rooms + 1, current.owned_count + 1
  return true
end

local function current_exit(room_id, direction)
  if type(getRoomExits) ~= "function" then return nil end
  local ok, exits = pcall(getRoomExits, room_id)
  if not ok or type(exits) ~= "table" then return nil end
  return tonumber(exits[direction] or exits[long_direction[direction]])
end

local function source_exits(room_id)
  local result = decode(room_data(room_id, "source_exits"))
  return type(result) == "table" and result or {}
end

local function cached_source_exits(room_id)
  map.runtime.old_source_exits = map.runtime.old_source_exits or {}
  local cached = map.runtime.old_source_exits[room_id]
  if cached then return cached end
  cached = source_exits(room_id)
  map.runtime.old_source_exits[room_id] = cached
  return cached
end

local function import_exit(exit_record)
  local current = map.runtime
  if current.blocked_vnums[tostring(exit_record.from_vnum)] or current.blocked_vnums[tostring(exit_record.to_vnum)] then
    current.skipped_exits = current.skipped_exits + 1
    return true
  end
  local from_id, to_id = room_id_for_vnum(exit_record.from_vnum), room_id_for_vnum(exit_record.to_vnum)
  if not from_id or not to_id or not is_owned_for_vnum(from_id, exit_record.from_vnum)
      or not is_owned_for_vnum(to_id, exit_record.to_vnum) then
    current.skipped_exits, current.collision_count = current.skipped_exits + 1, current.collision_count + 1
    return true
  end
  local existing, old = current_exit(from_id, exit_record.direction), cached_source_exits(from_id)
  local old_vnum = valid_integer(old[exit_record.direction])
  local old_target_id = old_vnum and room_id_for_vnum(old_vnum) or nil
  if old_target_id and not is_owned_for_vnum(old_target_id, old_vnum) then old_target_id = nil end
  if existing and existing ~= to_id and (not old_target_id or existing ~= old_target_id) then
    current.skipped_exits, current.collision_count = current.skipped_exits + 1, current.collision_count + 1
    return true
  end
  if existing ~= to_id then
    local ok, result = safe_call(setExit, from_id, to_id, exit_record.direction)
    if not ok or result == false then
      return nil, "Mudlet could not reconcile source exit " .. tostring(exit_record.from_vnum)
        .. " " .. tostring(exit_record.direction) .. "."
    end
  end
  current.imported_exits = current.imported_exits + 1
  return true
end

local function reconcile_room_exits(room)
  local current, room_id = map.runtime, room_id_for_vnum(room.vnum)
  if not room_id or not is_owned_for_vnum(room_id, room.vnum) then return true end
  local old, expected = cached_source_exits(room_id), current.expected_exits[tostring(room.vnum)] or {}
  for direction, old_vnum in pairs(old) do
    if long_direction[direction] and expected[direction] == nil then
      old_vnum = valid_integer(old_vnum)
      local old_target_id = old_vnum and room_id_for_vnum(old_vnum) or nil
      if old_target_id and not is_owned_for_vnum(old_target_id, old_vnum) then old_target_id = nil end
      if old_target_id and current_exit(room_id, direction) == old_target_id then
        local ok, result = pcall(setExit, room_id, -1, direction)
        if not ok or result == false then return nil, "Mudlet could not remove an obsolete source exit for vnum " .. tostring(room.vnum) .. "." end
      end
    end
  end
  if not write_room_data(room_id, "source_exits", encode(expected))
      or not write_room_data(room_id, "source_exits_sha256", current.source_hash) then
    return nil, "Mudlet could not persist source exit ownership for vnum " .. tostring(room.vnum) .. "."
  end
  return true
end

local function finish_import()
  local current = map.runtime
  if type(deleteNamedTimer) == "function" then pcall(deleteNamedTimer, TIMER_USER, TIMER_NAME) end
  if not persist_progress() or not set_map_value("completed_source_sha256", current.source_hash)
      or not set_map_value("source_format", OWNER_VALUE) or not set_map_value("import_active_source_sha256", "")
      or not set_map_value("import_progress", "") then
    set_error("The completed import could not be recorded; its checkpoint remains resumable.", true)
    return
  end
  current.phase, current.work_phase, current.resume_pending, current.error_message = "complete", nil, false, nil
  current.owned_count = owned_room_count()
  if type(updateMap) == "function" then pcall(updateMap) end
  clear_transient_runtime()
  map.center_current()
  emit_status()
  if type(raiseEvent) == "function" then pcall(raiseEvent, FINISHED_EVENT, copy_status()) end
  message("Import complete: " .. tostring(current.owned_count) .. " owned rooms; "
    .. tostring(current.collision_count or 0) .. " protected collisions.")
end

local function cancel_at_checkpoint()
  local current = map.runtime
  if not current.cancel_requested then return false end
  if not persist_progress() then
    set_error("The import checkpoint could not be persisted; cancellation stopped without discarding runtime state.", true)
    return true
  end
  if type(deleteNamedTimer) == "function" then pcall(deleteNamedTimer, TIMER_USER, TIMER_NAME) end
  current.phase, current.resume_pending, current.cancel_requested = "cancelled", true, false
  clear_transient_runtime()
  emit_status()
  message("Map import cancelled at a durable checkpoint; run aard map import to resume.")
  return true
end

function map._process_import()
  local current = map.runtime
  if cancel_at_checkpoint() then return end
  if current.work_phase == "rooms" then
    local last = math.min(current.room_index + BATCH_SIZE, current.room_total)
    while current.room_index &lt; last do
      local next_index = current.room_index + 1
      local ok, error_message = import_room(current.data.rooms[next_index])
      if not ok then set_error(error_message); return end
      current.room_index = next_index
    end
    if current.room_index == current.room_total then
      current.work_phase = current.write_exits and "exits" or nil
      current.phase = current.work_phase or current.phase
    end
    if not current.work_phase then finish_import(); return end
  elseif current.work_phase == "exits" then
    local last = math.min(current.exit_index + BATCH_SIZE, current.exit_total)
    while current.exit_index &lt; last do
      local next_index = current.exit_index + 1
      local ok, error_message = import_exit(current.data.exits[next_index])
      if not ok then set_error(error_message); return end
      current.exit_index = next_index
    end
    if current.exit_index == current.exit_total then
      current.work_phase, current.phase = "reconcile", "reconcile"
    end
  elseif current.work_phase == "reconcile" then
    local last = math.min(current.reconcile_index + BATCH_SIZE, current.room_total)
    while current.reconcile_index &lt; last do
      local next_index = current.reconcile_index + 1
      local ok, error_message = reconcile_room_exits(current.data.rooms[next_index])
      if not ok then set_error(error_message); return end
      current.reconcile_index = next_index
    end
    if current.reconcile_index == current.room_total then finish_import(); return end
  else
    set_error("The saved import phase is invalid.")
    return
  end
  if not persist_progress() then
    set_error("The import checkpoint could not be persisted.", true)
    return
  end
  emit_status()
end

local function expected_exits(data)
  local expected = {}
  for _, exit_record in ipairs(data.exits) do
    local from = tostring(exit_record.from_vnum)
    expected[from] = expected[from] or {}
    expected[from][exit_record.direction] = exit_record.to_vnum
  end
  return expected
end

function map.start_import()
  local current = map.runtime
  if current.phase == "rooms" or current.phase == "exits" or current.phase == "reconcile" then
    return false, "A map import is already running."
  end
  if type(deleteNamedTimer) == "function" then pcall(deleteNamedTimer, TIMER_USER, TIMER_NAME) end
  local data, resource_error = read_resource()
  if not data then set_error(resource_error); return false, resource_error end
  local apis_ok, api_error = mapper_apis_available()
  if not apis_ok then set_error(api_error); return false, api_error end
  if not set_map_value("owner", OWNER_VALUE) then
    set_error("The profile map cannot persist aardwolf-mudlet ownership metadata.", true)
    return false, "The profile map cannot persist aardwolf-mudlet ownership metadata."
  end
  local registered, environment_error = register_environments(data)
  if not registered then set_error(environment_error); return false, environment_error end
  local area_ids, area_error = ensure_areas(data)
  if not area_ids then set_error(area_error); return false, area_error end
  local saved = get_map_table("import_progress")
  local resume = saved.source_hash == data.source.sha256
    and (saved.phase == "rooms" or saved.phase == "exits" or saved.phase == "reconcile")
  current.data, current.source_hash, current.area_ids = data, data.source.sha256, area_ids
  current.environment_ids = get_map_table("environment_ids")
  current.owned_by_vnum, current.expected_exits, current.old_source_exits = build_owned_vnum_index(), expected_exits(data), {}
  current.room_total, current.exit_total = #data.rooms, #data.exits
  current.room_index = resume and cursor(saved.room_index, #data.rooms) or 0
  current.exit_index = resume and cursor(saved.exit_index, #data.exits) or 0
  current.reconcile_index = resume and cursor(saved.reconcile_index, #data.rooms) or 0
  current.created_rooms = resume and (nonnegative_integer(saved.created_rooms) or 0) or 0
  current.reused_rooms = resume and (nonnegative_integer(saved.reused_rooms) or 0) or 0
  current.skipped_rooms = resume and (nonnegative_integer(saved.skipped_rooms) or 0) or 0
  current.imported_exits = resume and (nonnegative_integer(saved.imported_exits) or 0) or 0
  current.skipped_exits = resume and (nonnegative_integer(saved.skipped_exits) or 0) or 0
  current.collision_count = resume and (nonnegative_integer(saved.collision_count) or 0) or 0
  current.blocked_vnums = resume and type(saved.blocked_vnums) == "table" and saved.blocked_vnums or {}
  current.write_exits = resume and saved.write_exits == true or get_map_value("completed_source_sha256") ~= data.source.sha256
  current.work_phase = resume and saved.phase or "rooms"
  current.phase, current.resume_pending, current.cancel_requested, current.error_message = current.work_phase, false, false, nil
  current.owned_count, current.palette, current.registered_environments = owned_room_count(), palette_setting(), registered
  if not set_map_table("source", data.source) or not set_map_table("source_environments", data.environments) then
    set_error("The source map provenance could not be persisted.", true)
    return false, "The source map provenance could not be persisted."
  end
  if not persist_progress() then
    set_error("The initial import checkpoint could not be persisted.", true)
    return false, "The initial import checkpoint could not be persisted."
  end
  if type(registerNamedTimer) ~= "function" then
    set_error("Mudlet named timers are unavailable.")
    return false, "Mudlet named timers are unavailable."
  end
  local ok, result = pcall(registerNamedTimer, TIMER_USER, TIMER_NAME, 0.01, map._process_import, true)
  if not ok or result == false then
    set_error("Mudlet could not schedule the map import.")
    return false, "Mudlet could not schedule the map import."
  end
  emit_status()
  message((resume and "Resuming" or "Starting") .. " bounded import of " .. tostring(#data.rooms)
    .. " rooms and " .. tostring(#data.exits) .. " exits.")
  return true
end

function map.cancel_import()
  local phase = map.runtime.phase
  if phase ~= "rooms" and phase ~= "exits" and phase ~= "reconcile" then return false, "No map import is running." end
  map.runtime.cancel_requested = true
  return true
end

function map.status()
  return copy_status()
end

function map.set_palette(name)
  if not palettes[name] then return false, "Palette must be source, obsidian, or high-contrast." end
  if not set_map_value("palette", name) then return false, "The terrain palette setting could not be persisted." end
  map.runtime.palette = name
  local data, resource_error = read_resource()
  if not data then map.runtime.palette_error = resource_error; emit_status(); return false, resource_error end
  local registered, environment_error = register_environments(data)
  if not registered then map.runtime.palette_error = environment_error; emit_status(); return false, environment_error end
  map.runtime.palette_error = nil
  if type(updateMap) == "function" then pcall(updateMap) end
  emit_status()
  return true
end

function map.center_current()
  local info = gmcp and gmcp.room and gmcp.room.info
  local vnum = type(info) == "table" and valid_integer(info.num) or nil
  local room_id = vnum and room_id_for_vnum(vnum) or nil
  local resolved = room_id and is_owned_for_vnum(room_id, vnum) and room_id or nil
  map.runtime.current_vnum, map.runtime.resolved_room_id = vnum, resolved
  if resolved and type(centerview) == "function" then pcall(centerview, resolved) end
  emit_status()
  return resolved ~= nil, resolved and nil or "The current GMCP room has not been imported by aardwolf-mudlet."
end

function map.on_room_info()
  return map.center_current()
end

function map.zoom(delta)
  local amount = tonumber(delta)
  if not amount or amount ~= amount or amount == math.huge or amount == -math.huge then return false, "Zoom delta must be a finite number." end
  if type(setMapZoom) ~= "function" then return false, "Mudlet map zoom is unavailable." end
  local current = tonumber(get_map_value("zoom")) or 20
  if type(getMapZoom) == "function" then
    local ok, value = pcall(getMapZoom)
    if ok and tonumber(value) then current = tonumber(value) end
  end
  local target = math.max(3, math.min(200, current + amount))
  local ok, result = pcall(setMapZoom, target)
  if not ok or result == false then return false, "Mudlet could not change map zoom." end
  set_map_value("zoom", target)
  return true, target
end

local function on_uninstall(_, package_name)
  if package_name == nil or tostring(package_name) == "aardwolf-mudlet" then map.shutdown("uninstalled") end
end

local function on_exit() map.shutdown("exited") end

function map.initialize()
  if map.runtime.work_phase and map.runtime.data then persist_progress() end
  delete_runtime_objects()
  local progress = get_map_table("import_progress")
  local resumable = progress.source_hash and (progress.phase == "rooms" or progress.phase == "exits" or progress.phase == "reconcile")
  map.runtime = {
    phase = resumable and "interrupted" or "idle", source_hash = resumable and progress.source_hash or get_map_value("completed_source_sha256"),
    room_index = nonnegative_integer(progress.room_index) or 0, exit_index = nonnegative_integer(progress.exit_index) or 0,
    room_total = nonnegative_integer(progress.room_total) or 0, exit_total = nonnegative_integer(progress.exit_total) or 0,
    reconcile_index = nonnegative_integer(progress.reconcile_index) or 0, created_rooms = nonnegative_integer(progress.created_rooms) or 0,
    reused_rooms = nonnegative_integer(progress.reused_rooms) or 0, skipped_rooms = nonnegative_integer(progress.skipped_rooms) or 0,
    imported_exits = nonnegative_integer(progress.imported_exits) or 0, skipped_exits = nonnegative_integer(progress.skipped_exits) or 0,
    collision_count = nonnegative_integer(progress.collision_count) or 0, owned_count = owned_room_count(),
    registered_environments = tonumber(get_map_value("registered_environment_count")) or 0,
    palette = palette_setting(), resume_pending = resumable == true,
    cancel_requested = false,
  }
  if type(registerNamedEventHandler) == "function" then
    pcall(registerNamedEventHandler, TIMER_USER, ROOM_HANDLER, "gmcp.room.info", map.on_room_info)
    pcall(registerNamedEventHandler, TIMER_USER, EXIT_HANDLER, "sysExitEvent", on_exit)
    pcall(registerNamedEventHandler, TIMER_USER, UNINSTALL_HANDLER, "sysUninstallPackage", on_uninstall)
  end
  map.runtime.owned_by_vnum = build_owned_vnum_index()
  map.center_current()
  emit_status()
  return true
end

function map.shutdown(phase)
  local was_importing = map.runtime.phase == "rooms" or map.runtime.phase == "exits" or map.runtime.phase == "reconcile"
  if was_importing then persist_progress() end
  delete_runtime_objects()
  clear_transient_runtime()
  map.runtime.phase = was_importing and "interrupted" or (phase or "unloaded")
  map.runtime.resume_pending = was_importing or map.runtime.resume_pending == true
  emit_status()
  return true
end
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.protocol</name><script>aardwolf_mudlet.protocol = aardwolf_mudlet.protocol or {}

local function number(value) return aardwolf_mudlet.util.finite(value) end
local function text(value) return aardwolf_mudlet.util.text(value, 200) end
local function source(path)
  local value = gmcp
  for part in path:gmatch("[^.]+") do
    if type(value) ~= "table" then return nil end
    value = value[part]
  end
  return type(value) == "table" and value or nil
end

local function pick(payload, fields, include_primitives)
  local out = {}
  if include_primitives then
    local count=0
    for field,value in pairs(payload) do
      count=count+1; if count&gt;100 then break end
      if type(field)=="string" then
        if type(value)=="number" or tonumber(value) then out[field]=number(value)
        elseif type(value)=="string" then out[field]=text(value)
        elseif type(value)=="boolean" then out[field]=value end
      end
    end
  end
  for _, field in ipairs(fields) do
    local value = payload[field]
    if type(value) == "number" or tonumber(value) then out[field] = number(value)
    elseif type(value) == "string" then out[field] = text(value) end
  end
  return out
end

local function publish(section, value, status, error_message)
  aardwolf_mudlet.state.record(section, value, status, error_message)
  if aardwolf_mudlet.ui and aardwolf_mudlet.ui.request_render then aardwolf_mudlet.ui.request_render() end
end

function aardwolf_mudlet.protocol.base()
  local payload = source("char.base")
  if not payload then return publish("base", {}, "error", "Malformed gmcp.char.base") end
  local value = pick(payload, {"name", "class", "subclass", "race", "clan", "pretitle", "perlevel", "tier", "remorts", "redos", "classes", "level", "pups", "totpups"}, true)
  local key = (value.name or "") .. ":" .. tostring(value.class or "")
  if aardwolf_mudlet.state.character_key and aardwolf_mudlet.state.character_key ~= key then
    aardwolf_mudlet.state.reset_session("connected")
    if aardwolf_mudlet.details then aardwolf_mudlet.details.stop("character changed") end
  end
  aardwolf_mudlet.state.character_key = key ~= ":" and key or nil
  publish("base", value, next(value) and "current" or "partial")
end

local definitions = {
  vitals = {"hp", "mana", "moves"},
  maxstats = {"maxhp", "maxmana", "maxmoves", "maxstr", "maxint", "maxwis", "maxdex", "maxcon", "maxluck"},
  status = {"state", "level", "tnl", "hunger", "thirst", "align", "pos", "enemy", "enemypct"},
  stats = {"str", "int", "wis", "dex", "con", "luck", "hr", "dr", "saves"},
  worth = {"gold", "bank", "qp", "qpearned", "tp", "trains", "pracs"},
}

for section, fields in pairs(definitions) do
  aardwolf_mudlet.protocol[section] = function()
    local payload = source("char." .. section)
    if not payload then return publish(section, {}, "error", "Malformed gmcp.char." .. section) end
    local value = pick(payload, fields, true)
    publish(section, value, next(value) and "current" or "partial")
  end
end

function aardwolf_mudlet.protocol.room()
  local payload = source("room.info")
  if not payload then return publish("room", {}, "error", "Malformed gmcp.room.info") end
  local value = pick(payload, {"num", "name", "area", "zone", "terrain", "mapterrain", "details", "outside", "racebonus"})
  value.exits = {}
  local allowed = {n=true,e=true,s=true,w=true,u=true,d=true}
  for direction, destination in pairs(type(payload.exits) == "table" and payload.exits or {}) do
    if allowed[direction] then value.exits[direction] = number(destination) or text(destination) end
  end
  local coordinates = type(payload.coord) == "table" and payload.coord or type(payload.coords) == "table" and payload.coords or {}
  value.coordinates = pick(coordinates, {"id", "x", "y", "cont"})
  value.x, value.y, value.z, value.cont = value.coordinates.x, value.coordinates.y, number(coordinates.z), value.coordinates.cont
  publish("room", value, (value.num or value.name) and "current" or "partial")
end

function aardwolf_mudlet.protocol.group()
  local payload = source("group")
  if not payload then return publish("group", {members = {}}, "unavailable") end
  local value = pick(payload, {"groupname", "leader", "status"})
  value.members = {}
  local members = payload.members or payload.chars or payload
  if type(members) == "table" then
    for key, member in pairs(members) do
      if #value.members &gt;= 40 then break end
      if type(member) == "table" then
        local info=type(member.info)=="table" and member.info or member
        local normalized=pick(info, {"lvl", "level", "hp", "mhp", "mn", "mmn", "mv", "mmv", "align", "tnl", "qt", "qs", "here"}, true)
        normalized.name=text(member.name or info.name)
        if not normalized.name and type(key)=="string" then normalized.name=text(key) end
        value.members[#value.members + 1] = normalized
      end
    end
  end
  table.sort(value.members,function(left,right) return tostring(left.name or "")&lt;tostring(right.name or "") end)
  local status = next(payload) and "current" or "partial"
  publish("group", value, status)
end

function aardwolf_mudlet.protocol.group_invite(player, description)
  player, description = text(player), text(description)
  if not player then return false end
  local value = aardwolf_mudlet.util.copy(aardwolf_mudlet.state.value("group"))
  value.members = type(value.members) == "table" and value.members or {}
  value.invitation = {player = player, description = description or "", status = "pending", received_at = os.time()}
  publish("group", value, "partial")
  return true
end

function aardwolf_mudlet.protocol.group_cancel(player, reason)
  player, reason = text(player), text(reason)
  local value = aardwolf_mudlet.util.copy(aardwolf_mudlet.state.value("group"))
  value.members = type(value.members) == "table" and value.members or {}
  value.invitation = {player = player or "", description = reason or "Invitation cancelled", status = "cancelled", received_at = os.time()}
  publish("group", value, "partial")
  return true
end

function aardwolf_mudlet.protocol.quest()
  local payload = source("comm.quest")
  if not payload then return publish("quest", {}, "unavailable") end
  local value = pick(payload, {"action", "status", "targ", "target", "room", "area", "timer", "time"})
  publish("quest", value, next(value) and "current" or "partial")
  if aardwolf_mudlet.sound and aardwolf_mudlet.sound.on_event then aardwolf_mudlet.sound.on_event("quest." .. tostring(value.action or value.status or "update"):lower()) end
end

function aardwolf_mudlet.protocol.tick()
  local payload = source("comm.tick")
  local value = type(payload) == "table" and pick(payload, {"ctime", "time"}, true) or {}
  value.last_seen = os.time()
  value.duration = 30
  publish("tick", value, type(payload) == "table" and "current" or "partial")
  if aardwolf_mudlet.sound and aardwolf_mudlet.sound.on_event then aardwolf_mudlet.sound.on_event("tick") end
end

function aardwolf_mudlet.protocol.channel()
  if aardwolf_mudlet.chat and aardwolf_mudlet.chat.on_gmcp then aardwolf_mudlet.chat.on_gmcp() end
end

function aardwolf_mudlet.protocol.map_status(_, snapshot)
  if type(snapshot) ~= "table" and aardwolf_mudlet.map and aardwolf_mudlet.map.status then
    local ok, value = pcall(aardwolf_mudlet.map.status)
    if ok then snapshot = value end
  end
  local desired=aardwolf_mudlet.settings and aardwolf_mudlet.settings.data and aardwolf_mudlet.settings.data.theme
  if type(snapshot)=="table" and aardwolf_mudlet.settings.data.sync_map_theme==true and (desired=="obsidian" or desired=="high-contrast") and snapshot.palette~=desired
      and aardwolf_mudlet.map and aardwolf_mudlet.map.set_palette then
    pcall(aardwolf_mudlet.map.set_palette,desired,true)
    local ok,current=pcall(aardwolf_mudlet.map.status); if ok and type(current)=="table" then snapshot=current end
  end
  publish("map", type(snapshot) == "table" and snapshot or {}, type(snapshot) == "table" and "current" or "unavailable")
end
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.commands</name><script>aardwolf_mudlet.commands = aardwolf_mudlet.commands or {}
local function message(value) if aardwolf_mudlet.ui and aardwolf_mudlet.ui.message then aardwolf_mudlet.ui.message(value) else cecho("\n&lt;cyan&gt;[aardwolf-mudlet]&lt;reset&gt; "..value) end end

function aardwolf_mudlet.commands.show() aardwolf_mudlet.settings.update("visible",true); aardwolf_mudlet.settings.data.collapsed_by_user=false; aardwolf_mudlet.ui.show() end
function aardwolf_mudlet.commands.hide() aardwolf_mudlet.settings.update("visible",false); aardwolf_mudlet.ui.hide() end
function aardwolf_mudlet.commands.status()
  local data=aardwolf_mudlet.settings.data
  local layout=aardwolf_mudlet.ui and aardwolf_mudlet.ui.layout or {}
  message(string.format("visible=%s tab=%s width=%d theme=%s density=%s scale=%d%% dock-pinned=%s dock-width=%s suspended=%s console=%s initialization-deferred=%s connection=%s",tostring(data.visible),data.active_tab,data.workspace_width,data.theme,data.density,data.text_scale,tostring(data.inspector_pinned),tostring(layout.dock or 0),tostring(layout.suspended==true),tostring(layout.window_width and math.max(0,layout.window_width-layout.base-layout.total) or "--"),tostring(aardwolf_mudlet.lifecycle and aardwolf_mudlet.lifecycle.deferred==true),aardwolf_mudlet.state.connection))
end
function aardwolf_mudlet.commands.repair()
  local ok=aardwolf_mudlet.lifecycle and aardwolf_mudlet.lifecycle.repair and aardwolf_mudlet.lifecycle.repair()
  message(ok and "Interface viewport repaired and rebuilt." or "Interface repair could not build Geyser; use 'aard status' for text output.")
end
function aardwolf_mudlet.commands.set_tab(tab)
  if tab=="map" then tab="overview" end
  if ({overview=true,character=true,group=true,inventory=true,chat=true})[tab] then aardwolf_mudlet.ui.set_tab(tab) end
end
aardwolf_mudlet.commands.select_tab=aardwolf_mudlet.commands.set_tab
function aardwolf_mudlet.commands.toggle_pin(tab)
  local data=aardwolf_mudlet.settings.data
  if tab=="map" then tab="overview" end
  if tab=="off" then data.inspector_pinned=false elseif tab then data.inspector_pinned=true; data.active_tab=tab else data.inspector_pinned=not data.inspector_pinned end
  aardwolf_mudlet.settings.save(); aardwolf_mudlet.ui.reflow(); aardwolf_mudlet.ui.request_render()
end
function aardwolf_mudlet.commands.details_show() aardwolf_mudlet.commands.set_tab("inventory"); aardwolf_mudlet.commands.toggle_pin("inventory") end
function aardwolf_mudlet.commands.details_hide() aardwolf_mudlet.commands.toggle_pin("off"); if aardwolf_mudlet.settings.data.active_tab=="inventory" then aardwolf_mudlet.commands.set_tab("overview") end end
function aardwolf_mudlet.commands.details_toggle() if aardwolf_mudlet.settings.data.inspector_pinned or aardwolf_mudlet.settings.data.active_tab=="inventory" then aardwolf_mudlet.commands.details_hide() else aardwolf_mudlet.commands.details_show() end end
function aardwolf_mudlet.commands.details_refresh() aardwolf_mudlet.details.refresh() end
function aardwolf_mudlet.commands.details_status() local value=aardwolf_mudlet.state.value("details"); message(string.format("inventory status=%s generation=%d equipment=%d bags=%d",aardwolf_mudlet.state.envelope("details").status,value.generation or 0,#(value.equipment or {}),#(value.bags or {}))) end
function aardwolf_mudlet.commands.toggle_palette(mode)
  local value=mode=="show" or (mode~="hide" and not aardwolf_mudlet.settings.data.palette_open)
  aardwolf_mudlet.settings.update("palette_open",value); aardwolf_mudlet.ui.toggle_palette(value)
end
function aardwolf_mudlet.commands.select_inventory_tab(tab) if tab=="equipment" or tab=="bags" then aardwolf_mudlet.settings.update("inventory_tab",tab); aardwolf_mudlet.ui.request_render() end end
function aardwolf_mudlet.commands.toggle_empty() aardwolf_mudlet.settings.update("show_empty_slots",not aardwolf_mudlet.settings.data.show_empty_slots); aardwolf_mudlet.ui.request_render() end
function aardwolf_mudlet.commands.set_theme(theme) if theme=="obsidian" or theme=="high-contrast" then aardwolf_mudlet.settings.update("theme",theme); aardwolf_mudlet.ui.theme(); if aardwolf_mudlet.map and aardwolf_mudlet.map.set_palette and aardwolf_mudlet.settings.data.sync_map_theme then aardwolf_mudlet.map.set_palette(theme) end end end
function aardwolf_mudlet.commands.toggle_theme() aardwolf_mudlet.commands.set_theme(aardwolf_mudlet.settings.data.theme=="obsidian" and "high-contrast" or "obsidian") end
function aardwolf_mudlet.commands.set_density(value) if value=="compact" or value=="comfortable" then aardwolf_mudlet.settings.update("density",value); aardwolf_mudlet.ui.reflow() end end
function aardwolf_mudlet.commands.set_scale(value) value=tonumber(value); if ({[90]=true,[100]=true,[115]=true,[130]=true})[value] then aardwolf_mudlet.settings.update("text_scale",value); aardwolf_mudlet.ui.reflow() end end

local function summary_lines(section)
  local envelope=aardwolf_mudlet.state.envelope(section); local rows={section..": "..envelope.status}; local value=envelope.value or {}
  local count=0
  local function walk(prefix,item,depth)
    if count&gt;=80 then return end
    if type(item)~="table" or depth&lt;=0 then rows[#rows+1]=string.format("  %s: %s",prefix,tostring(item)); count=count+1; return end
    local keys={}; for key in pairs(item) do keys[#keys+1]=key end; table.sort(keys,function(left,right) return tostring(left)&lt;tostring(right) end)
    for _,key in ipairs(keys) do walk(prefix=="" and tostring(key) or prefix.."."..tostring(key),item[key],depth-1); if count&gt;=80 then break end end
  end
  walk("",value,3)
  if envelope.error then rows[#rows+1]="  error: "..envelope.error end; return rows
end
function aardwolf_mudlet.commands.summary(section)
  local map={room={"room"},character={"base","vitals","maxstats","status","stats","worth"},quest={"quest"},group={"group"},equipment={"details"},bags={"details"},actions={}}
  local sections=section=="all" and {"room","base","vitals","maxstats","status","stats","worth","quest","group","details","map"} or map[section]
  local rows={}
  if section=="actions" then for _,action in ipairs(aardwolf_mudlet.actions.context()) do rows[#rows+1]=action.id..": "..action.label end; for _,action in ipairs(aardwolf_mudlet.actions.custom()) do rows[#rows+1]=action.id..": "..action.label end
  elseif section=="equipment" or section=="bags" then
    local envelope=aardwolf_mudlet.state.envelope("details"); rows[#rows+1]=section..": "..envelope.status
    local selected=(envelope.value or {})[section] or {}; local keys={}; for key in pairs(selected) do keys[#keys+1]=key end; table.sort(keys,function(left,right) return tostring(left)&lt;tostring(right) end)
    for _,key in ipairs(keys) do local item=selected[key]; rows[#rows+1]=string.format("  %s: %s%s",tostring(key),tostring(item.name or "Unknown"),item.used_weight and (" "..tostring(item.used_weight).."/"..tostring(item.max_weight or "--").." weight") or "") end
  elseif sections then for _,name in ipairs(sections) do for _,row in ipairs(summary_lines(name)) do rows[#rows+1]=row end end end
  message(table.concat(rows,"\n"))
end

local function result_message(ok, value, success)
  message(ok and (success or tostring(value or "Done.")) or tostring(value or "Command failed."))
  return ok
end

function aardwolf_mudlet.commands.action_add(label,command,category) local ok,error_message=aardwolf_mudlet.actions.add(label,command,category); message(ok and "Custom action added." or error_message) end
function aardwolf_mudlet.commands.action_edit(id,label,command,category) message(aardwolf_mudlet.actions.update(id,label,command,category) and "Custom action updated." or "Custom action was not updated.") end
function aardwolf_mudlet.commands.action_remove(id) message(aardwolf_mudlet.actions.remove(id) and "Custom action removed." or "Unknown custom action.") end
function aardwolf_mudlet.commands.action_move(id,order) message(aardwolf_mudlet.actions.move(id,order) and "Custom action moved." or "Unable to move custom action.") end
function aardwolf_mudlet.commands.action_list() aardwolf_mudlet.commands.summary("actions") end
function aardwolf_mudlet.commands.action_editor_submit(label,command,category)
  if not label and aardwolf_mudlet.ui and aardwolf_mudlet.ui.editor_value then label,category,command=aardwolf_mudlet.ui.editor_value() end
  if not label or not command then message("Enter Label | Category | command in the custom action editor."); return false end
  local ok,error_message=aardwolf_mudlet.actions.add(label,command,category); message(ok and "Custom action added." or error_message); if ok then aardwolf_mudlet.ui.editor_open=false; aardwolf_mudlet.ui.request_render() end; return ok
end

function aardwolf_mudlet.commands.tick_status()
  local envelope=aardwolf_mudlet.state.envelope("tick")
  message(string.format("tick status=%s remaining=%s",tostring(envelope.status),tostring(aardwolf_mudlet.state.tick_remaining() or "unavailable")))
end

function aardwolf_mudlet.commands.legacy_tts(input)
  local value=tostring(input or ""):match("^%s*(.-)%s*$")
  local lower=value:lower()
  if lower=="sapi on" then return aardwolf_mudlet.commands.dispatch("tts on") end
  if lower=="sapi off" then return aardwolf_mudlet.commands.dispatch("tts off") end
  if lower=="sapi clear" or lower=="sapi skip" or lower=="tts_stop" then return aardwolf_mudlet.commands.dispatch("tts clear") end
  local spoken=value:match("^[Ss][Aa][Pp][Ii]%s+[Ss][Aa][Yy]%s+(.+)$") or value:match("^tts_note%s+(.+)$") or value:match("^tts_interrupt%s+(.+)$")
  if spoken then return result_message(aardwolf_mudlet.accessibility.say(spoken,true)) end
  local rate=value:match("^[Ss][Aa][Pp][Ii]%s+[Rr][Aa][Tt][Ee]%s+([%-%.%d]+)$")
  if rate then return result_message(aardwolf_mudlet.accessibility.set_rate(rate)) end
  if lower=="sapi faster" or lower=="sapi slower" then
    local current=tonumber(aardwolf_mudlet.settings.data.accessibility.rate) or 0
    return result_message(aardwolf_mudlet.accessibility.set_rate(current+(lower=="sapi faster" and 0.1 or -0.1)))
  end
  local voice=value:match("^[Ss][Aa][Pp][Ii]%s+[Vv][Oo][Ii][Cc][Ee]%s+(.+)$")
  if voice then return result_message(aardwolf_mudlet.accessibility.set_voice(voice)) end
  if lower=="sapi list voices" then return message(table.concat(aardwolf_mudlet.accessibility.voices(),", ")) end
  if lower=="sapi test" then return result_message(aardwolf_mudlet.accessibility.say("Aardwolf Mudlet text to speech test.",true)) end
  return message(aardwolf_mudlet.accessibility.status())
end

local function call_result(callback, success, ...)
  local ok, value = callback(...)
  return result_message(ok, value, success)
end

local function boolean_word(value)
  if value == "on" or value == "show" or value == "true" then return true end
  if value == "off" or value == "hide" or value == "false" then return false end
end

function aardwolf_mudlet.commands.help()
  if aardwolf_mudlet.help and aardwolf_mudlet.help.summary then message(aardwolf_mudlet.help.summary()) end
end

function aardwolf_mudlet.commands.dispatch(argument)
  local text = tostring(argument or ""):match("^%s*(.-)%s*$")
  if text == "" or text == "status" then return aardwolf_mudlet.commands.status() end
  if text == "help" then return aardwolf_mudlet.commands.help() end
  local family, rest = text:match("^(%S+)%s*(.-)$")
  family = family and family:lower()
  if family == "ui" then
    if rest == "show" then return aardwolf_mudlet.commands.show() end
    if rest == "hide" then return aardwolf_mudlet.commands.hide() end
    if rest == "status" then return aardwolf_mudlet.commands.status() end
    local key, value = rest:match("^(theme|density|scale)%s+(%S+)$")
    if key == "theme" then return aardwolf_mudlet.commands.set_theme(value) end
    if key == "density" then return aardwolf_mudlet.commands.set_density(value) end
    if key == "scale" then return aardwolf_mudlet.commands.set_scale(value) end
  elseif family == "map" and aardwolf_mudlet.map then
    if rest == "import" then return call_result(aardwolf_mudlet.map.start_import, "Map import started or resumed.") end
    if rest == "import cancel" then return call_result(aardwolf_mudlet.map.cancel_import, "Map import cancellation requested.") end
    if rest == "status" then local value=aardwolf_mudlet.map.status(); return message(type(value)=="table" and string.format("map phase=%s rooms=%s exits=%s imported-exits=%s collisions=%s",tostring(value.phase),tostring(value.room_index),tostring(value.exit_index),tostring(value.imported_exits),tostring(value.collision_count)) or tostring(value)) end
    if rest == "center" then return call_result(aardwolf_mudlet.map.center_current, "Map centered.") end
    local palette = rest:match("^palette%s+(source|obsidian|high%-contrast)$")
    if palette then return call_result(aardwolf_mudlet.map.set_palette, "Map palette updated.", palette) end
    local zoom = rest:match("^zoom%s+(in|out)$")
    if zoom then return call_result(aardwolf_mudlet.map.zoom, "Map zoom updated.", zoom=="in" and -1 or 1) end
  elseif family == "inventory" then
    if rest == "refresh" then aardwolf_mudlet.details.refresh(); return message("Inventory refresh started.") end
    if rest == "status" then return aardwolf_mudlet.commands.details_status() end
  elseif family == "capture" then
    if rest == "copy" then return result_message(aardwolf_mudlet.capture.copy()) end
    return call_result(aardwolf_mudlet.capture.start, "Tagged command capture started.", rest)
  elseif family == "chat" then
    if rest == "show" then aardwolf_mudlet.commands.set_tab("chat"); return end
    if rest == "hide" then aardwolf_mudlet.commands.set_tab("overview"); return end
    if rest == "status" then local buffer=aardwolf_mudlet.chat.active_buffer(); return message(string.format("chat tab=%s messages=%d preserve-main=%s logging=%s",aardwolf_mudlet.chat.active_name(),#buffer.messages,tostring(aardwolf_mudlet.settings.data.chat.preserve_main),tostring(aardwolf_mudlet.settings.data.chat.logging))) end
    if rest == "clear" then return result_message(aardwolf_mudlet.chat.clear(), nil, "Active chat tab cleared.") end
    local mode = rest:match("^preserve%s+(on|off)$"); if mode then return result_message(aardwolf_mudlet.chat.set_preserve(mode=="on")) end
    mode = rest:match("^log%s+(on|off)$"); if mode then return result_message(aardwolf_mudlet.chat.set_logging(mode=="on")) end
    local name = rest:match("^tab%s+select%s+(.+)$"); if name then return result_message(aardwolf_mudlet.chat.select(name)) end
    local add_name, filters = rest:match("^tab%s+add%s+([^|]+)|(.+)$"); if add_name then return result_message(aardwolf_mudlet.chat.add_tab(add_name,filters)) end
    local old,new = rest:match("^tab%s+rename%s+([^|]+)|(.+)$"); if old then return result_message(aardwolf_mudlet.chat.rename_tab(old,new)) end
    local move_name,position = rest:match("^tab%s+move%s+([^|]+)|(%d+)$"); if move_name then return result_message(aardwolf_mudlet.chat.move_tab(move_name,position)) end
    name = rest:match("^tab%s+remove%s+(.+)$"); if name then return result_message(aardwolf_mudlet.chat.remove_tab(name)) end
    local filter_name, values = rest:match("^filter%s+([^|]+)|(.+)$"); if filter_name then return result_message(aardwolf_mudlet.chat.set_filters(filter_name,values)) end
    local limit = rest:match("^limit%s+(%d+)$"); if limit then return result_message(aardwolf_mudlet.chat.set_limit(limit)) end
    if rest == "copy" then return result_message(aardwolf_mudlet.chat.copy_active(false)) end
    if rest == "copy color" then return result_message(aardwolf_mudlet.chat.copy_active(true)) end
    local url = rest:match("^open%s+(.+)$"); if url then return result_message(aardwolf_mudlet.chat.open_url(url)) end
  elseif family == "action" then
    if rest == "list" then return aardwolf_mudlet.commands.action_list() end
    local label, category, command = rest:match("^add%s+([^|]+)|([^|]+)|(.+)$"); if label then return aardwolf_mudlet.commands.action_add(label,command,category) end
    local id,new_label,new_category,new_command = rest:match("^edit%s+(custom%-%d+)|([^|]+)|([^|]+)|(.+)$"); if id then return aardwolf_mudlet.commands.action_edit(id,new_label,new_command,new_category) end
    id = rest:match("^remove%s+(custom%-%d+)$"); if id then return aardwolf_mudlet.commands.action_remove(id) end
    local order; id,order = rest:match("^move%s+(custom%-%d+)%s+(%d+)$"); if id then return aardwolf_mudlet.commands.action_move(id,order) end
  elseif family == "tts" then
    local value = boolean_word(rest); if value ~= nil then return result_message(aardwolf_mudlet.accessibility.set_enabled(value)) end
    if rest == "clear" then return result_message(aardwolf_mudlet.accessibility.clear()) end
    if rest == "status" then return message(aardwolf_mudlet.accessibility.status()) end
    local say = rest:match("^say%s+(.+)$"); if say then return result_message(aardwolf_mudlet.accessibility.say(say,true)) end
    local rate = rest:match("^rate%s+([%-%.%d]+)$"); if rate then return result_message(aardwolf_mudlet.accessibility.set_rate(rate)) end
    local voice = rest:match("^voice%s+(.+)$"); if voice then return result_message(aardwolf_mudlet.accessibility.set_voice(voice)) end
  elseif family == "sound" then
    local value = boolean_word(rest); if value ~= nil then return result_message(aardwolf_mudlet.sound.set_enabled(value)) end
    if rest == "status" then return message(aardwolf_mudlet.sound.status()) end
    local event,file,volume = rest:match("^map%s+([^|]+)|([^|]+)|(%d+)$")
    if not event then event,file = rest:match("^map%s+([^|]+)|(.+)$") end
    if event then return call_result(aardwolf_mudlet.sound.map, nil, event,file,volume) end
    event = rest:match("^remove%s+(.+)$"); if event then return result_message(aardwolf_mudlet.sound.remove(event)) end
  elseif family == "data" then
    if rest == "status" then return message(aardwolf_mudlet.data.status()) end
    if rest == "export" then return result_message(aardwolf_mudlet.data.export()) end
    if rest == "import" then return result_message(aardwolf_mudlet.data.import()) end
    if rest == "backup" then return result_message(aardwolf_mudlet.data.backup()) end
    local backup_mode=rest:match("^backup%s+(on|off)$"); if backup_mode then return result_message(aardwolf_mudlet.data.set_auto_backup(backup_mode=="on")) end
  end
  message("Unknown aard command. Use 'aard help'.")
end
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.ui</name><script>-- Presentation for the Aardwolf Adaptive Command Deck.
-- This module deliberately consumes validated state and delegates behavior; it
-- does not read GMCP or send commands itself.
aardwolf_mudlet = aardwolf_mudlet or {}
aardwolf_mudlet.ui = aardwolf_mudlet.ui or {}

local ui = aardwolf_mudlet.ui
local util = aardwolf_mudlet.util
local settings = aardwolf_mudlet.settings
local state = aardwolf_mudlet.state
local actions = aardwolf_mudlet.actions
local commands = aardwolf_mudlet.commands
local constants = aardwolf_mudlet.constants

local PREFIX = "aardwolf-mudlet::ui::"
local TIMER_USER = "aardwolf_mudlet"
local RENDER_TIMER = "aardwolf-mudlet::timer::render"
local WORKSPACE_MIN, WORKSPACE_DEFAULT, WORKSPACE_MAX = 360, 440, 520
local DOCK_MIN, DOCK_MAX = 360, 440
local COLLAPSED_WIDTH, GAP = 44, 6
local HUD_MARGIN, HUD_GAP, HUD_ROW_HEIGHT = 6, 4, 24
local CONTENT_MARGIN = 8
local TABS = {"overview", "character", "group", "inventory", "chat"}
local TAB_LABELS = {overview = "Overview", character = "Character", group = "Group", inventory = "Equipment/Bags", chat = "Chat"}
local SCALE = {[90] = 0.90, [100] = 1.00, [115] = 1.15, [130] = 1.30}

local fallback_themes = {
  obsidian = {
    panel = "#080b12", section = "#111827", elevated = "#172033", border = "#3d4d68",
    text = "#f7f3ff", muted = "#aab6cc", accent = "#55d6ff", jewel = "#c55cff",
    good = "#35e08d", warning = "#ffd166", danger = "#ff5d73", hp = "#35e08d",
    mana = "#55a7ff", moves = "#ffd166", enemy = "#ff5d73", hunger = "#ff9f43",
    thirst = "#45d7ff", track = "#273248", selected = "#263a58",
  },
  ["high-contrast"] = {
    panel = "#000000", section = "#080808", elevated = "#111111", border = "#ffffff",
    text = "#ffffff", muted = "#eeeeee", accent = "#00ffff", jewel = "#ff00ff",
    good = "#00ff66", warning = "#ffff00", danger = "#ff3333", hp = "#00ff66",
    mana = "#00aaff", moves = "#ffff00", enemy = "#ff3333", hunger = "#ff9900",
    thirst = "#00ffff", track = "#444444", selected = "#222222",
  },
}

local function finite(value)
  if util and util.finite then return util.finite(value) end
  local number = tonumber(value)
  if not number or number ~= number or number == math.huge or number == -math.huge then return nil end
  return number
end

local function bounded(value, minimum, maximum, fallback)
  if util and util.bounded then
    local result = util.bounded(value, minimum, maximum)
    if result ~= nil then return result end
  end
  local number = finite(value)
  if not number then return fallback end
  return math.max(minimum, math.min(maximum, number))
end

local function clean(value, maximum)
  if util and util.text then return util.text(value, maximum or 240) end
  if type(value) ~= "string" and type(value) ~= "number" then return nil end
  local result = tostring(value):gsub("[%z\1-\8\11\12\14-\31]", "")
  if #result &gt; (maximum or 240) then result = result:sub(1, maximum or 240) .. "..." end
  return result
end

local function escape(value)
  if util and util.escape then return util.escape(value) end
  local result = clean(value, 2000) or ""
  return result:gsub("&amp;", "&amp;amp;"):gsub("&lt;", "&amp;lt;"):gsub("&gt;", "&amp;gt;")
    :gsub('"', "&amp;quot;"):gsub("'", "&amp;#39;")
end

local function display(value)
  if util and util.display then return util.display(value) end
  local number = finite(value)
  if not number then return clean(value, 80) or "--" end
  if number == math.floor(number) then return string.format("%d", number) end
  return string.format("%.1f", number)
end

local function safe(object, method, ...)
  if util and util.safe_call then return util.safe_call(object, method, ...) end
  if type(object) ~= "table" or type(object[method]) ~= "function" then return false end
  return pcall(object[method], object, ...)
end

local function value(section)
  local result = state and state.value and state.value(section)
  return type(result) == "table" and result or {}
end

local function envelope(section)
  local result = state and state.envelope and state.envelope(section)
  return type(result) == "table" and result or {status = "unavailable"}
end

local function data()
  settings.data = settings.data or {}
  return settings.data
end

local function theme()
  local name = data().theme == "high-contrast" and "high-contrast" or "obsidian"
  local configured = constants and constants.themes and constants.themes[name]
  local base = fallback_themes[name]
  if type(configured) ~= "table" then return base end
  return setmetatable(configured, {__index = base})
end

local function font_size(base)
  local selected = tonumber(data().text_scale) or 100
  return math.max(7, math.floor(base * (SCALE[selected] or 1) + 0.5))
end

local function density(compact, comfortable)
  return data().density == "compact" and compact or comfortable
end

local function widget(name)
  return ui.widgets and ui.widgets[name]
end

local function window_size()
  if type(getMainWindowSize) == "function" then
    local ok, width, height = pcall(getMainWindowSize)
    if ok and finite(width) and finite(height) then return width, height end
  end
  return 1200, 800
end

local function tooltip(object, text)
  if not safe(object, "setToolTip", text) then safe(object, "setToolTip", text, 0) end
end

local function new_label(name, parent)
  return Geyser.Label:new({name = PREFIX .. name, x = 0, y = 0, width = 10, height = 10}, parent)
end

local function new_container(name, parent)
  return Geyser.Container:new({name = PREFIX .. name, x = 0, y = 0, width = 10, height = 10}, parent)
end

local function new_button(name, parent, callback, tip)
  local class = type(Geyser.Button) == "table" and Geyser.Button or Geyser.Label
  local button = class:new({name = PREFIX .. name, x = 0, y = 0, width = 10, height = 10}, parent)
  safe(button, "setClickCallback", callback)
  safe(button, "setCursor", "PointingHand")
  if tip then tooltip(button, tip) end
  return button
end

local function set_button_text(object, text)
  if not safe(object, "setText", text) then safe(object, "echo", "&lt;div align='center'&gt;" .. escape(text) .. "&lt;/div&gt;") end
end

local function persist()
  if settings and settings.save then settings.save() end
end

function ui.message(message)
  if type(echo) == "function" then echo("\n[aardwolf-mudlet] " .. tostring(message) .. "\n") end
end

function ui.available()
  return type(Geyser) == "table" and type(Geyser.Container) == "table"
    and type(Geyser.Label) == "table" and type(Geyser.Mapper) == "table"
    and type(getBorderRight) == "function" and type(setBorderRight) == "function"
end

local function layout()
  local window_width, window_height = window_size()
  local current_right = finite(type(getBorderRight) == "function" and getBorderRight() or 0) or 0
  local claim = data().border_claim
  local base = type(claim) == "table" and current_right == claim.applied and finite(claim.base) or current_right
  base = base or 0
  local usable = math.max(0, window_width - base)
  local minimum_console = math.max(640, usable * 0.50)
  local desired = math.floor(bounded(data().workspace_width, WORKSPACE_MIN, WORKSPACE_MAX, WORKSPACE_DEFAULT))
  local suspended = usable &lt; minimum_console + COLLAPSED_WIDTH
  local collapsed = suspended or data().collapsed_by_user == true or usable - desired &lt; minimum_console
  local workspace = suspended and 0 or (collapsed and COLLAPSED_WIDTH or desired)
  local dock = 0
  if not collapsed and data().inspector_pinned == true then
    dock = math.floor(bounded(usable * 0.22, DOCK_MIN, DOCK_MAX, DOCK_MIN))
    if usable - workspace - GAP - dock &lt; minimum_console then dock = 0 end
  end
  return {
    window_width = window_width, height = window_height, base = base, usable = usable,
    console_min = minimum_console, collapsed = collapsed, suspended = suspended, workspace = workspace,
    dock = dock, total = workspace + (dock &gt; 0 and GAP + dock or 0),
  }
end

local function apply_right_claim(size)
  local d = data()
  local current = finite(getBorderRight()) or 0
  local claim = d.border_claim
  local base = type(claim) == "table" and current == claim.applied and claim.base or current
  if type(claim) == "table" and current ~= claim.applied then ui.border_conflict = true end
  if size &lt;= 0 then
    d.border_claim = nil
    persist()
    return base
  end
  setBorderRight(base + size)
  d.border_claim = {base = base, width = size, applied = base + size}
  ui.border_conflict = false
  persist()
  return base
end

local function release_right_claim()
  local claim = data().border_claim
  if type(claim) ~= "table" then return true end
  local current = finite(getBorderRight()) or 0
  if current == claim.applied then setBorderRight(claim.base) else ui.border_conflict = true end
  data().border_claim = nil
  persist()
  return not ui.border_conflict
end

local function hud_rows(width)
  if width &lt; 640 then return {{"hp", "mana"}, {"moves", "tnl", "target"}, {"hunger", "thirst"}} end
  return {{"hp", "mana", "moves"}, {"tnl", "target", "hunger", "thirst"}}
end

local function hud_height(width)
  return HUD_MARGIN * 2 + #hud_rows(width) * HUD_ROW_HEIGHT + (#hud_rows(width) - 1) * HUD_GAP
end

local function apply_bottom_claim(height)
  if type(getBorderBottom) ~= "function" or type(setBorderBottom) ~= "function" then return 0 end
  local d = data()
  local current = finite(getBorderBottom()) or 0
  local claim = d.bottom_border_claim
  local base = type(claim) == "table" and current == claim.applied and claim.base or current
  setBorderBottom(base + height)
  d.bottom_border_claim = {base = base, height = height, applied = base + height}
  persist()
  return base
end

local function release_bottom_claim()
  if type(getBorderBottom) ~= "function" or type(setBorderBottom) ~= "function" then return end
  local claim = data().bottom_border_claim
  if type(claim) ~= "table" then return end
  if (finite(getBorderBottom()) or 0) == claim.applied then setBorderBottom(claim.base) else ui.bottom_border_conflict = true end
  data().bottom_border_claim = nil
  persist()
end

function ui.release_saved_claims()
  -- Claim records are proof of ownership only when the live border still
  -- equals the exact applied value. The release helpers deliberately leave a
  -- conflicting border untouched, but discard stale ownership either way.
  release_bottom_claim()
  release_right_claim()
end

local function make_gauge(name, parent)
  local gauge = new_container("hud-" .. name, parent)
  gauge.surface = new_label("hud-" .. name .. "-surface", gauge)
  gauge.track = new_label("hud-" .. name .. "-track", gauge)
  gauge.fill = new_label("hud-" .. name .. "-fill", gauge)
  gauge.text = new_label("hud-" .. name .. "-text", gauge)
  return gauge
end

local function set_gauge(name, label, current, maximum, explicit_percent)
  local gauge = widget("gauge_" .. name)
  if not gauge then return end
  local percent = finite(explicit_percent)
  if percent == nil and finite(current) and finite(maximum) and finite(maximum) &gt; 0 then percent = current / maximum * 100 end
  percent = bounded(percent, 0, 100, 0)
  safe(gauge.fill, "resize", string.format("%.1f%%", percent), 3)
  local suffix = maximum ~= nil and (" " .. display(current) .. "/" .. display(maximum)) or (" " .. display(current))
  safe(gauge.text, "echo", "&lt;div align='center'&gt;&lt;b&gt;" .. escape(label .. suffix) .. "&lt;/b&gt;&lt;/div&gt;")
end

local function set_target_gauge()
  local gauge = widget("gauge_target")
  if not gauge then return end
  local status = value("status")
  local status_state = envelope("status").status
  local name = clean(status.enemy, 80)
  local percent = bounded(status.enemypct, 0, 100, 0)
  safe(gauge.fill, "resize", string.format("%.1f%%", percent), 3)
  local label
  if status_state == "unavailable" or status_state == "error" then label = "Target unavailable"
  elseif not name or name == "" or name:lower() == "none" then label = "Target No target"
  else label = "Target " .. name .. " " .. display(percent) .. "%" end
  safe(gauge.text, "echo", "&lt;div align='center'&gt;&lt;b&gt;" .. escape(label) .. "&lt;/b&gt;&lt;/div&gt;")
end

local function status_badge(section)
  local status = clean(envelope(section).status, 20) or "unavailable"
  local labels = {current = "CURRENT", partial = "PARTIAL", stale = "STALE", error = "ERROR", unavailable = "UNAVAILABLE"}
  return labels[status] or status:upper(), status
end

local function key_value(rows)
  local output = {"&lt;table width='100%' cellspacing='2' cellpadding='1'&gt;"}
  for _, row in ipairs(rows) do
    output[#output + 1] = "&lt;tr&gt;&lt;td&gt;&lt;font color='" .. theme().muted .. "'&gt;" .. escape(row[1]) .. "&lt;/font&gt;&lt;/td&gt;&lt;td align='right'&gt;&lt;b&gt;" .. escape(display(row[2])) .. "&lt;/b&gt;&lt;/td&gt;&lt;/tr&gt;"
  end
  output[#output + 1] = "&lt;/table&gt;"
  return table.concat(output)
end

local function tab_callback(name)
  return function()
    if commands and commands.set_tab then commands.set_tab(name) else ui.set_tab(name) end
  end
end

local function inventory_callback(name)
  return function()
    if commands and commands.select_inventory_tab then commands.select_inventory_tab(name)
    else data().inventory_tab = name; persist(); ui.request_render() end
  end
end

local function toggle_empty()
  if commands and commands.toggle_empty then commands.toggle_empty()
  else data().show_empty_slots = not data().show_empty_slots; persist(); ui.request_render() end
end

local function execute_action(id)
  return function() if actions and actions.execute then actions.execute(id); ui.request_render() end end
end

local function pending_action()
  if not actions then return nil end
  if type(actions.pending) == "function" then return actions.pending() end
  return type(actions.pending) == "table" and actions.pending or nil
end

local function confirm_action()
  if not actions then return end
  if actions.confirm_pending then actions.confirm_pending()
  elseif actions.confirm then actions.confirm() end
  ui.request_render()
end

local function cancel_action()
  if not actions then return end
  if actions.cancel_pending then actions.cancel_pending()
  elseif actions.cancel then actions.cancel() end
  ui.request_render()
end

local function command_line_text(object)
  if type(object) ~= "table" then return nil end
  for _, method in ipairs({"getText", "getCommandLine", "text"}) do
    if type(object[method]) == "function" then
      local ok, result = pcall(object[method], object)
      if ok and type(result) == "string" then return result end
    end
  end
  return nil
end

local function submit_custom_editor()
  local specification = command_line_text(widget("editor_command")) or ""
  local label, category, command = specification:match("^%s*([^|]+)%s*|%s*([^|]+)%s*|%s*(.-)%s*$")
  if not label then label, command = specification:match("^%s*([^|]+)%s*|%s*(.-)%s*$"); category = "Custom" end
  local ok, error_message
  if ui.editing_action_id then
    local update = actions and (actions.update_custom or actions.update)
    if update then ok, error_message = update(ui.editing_action_id, label, command, category) end
  else
    local add = actions and (actions.add_custom or actions.add)
    if add then ok, error_message = add(label, command, category) end
  end
  if ok then
    ui.editor_open, ui.editing_action_id = false, nil
  else
    ui.message(error_message or "Enter Label | Category | command.")
  end
  ui.request_render()
end

local function create_content(parent)
  local content_parent = parent
  ui.scroll_capable = type(Geyser.ScrollBox) == "table"
  if ui.scroll_capable then
    ui.widgets.scroll = Geyser.ScrollBox:new({name = PREFIX .. "scroll", x = 0, y = 0, width = 10, height = 10}, parent)
    content_parent = ui.widgets.scroll
  else
    ui.widgets.pager = new_container("pager", parent)
    ui.widgets.page_prev = new_button("page-prev", ui.widgets.pager, function() ui.page = math.max(1, (ui.page or 1) - 1); ui.request_render() end, "Previous page")
    ui.widgets.page_next = new_button("page-next", ui.widgets.pager, function() ui.page = (ui.page or 1) + 1; ui.request_render() end, "Next page")
    ui.widgets.page_status = new_label("page-status", ui.widgets.pager)
    content_parent = ui.widgets.pager
  end
  for _, tab in ipairs(TABS) do
    ui.widgets[tab .. "_content"] = new_container(tab .. "-content", content_parent)
  end
  return content_parent
end

function ui.build()
  if ui.root then return true end
  if not ui.available() then return false end
  local l = layout()
  local base = apply_right_claim(l.total)
  local console_width = math.max(0, l.window_width - base - l.total)
  local bottom_height = hud_height(console_width)
  local bottom_base = apply_bottom_claim(bottom_height)
  ui.root = Geyser.Container:new({name = PREFIX .. "root", x = -(base + l.total), y = 0, width = l.total, height = "100%"})
  ui.bottom_root = Geyser.Container:new({name = PREFIX .. "bottom-root", x = 0, y = -(bottom_base + bottom_height), width = console_width, height = bottom_height})
  ui.widgets = {}
  local w = ui.widgets
  w.background = new_label("background", ui.root)
  w.rail = new_button("restore-rail", ui.root, function()
    data().collapsed_by_user = false; persist(); ui.reflow(); ui.request_render()
  end, "Restore Aardwolf Command Deck")
  w.workspace = new_container("workspace", ui.root)
  w.header = new_label("header", w.workspace)
  w.palette_toggle = new_button("palette-toggle", w.workspace, function()
    if commands and commands.toggle_palette then commands.toggle_palette() else ui.toggle_palette() end
  end, "Open the contextual action drawer")
  w.context = new_container("context-strip", w.workspace)
  for index = 1, 4 do w["context_" .. index] = new_button("context-" .. index, w.context, function() end) end
  w.map_toolbar = new_container("map-toolbar", w.workspace)
  w.map_import = new_button("map-import", w.map_toolbar, execute_action("map-import"), "Merge the packaged Aardwolf snapshot; do not use Mudlet's native map loader")
  w.map_center = new_button("map-center", w.map_toolbar, function() if aardwolf_mudlet.lifecycle and aardwolf_mudlet.lifecycle.center_map then aardwolf_mudlet.lifecycle.center_map() end end, "Center the map on the current room")
  w.map_zoom_in = new_button("map-zoom-in", w.map_toolbar, execute_action("map-zoom-in"), "Zoom map in")
  w.map_zoom_out = new_button("map-zoom-out", w.map_toolbar, execute_action("map-zoom-out"), "Zoom map out")
  w.map_status = new_label("map-status", w.workspace)
  -- Geyser.Mapper ultimately positions Mudlet's native mapper relative to its
  -- window. A mapper nested beneath ScrollBox loses the outer workspace
  -- offset on Mudlet 4.22 and is drawn over the game console. Keep it as a
  -- direct workspace child and place it within the persistent map region.
  w.mapper = Geyser.Mapper:new({name = PREFIX .. "mapper", x = 0, y = 0, width = 10, height = 10, embedded = true}, w.workspace)
  w.map_legend = new_label("map-legend", w.workspace)

  -- The same data dock moves between the far-right pinned column and a
  -- stacked position below the map. Building it once avoids duplicate Geyser
  -- widgets, stale scroll state, and competing lifecycle ownership.
  w.data_dock = new_container("data-dock", ui.root)
  w.dock_background = new_label("dock-background", w.data_dock)
  w.dock_header = new_label("dock-header", w.data_dock)
  w.pin_toggle = new_button("pin-toggle", w.data_dock, function()
    if commands and commands.toggle_pin then commands.toggle_pin() else data().inspector_pinned = not data().inspector_pinned; persist(); ui.reflow() end
  end, "Pin or unpin the player data dock")
  w.tabs = new_container("tabs", w.data_dock)
  for _, tab in ipairs(TABS) do w["tab_" .. tab] = new_button("tab-" .. tab, w.tabs, tab_callback(tab), "Show " .. TAB_LABELS[tab] .. " data") end
  w.content = new_container("content", w.data_dock)
  create_content(w.content)

  w.overview_actions = new_container("overview-actions", w.overview_content)
  w.overview_character_refresh = new_button("overview-character-refresh", w.overview_actions, execute_action("character-refresh"), "Request a fresh character GMCP snapshot")
  w.overview_quest_refresh = new_button("overview-quest-refresh", w.overview_actions, execute_action("quest-refresh"), "Request fresh quest status")
  w.overview_inventory_refresh = new_button("overview-inventory-refresh", w.overview_actions, execute_action("details-refresh"), "Refresh equipment and bag details")
  w.overview_card = new_label("overview-card", w.overview_content)
  w.character_card = new_label("character-card", w.character_content)
  w.group_card = new_label("group-card", w.group_content)
  w.inventory_toolbar = new_container("inventory-toolbar", w.inventory_content)
  w.inventory_equipment = new_button("inventory-equipment", w.inventory_toolbar, inventory_callback("equipment"), "Show equipped items")
  w.inventory_bags = new_button("inventory-bags", w.inventory_toolbar, inventory_callback("bags"), "Show containers and weight")
  w.inventory_empty = new_button("inventory-empty", w.inventory_toolbar, toggle_empty, "Show or hide empty equipment slots")
  w.inventory_refresh = new_button("inventory-refresh", w.inventory_toolbar, function() if aardwolf_mudlet.details and aardwolf_mudlet.details.refresh then aardwolf_mudlet.details.refresh() end end, "Refresh equipment and bag details")
  w.inventory_card = new_label("inventory-card", w.inventory_content)
  w.chat_toolbar = new_container("chat-toolbar", w.chat_content)
  for index = 1, 5 do
    w["chat_tab_" .. index] = new_button("chat-tab-" .. index, w.chat_toolbar, function()
      local tabs = aardwolf_mudlet.chat and aardwolf_mudlet.chat.tabs and aardwolf_mudlet.chat.tabs() or {}
      if tabs[index] and aardwolf_mudlet.chat.select then aardwolf_mudlet.chat.select(index) end
    end, "Select chat tab")
  end
  w.chat_clear = new_button("chat-clear", w.chat_toolbar, function() if aardwolf_mudlet.chat then aardwolf_mudlet.chat.clear() end; ui.request_render() end, "Clear the active package chat buffer")
  w.chat_copy = new_button("chat-copy", w.chat_toolbar, function() if aardwolf_mudlet.chat then aardwolf_mudlet.chat.copy_active(false) end end, "Copy the active chat tab as plain text")
  w.chat_card = new_label("chat-card", w.chat_content)

  w.drawer = new_container("action-drawer", ui.root)
  w.drawer_background = new_label("action-drawer-background", w.drawer)
  w.drawer_header = new_label("action-drawer-header", w.drawer)
  w.drawer_close = new_button("action-drawer-close", w.drawer, function() ui.toggle_palette(false) end, "Close action drawer")
  w.drawer_actions = new_label("action-drawer-actions", w.drawer)
  w.drawer_buttons = new_container("action-drawer-buttons", w.drawer)
  for index = 1, 8 do w["drawer_action_" .. index] = new_button("action-drawer-action-" .. index, w.drawer_buttons, function() end) end
  w.drawer_prev = new_button("action-drawer-prev", w.drawer, function() ui.action_page = math.max(1, (ui.action_page or 1) - 1); ui.request_render() end, "Previous action page")
  w.drawer_next = new_button("action-drawer-next", w.drawer, function() ui.action_page = (ui.action_page or 1) + 1; ui.request_render() end, "Next action page")
  w.drawer_page = new_label("action-drawer-page", w.drawer)
  w.drawer_add = new_button("action-drawer-add", w.drawer, function() ui.prompt_custom_editor() end, "Add a custom action")

  w.editor = new_container("custom-editor", ui.root)
  w.editor_background = new_label("custom-editor-background", w.editor)
  w.editor_title = new_label("custom-editor-title", w.editor)
  w.editor_help = new_label("custom-editor-help", w.editor)
  if type(Geyser.CommandLine) == "table" then
    w.editor_command = Geyser.CommandLine:new({name = PREFIX .. "custom-editor-command", x = 0, y = 0, width = 10, height = 26}, w.editor)
  else
    w.editor_command = new_label("custom-editor-command-fallback", w.editor)
  end
  w.editor_save = new_button("custom-editor-save", w.editor, submit_custom_editor, "Validate and save the custom action")
  w.editor_cancel = new_button("custom-editor-cancel", w.editor, function() ui.editor_open = false; ui.request_render() end, "Cancel editing")

  w.confirm = new_container("custom-confirm", ui.root)
  w.confirm_background = new_label("custom-confirm-background", w.confirm)
  w.confirm_text = new_label("custom-confirm-text", w.confirm)
  w.confirm_send = new_button("custom-confirm-send", w.confirm, confirm_action, "Send this custom command")
  w.confirm_cancel = new_button("custom-confirm-cancel", w.confirm, cancel_action, "Cancel without sending")

  w.bottom_background = new_label("bottom-background", ui.bottom_root)
  for _, name in ipairs({"hp", "mana", "moves", "tnl", "target", "hunger", "thirst"}) do w["gauge_" .. name] = make_gauge(name, ui.bottom_root) end
  ui.page = 1
  ui.action_page = 1
  ui.mapper = w.mapper
  ui.apply_theme()
  ui.reflow()
  return true
end

function ui.apply_theme()
  if not ui.widgets then return end
  local t = theme()
  local panel = string.format("background-color:%s;color:%s;border:1px solid %s;", t.panel, t.text, t.border)
  local section = string.format("background-color:%s;color:%s;border:1px solid %s;padding:%dpx;font-size:%dpt;", t.section, t.text, t.border, density(3, 6), font_size(9))
  local header = string.format("background-color:%s;color:%s;border-bottom:2px solid %s;padding:%dpx;font-size:%dpt;font-weight:bold;", t.section, t.accent, t.jewel, density(4, 7), font_size(11))
  local button = string.format("background-color:%s;color:%s;border:1px solid %s;padding:3px;font-size:%dpt;font-weight:bold;", t.elevated, t.text, t.border, font_size(9))
  local selected = string.format("background-color:%s;color:%s;border:1px solid %s;padding:3px;font-size:%dpt;font-weight:bold;", t.selected, t.accent, t.accent, font_size(9))
  for _, name in ipairs({"background", "bottom_background", "dock_background", "drawer_background", "editor_background", "confirm_background"}) do safe(widget(name), "setStyleSheet", panel) end
  for _, name in ipairs({"header", "dock_header", "drawer_header", "editor_title"}) do safe(widget(name), "setStyleSheet", header) end
  for _, name in ipairs({"map_status", "map_legend", "overview_card", "character_card", "group_card", "inventory_card", "chat_card", "drawer_actions", "editor_help", "confirm_text"}) do safe(widget(name), "setStyleSheet", section) end
  for _, name in ipairs({"rail", "palette_toggle", "pin_toggle", "map_import", "map_center", "map_zoom_in", "map_zoom_out", "overview_character_refresh", "overview_quest_refresh", "overview_inventory_refresh", "inventory_equipment", "inventory_bags", "inventory_empty", "inventory_refresh", "chat_clear", "chat_copy", "drawer_close", "drawer_add", "drawer_prev", "drawer_next", "editor_save", "editor_cancel", "confirm_send", "confirm_cancel", "page_prev", "page_next"}) do safe(widget(name), "setStyleSheet", button) end
  for index = 1, 4 do safe(widget("context_" .. index), "setStyleSheet", button) end
  for index = 1, 8 do safe(widget("drawer_action_" .. index), "setStyleSheet", button) end
  for index = 1, 5 do safe(widget("chat_tab_" .. index), "setStyleSheet", button) end
  for _, tab in ipairs(TABS) do safe(widget("tab_" .. tab), "setStyleSheet", data().active_tab == tab and selected or button) end
  safe(widget("inventory_equipment"), "setStyleSheet", data().inventory_tab ~= "bags" and selected or button)
  safe(widget("inventory_bags"), "setStyleSheet", data().inventory_tab == "bags" and selected or button)
  local colors = {hp = t.hp, mana = t.mana, moves = t.moves, tnl = t.accent, target = t.enemy, hunger = t.hunger, thirst = t.thirst}
  for name, color in pairs(colors) do
    local gauge = widget("gauge_" .. name)
    safe(gauge.surface, "setStyleSheet", string.format("background-color:%s;border:1px solid %s;", t.section, t.border))
    safe(gauge.track, "setStyleSheet", "background-color:" .. t.track .. ";")
    safe(gauge.fill, "setStyleSheet", "background-color:" .. color .. ";")
    safe(gauge.text, "setStyleSheet", string.format("background-color:transparent;color:%s;font-size:%dpt;", t.text, font_size(8)))
  end
end

local function resize_claim(l)
  if l.total &lt;= 0 then
    release_right_claim()
    ui.base_right = l.base
    return
  end
  local claim = data().border_claim
  if type(claim) ~= "table" then ui.base_right = apply_right_claim(l.total); return end
  local current = finite(getBorderRight()) or 0
  if current == claim.applied then
    setBorderRight(claim.base + l.total); claim.width = l.total; claim.applied = claim.base + l.total
    ui.base_right = claim.base
  else ui.border_conflict = true; ui.base_right = claim.base or 0 end
end

function ui.reflow()
  if not ui.root then return false end
  local l = layout()
  resize_claim(l)
  ui.layout = l
  safe(ui.root, "move", -(ui.base_right + l.total), 0); safe(ui.root, "resize", l.total, "100%")
  safe(widget("background"), "move", 0, 0); safe(widget("background"), "resize", "100%", "100%")
  if l.suspended then
    safe(ui.root, "hide")
  elseif l.collapsed then
    safe(ui.root, "show")
    safe(widget("rail"), "move", 0, 0); safe(widget("rail"), "resize", COLLAPSED_WIDTH, "100%"); safe(widget("rail"), "show")
    safe(widget("workspace"), "hide"); safe(widget("data_dock"), "hide")
  else
    safe(ui.root, "show")
    safe(widget("rail"), "hide"); safe(widget("workspace"), "show")
    safe(widget("workspace"), "move", 0, 0); safe(widget("workspace"), "resize", l.workspace, "100%")
    safe(widget("header"), "move", CONTENT_MARGIN, CONTENT_MARGIN); safe(widget("header"), "resize", l.workspace - 68, density(92, 112))
    safe(widget("palette_toggle"), "move", l.workspace - 52, CONTENT_MARGIN); safe(widget("palette_toggle"), "resize", 44, 32)
    local header_bottom = CONTENT_MARGIN + density(92, 112) + GAP
    safe(widget("context"), "move", CONTENT_MARGIN, header_bottom); safe(widget("context"), "resize", l.workspace - CONTENT_MARGIN * 2, 32)
    local action_width = (l.workspace - CONTENT_MARGIN * 2 - GAP * 3) / 4
    for index = 1, 4 do safe(widget("context_" .. index), "move", (index - 1) * (action_width + GAP), 0); safe(widget("context_" .. index), "resize", action_width, 32) end
    local map_y = header_bottom + 32 + GAP
    local available_height = math.max(80, l.height - map_y - CONTENT_MARGIN)
    local map_height, dock_x, dock_y, dock_width, dock_height
    if l.dock &gt; 0 then
      map_height = available_height
      dock_x, dock_y, dock_width, dock_height = l.workspace + GAP, 0, l.dock, l.height
    else
      map_height = math.floor(bounded(available_height * 0.45, 160, 420, 240))
      local minimum_split = 160 + GAP + 180
      if available_height &gt;= minimum_split and available_height - map_height - GAP &lt; 180 then map_height = available_height - GAP - 180 end
      if available_height &lt; minimum_split then map_height = math.max(80, math.floor((available_height - GAP) * 0.47)) end
      dock_x, dock_y, dock_width = CONTENT_MARGIN, map_y + map_height + GAP, l.workspace - CONTENT_MARGIN * 2
      dock_height = math.max(80, l.height - dock_y - CONTENT_MARGIN)
    end

    local map_status_height = map_height &gt;= 180 and 40 or 28
    local map_legend_height = map_height &gt;= 180 and 32 or 24
    local mapper_y = map_y + 34 + map_status_height + 4
    local legend_y = map_y + math.max(34 + map_status_height + 12, map_height - map_legend_height)
    local mapper_height = math.max(8, legend_y - mapper_y - 4)
    safe(widget("map_toolbar"), "move", CONTENT_MARGIN, map_y); safe(widget("map_toolbar"), "resize", l.workspace - CONTENT_MARGIN * 2, 30)
    safe(widget("map_import"), "move", 0, 0); safe(widget("map_import"), "resize", 78, 30)
    safe(widget("map_center"), "move", 84, 0); safe(widget("map_center"), "resize", 78, 30)
    safe(widget("map_zoom_in"), "move", 168, 0); safe(widget("map_zoom_in"), "resize", 38, 30)
    safe(widget("map_zoom_out"), "move", 212, 0); safe(widget("map_zoom_out"), "resize", 38, 30)
    safe(widget("map_status"), "move", CONTENT_MARGIN, map_y + 34); safe(widget("map_status"), "resize", l.workspace - CONTENT_MARGIN * 2, map_status_height)
    -- The mapper remains a direct workspace child because nested ScrollBox
    -- coordinates can place Mudlet's native mapper over the game console.
    safe(widget("mapper"), "move", CONTENT_MARGIN, mapper_y); safe(widget("mapper"), "resize", l.workspace - CONTENT_MARGIN * 2, mapper_height)
    safe(widget("map_legend"), "move", CONTENT_MARGIN, legend_y); safe(widget("map_legend"), "resize", l.workspace - CONTENT_MARGIN * 2, map_legend_height)

    safe(widget("data_dock"), "move", dock_x, dock_y); safe(widget("data_dock"), "resize", dock_width, dock_height); safe(widget("data_dock"), "show")
    safe(widget("dock_background"), "move", 0, 0); safe(widget("dock_background"), "resize", "100%", "100%")
    safe(widget("dock_header"), "move", CONTENT_MARGIN, CONTENT_MARGIN); safe(widget("dock_header"), "resize", dock_width - 84, 36)
    safe(widget("pin_toggle"), "move", dock_width - 68, CONTENT_MARGIN); safe(widget("pin_toggle"), "resize", 60, 32)
    local tabs_y = 50
    safe(widget("tabs"), "move", CONTENT_MARGIN, tabs_y); safe(widget("tabs"), "resize", dock_width - CONTENT_MARGIN * 2, 32)
    local tab_width = (dock_width - CONTENT_MARGIN * 2 - GAP * (#TABS - 1)) / #TABS
    for index, tab in ipairs(TABS) do safe(widget("tab_" .. tab), "move", (index - 1) * (tab_width + GAP), 0); safe(widget("tab_" .. tab), "resize", tab_width, 32) end
    local content_y = tabs_y + 32 + GAP
    local content_width, content_height = dock_width - CONTENT_MARGIN * 2, math.max(56, dock_height - content_y - CONTENT_MARGIN)
    safe(widget("content"), "move", CONTENT_MARGIN, content_y); safe(widget("content"), "resize", content_width, content_height)
    if widget("scroll") then safe(widget("scroll"), "move", 0, 0); safe(widget("scroll"), "resize", content_width, content_height) end
    if widget("pager") then
      safe(widget("pager"), "move", 0, 0); safe(widget("pager"), "resize", content_width, content_height)
      safe(widget("page_prev"), "move", 0, content_height - 28); safe(widget("page_prev"), "resize", 36, 26)
      safe(widget("page_next"), "move", content_width - 36, content_height - 28); safe(widget("page_next"), "resize", 36, 26)
      safe(widget("page_status"), "move", 42, content_height - 28); safe(widget("page_status"), "resize", content_width - 84, 26)
    end
    local page_reserved = widget("pager") and 32 or 0
    for _, tab in ipairs(TABS) do safe(widget(tab .. "_content"), "move", 0, 0); safe(widget(tab .. "_content"), "resize", content_width, content_height - page_reserved) end
    local overview_height = content_height - page_reserved
    if ui.scroll_capable then overview_height = math.max(overview_height, density(600, 700)) end
    safe(widget("overview_content"), "resize", content_width, overview_height)
    safe(widget("overview_actions"), "move", 0, 0); safe(widget("overview_actions"), "resize", content_width, 32)
    local overview_button_width = (content_width - GAP * 2) / 3
    for index, name in ipairs({"overview_character_refresh", "overview_quest_refresh", "overview_inventory_refresh"}) do safe(widget(name), "move", (index - 1) * (overview_button_width + GAP), 0); safe(widget(name), "resize", overview_button_width, 30) end
    safe(widget("overview_card"), "move", 0, 38); safe(widget("overview_card"), "resize", content_width, overview_height - 38)
    local character_height = content_height - page_reserved
    if ui.scroll_capable then character_height = math.max(character_height, density(720, 800)) end
    safe(widget("character_content"), "resize", content_width, character_height)
    safe(widget("character_card"), "move", 0, 0); safe(widget("character_card"), "resize", content_width, character_height)
    safe(widget("group_card"), "move", 0, 0); safe(widget("group_card"), "resize", content_width, content_height - page_reserved)
    safe(widget("inventory_toolbar"), "move", 0, 0); safe(widget("inventory_toolbar"), "resize", content_width, 32)
    safe(widget("inventory_equipment"), "move", 0, 0); safe(widget("inventory_equipment"), "resize", 86, 30)
    safe(widget("inventory_bags"), "move", 92, 0); safe(widget("inventory_bags"), "resize", 64, 30)
    safe(widget("inventory_empty"), "move", 162, 0); safe(widget("inventory_empty"), "resize", 100, 30)
    safe(widget("inventory_refresh"), "move", content_width - 72, 0); safe(widget("inventory_refresh"), "resize", 72, 30)
    safe(widget("inventory_card"), "move", 0, 38); safe(widget("inventory_card"), "resize", content_width, content_height - 38)
    safe(widget("chat_toolbar"), "move", 0, 0); safe(widget("chat_toolbar"), "resize", content_width, 66)
    local chat_tab_width = math.max(44, (content_width - GAP * 4) / 5)
    for index = 1, 5 do safe(widget("chat_tab_" .. index), "move", (index - 1) * (chat_tab_width + GAP), 0); safe(widget("chat_tab_" .. index), "resize", chat_tab_width, 30) end
    safe(widget("chat_clear"), "move", 0, 36); safe(widget("chat_clear"), "resize", 70, 28)
    safe(widget("chat_copy"), "move", 76, 36); safe(widget("chat_copy"), "resize", 70, 28)
    safe(widget("chat_card"), "move", 0, 72); safe(widget("chat_card"), "resize", content_width, content_height - 72)
    local overlay_width = l.workspace - CONTENT_MARGIN * 4
    for _, name in ipairs({"drawer", "editor", "confirm"}) do safe(widget(name), "move", CONTENT_MARGIN * 2, density(150, 180)); safe(widget(name), "resize", overlay_width, math.min(460, l.height - density(170, 200))) end
    for _, prefix in ipairs({"drawer", "editor", "confirm"}) do safe(widget(prefix .. "_background"), "move", 0, 0); safe(widget(prefix .. "_background"), "resize", "100%", "100%") end
    safe(widget("drawer_header"), "move", 8, 8); safe(widget("drawer_header"), "resize", overlay_width - 54, 34)
    safe(widget("drawer_close"), "move", overlay_width - 42, 8); safe(widget("drawer_close"), "resize", 34, 34)
    safe(widget("drawer_actions"), "move", 8, 48); safe(widget("drawer_actions"), "resize", overlay_width - 16, 26)
    local drawer_body_height = math.min(306, l.height - 330)
    safe(widget("drawer_buttons"), "move", 8, 78); safe(widget("drawer_buttons"), "resize", overlay_width - 16, drawer_body_height)
    local drawer_button_height = math.max(22, (drawer_body_height - GAP * 3) / 4)
    local drawer_column_width = (overlay_width - 16 - GAP) / 2
    for index = 1, 8 do
      local column, row = math.floor((index - 1) / 4), (index - 1) % 4
      safe(widget("drawer_action_" .. index), "move", column * (drawer_column_width + GAP), row * (drawer_button_height + GAP)); safe(widget("drawer_action_" .. index), "resize", drawer_column_width, drawer_button_height)
    end
    local drawer_controls_y = 84 + drawer_body_height
    safe(widget("drawer_prev"), "move", 8, drawer_controls_y); safe(widget("drawer_prev"), "resize", 34, 28)
    safe(widget("drawer_page"), "move", 48, drawer_controls_y); safe(widget("drawer_page"), "resize", overlay_width - 104, 28)
    safe(widget("drawer_next"), "move", overlay_width - 42, drawer_controls_y); safe(widget("drawer_next"), "resize", 34, 28)
    safe(widget("drawer_add"), "move", 8, drawer_controls_y + 34); safe(widget("drawer_add"), "resize", overlay_width - 16, 30)
    safe(widget("editor_title"), "move", 8, 8); safe(widget("editor_title"), "resize", overlay_width - 16, 34)
    safe(widget("editor_help"), "move", 8, 48); safe(widget("editor_help"), "resize", overlay_width - 16, 110)
    safe(widget("editor_command"), "move", 8, 166); safe(widget("editor_command"), "resize", overlay_width - 16, 28)
    safe(widget("editor_save"), "move", 8, 204); safe(widget("editor_save"), "resize", 90, 32)
    safe(widget("editor_cancel"), "move", 104, 204); safe(widget("editor_cancel"), "resize", 90, 32)
    safe(widget("confirm_text"), "move", 8, 48); safe(widget("confirm_text"), "resize", overlay_width - 16, 116)
    safe(widget("confirm_send"), "move", 8, 174); safe(widget("confirm_send"), "resize", 90, 34)
    safe(widget("confirm_cancel"), "move", 104, 174); safe(widget("confirm_cancel"), "resize", 90, 34)
  end

  local console_width = math.max(0, l.window_width - (ui.base_right or l.base) - l.total)
  local height = hud_height(console_width)
  local bottom_claim = data().bottom_border_claim
  local bottom_base = type(bottom_claim) == "table" and bottom_claim.base or 0
  if type(bottom_claim) == "table" and type(getBorderBottom) == "function" and getBorderBottom() == bottom_claim.applied and bottom_claim.height ~= height then
    setBorderBottom(bottom_base + height); bottom_claim.height = height; bottom_claim.applied = bottom_base + height
  end
  safe(ui.bottom_root, "move", 0, -(bottom_base + height)); safe(ui.bottom_root, "resize", console_width, height)
  safe(widget("bottom_background"), "move", 0, 0); safe(widget("bottom_background"), "resize", "100%", "100%")
  local rows = hud_rows(console_width)
  for row_index, row in ipairs(rows) do
    local available = console_width - HUD_MARGIN * 2 - HUD_GAP * (#row - 1)
    local item_width = math.max(40, available / #row)
    for index, name in ipairs(row) do
      local gauge = widget("gauge_" .. name); local x = HUD_MARGIN + (index - 1) * (item_width + HUD_GAP); local y = HUD_MARGIN + (row_index - 1) * (HUD_ROW_HEIGHT + HUD_GAP)
      safe(gauge, "move", x, y); safe(gauge, "resize", item_width, HUD_ROW_HEIGHT)
      safe(gauge.surface, "move", 0, 0); safe(gauge.surface, "resize", "100%", "100%")
      safe(gauge.track, "move", 3, HUD_ROW_HEIGHT - 6); safe(gauge.track, "resize", item_width - 6, 3)
      safe(gauge.fill, "move", 3, HUD_ROW_HEIGHT - 6); safe(gauge.fill, "resize", 0, 3)
      safe(gauge.text, "move", 0, 2); safe(gauge.text, "resize", "100%", HUD_ROW_HEIGHT - 7)
    end
  end
  ui.apply_theme()
  return true
end

local function render_header()
  local room, base, status = value("room"), value("base"), value("status")
  local tick = state and state.tick_remaining and state.tick_remaining() or nil
  local state_name = constants and constants.character_states and constants.character_states[finite(status.state)] or status.state_name or "Unknown"
  local room_badge = status_badge("room")
  local exits = room.exits
  local exit_names = {}
  if type(exits) == "table" then for direction, destination in pairs(exits) do if destination ~= nil and destination ~= false then exit_names[#exit_names + 1] = clean(direction, 12) end end end
  table.sort(exit_names)
  local html = string.format("&lt;b&gt;%s&lt;/b&gt; &lt;font color='%s'&gt;%s&lt;/font&gt;&lt;br&gt;&lt;font color='%s'&gt;%s&lt;/font&gt;&lt;br&gt;%s &amp;middot; vnum %s &amp;middot; %s &amp;middot; tick %s&lt;br&gt;&lt;font color='%s'&gt;%s&lt;/font&gt;",
    escape(base.name or base.character or "Aardwolf"), theme().accent, escape(state_name), theme().text,
    escape(room.name or "Room unavailable"), escape(room.area or room.zone or "Unknown area"), escape(room.num or room.vnum),
    escape(room.terrain or room.environment or "unknown"), escape(display(tick)), theme().muted,
    escape(#exit_names &gt; 0 and ("Exits: " .. table.concat(exit_names, " ")) or ("Room data: " .. room_badge)))
  safe(widget("header"), "echo", html)
  set_button_text(widget("palette_toggle"), "Actions")
  set_button_text(widget("rail"), "A\nA\nR\nD")
end

local function render_context()
  local list = actions and actions.context and actions.context() or {}
  for index = 1, 4 do
    local action = type(list) == "table" and list[index] or nil
    local button = widget("context_" .. index)
    set_button_text(button, action and (action.label or action.name or action.id) or "--")
    safe(button, "setClickCallback", action and execute_action(action.id) or function() end)
    tooltip(button, action and (action.description or action.label or action.id) or "No contextual action")
  end
end

local function render_map()
  local room, map = value("room"), value("map")
  local badge = status_badge("map")
  set_button_text(widget("map_import"), "Import")
  set_button_text(widget("map_center"), "Center")
  set_button_text(widget("map_zoom_in"), "+")
  set_button_text(widget("map_zoom_out"), "-")
  local phase = map.phase or map.import_phase or "idle"
  local owned = finite(map.owned_room_count or map.owned_count) or 0
  local guidance = owned == 0 and phase ~= "rooms" and phase ~= "exits" and " &amp;middot; &lt;font color='" .. theme().warning .. "'&gt;Import required&lt;/font&gt;" or ""
  local source_hash = clean(map.source_hash, 64) or "--"
  if #source_hash &gt; 10 then source_hash = source_hash:sub(1, 10) end
  safe(widget("map_status"), "echo", string.format("&lt;b&gt;Map %s&lt;/b&gt; &amp;middot; %s &amp;middot; owned %s &amp;middot; source %s%s&lt;br&gt;Room %s &amp;rarr; %s &amp;middot; coordinates %s, %s, %s",
    escape(badge), escape(phase), escape(display(owned)), escape(source_hash), guidance, escape(room.num or room.vnum), escape(map.room_id or map.resolved_room_id), escape(display(room.x)), escape(display(room.y)), escape(display(room.z))))
  local legend = map.legend or room.legend
  if type(legend) == "table" then
    local entries = {}; for name, color in pairs(legend) do entries[#entries + 1] = "&lt;font color='" .. escape(color) .. "'&gt;&amp;#9670;&lt;/font&gt; " .. escape(name) end; table.sort(entries)
    safe(widget("map_legend"), "echo", "&lt;b&gt;Terrain:&lt;/b&gt; " .. table.concat(entries, " &amp;nbsp; "))
  else safe(widget("map_legend"), "echo", "&lt;b&gt;Terrain:&lt;/b&gt; " .. escape(room.terrain or room.environment or "No terrain data")) end
end

local function table_count(items)
  if type(items) ~= "table" then return 0 end
  local count = 0
  for _, item in pairs(items) do if type(item) == "table" then count = count + 1 end end
  return count
end

local function overview_section(title, section, body, unavailable_message)
  local badge, status = status_badge(section)
  if status == "unavailable" or status == "error" then
    return "&lt;b&gt;" .. escape(title) .. " &amp;middot; " .. escape(badge) .. "&lt;/b&gt;&lt;br&gt;&lt;font color='" .. theme().muted .. "'&gt;" .. escape(unavailable_message) .. "&lt;/font&gt;"
  end
  return "&lt;b&gt;" .. escape(title) .. " &amp;middot; " .. escape(badge) .. "&lt;/b&gt;" .. body
end

local function render_overview()
  local base, status, worth, quest = value("base"), value("status"), value("worth"), value("quest")
  local details, group = value("details"), value("group")
  local equipment_count = table_count(details.equipment)
  local bags = type(details.bags) == "table" and details.bags or {}
  local bag_used, bag_max = 0, 0
  for _, bag in pairs(bags) do
    if type(bag) == "table" then bag_used = bag_used + (finite(bag.used_weight) or 0); bag_max = bag_max + (finite(bag.max_weight) or 0) end
  end
  local members = type(group.members) == "table" and group.members or {}
  local group_text = envelope("group").status == "unavailable" and "Group GMCP unavailable" or (#members == 0 and "Solo" or (#members .. " members"))
  local class_text = table.concat((function()
    local parts = {}; for _, item in ipairs({base.class, base.subclass}) do if clean(item, 80) and clean(item, 80) ~= "" then parts[#parts + 1] = clean(item, 80) end end; return parts
  end)(), " / ")
  if class_text == "" then class_text = "--" end

  local player = overview_section("Progression", "base", key_value({
    {"Player", base.name}, {"Class", class_text}, {"Level", base.level or status.level},
    {"Tier / Remorts", display(base.tier) .. " / " .. display(base.remorts)},
    {"TNL / Per level", display(status.tnl) .. " / " .. display(base.perlevel)},
  }), "Character data is unavailable. Use Refresh Character.")
  local quest_card = overview_section("Quest", "quest", key_value({
    {"Status", quest.status or quest.state}, {"Action", quest.action}, {"Target", quest.target or quest.targ},
    {"Room / Area", (clean(quest.room, 80) or "--") .. " / " .. (clean(quest.area, 80) or "--")},
    {"Timer", quest.timer or quest.time},
  }), "Quest data is unavailable. Use Refresh Quest.")
  local resources = overview_section("Resources", "worth", key_value({
    {"QP / Earned", display(worth.qp) .. " / " .. display(worth.qpearned)}, {"TP", worth.tp},
    {"Gold / Bank", display(worth.gold) .. " / " .. display(worth.bank)},
    {"Practices / Trains", display(worth.pracs) .. " / " .. display(worth.trains)},
  }), "Character worth data is unavailable.")
  local conditions = overview_section("Conditions", "status", key_value({
    {"Position / State", (clean(status.pos, 80) or "--") .. " / " .. display(status.state)},
    {"Alignment", status.align}, {"Hunger / Thirst", display(status.hunger) .. " / " .. display(status.thirst)},
    {"Enemy", clean(status.enemy, 80) ~= "" and status.enemy or "None"},
  }), "Character status is unavailable.")
  local loadout_status = envelope("details").status
  local loadout
  if loadout_status == "unavailable" or (equipment_count == 0 and #bags == 0 and not details.last_updated) then
    loadout = "&lt;b&gt;Loadout &amp;middot; " .. escape(loadout_status:upper()) .. "&lt;/b&gt;&lt;br&gt;&lt;font color='" .. theme().muted .. "'&gt;Equipment and bags are not loaded. Use Refresh Inventory.&lt;/font&gt;"
  else
    loadout = "&lt;b&gt;Loadout &amp;middot; " .. escape(loadout_status:upper()) .. "&lt;/b&gt;" .. key_value({
      {"Equipped slots", equipment_count}, {"Bags", #bags}, {"Bag weight", display(bag_used) .. " / " .. display(bag_max)},
      {"Updated", finite(details.last_updated) and os.date("%H:%M:%S", finite(details.last_updated)) or "--"},
    })
  end
  local group_card = "&lt;b&gt;Group &amp;middot; " .. escape((envelope("group").status or "unavailable"):upper()) .. "&lt;/b&gt;&lt;br&gt;" .. escape(group_text)

  set_button_text(widget("overview_character_refresh"), "Character")
  set_button_text(widget("overview_quest_refresh"), "Quest")
  set_button_text(widget("overview_inventory_refresh"), "Inventory")
  local body
  if not ui.scroll_capable and (ui.page or 1) == 1 then body = player .. "&lt;br&gt;&lt;br&gt;" .. quest_card
  elseif not ui.scroll_capable then body = resources .. "&lt;br&gt;&lt;br&gt;" .. conditions .. "&lt;br&gt;&lt;br&gt;" .. loadout .. "&lt;br&gt;&lt;br&gt;" .. group_card
  else body = "&lt;table width='100%'&gt;&lt;tr&gt;&lt;td valign='top' width='50%'&gt;" .. player .. "&lt;br&gt;&lt;br&gt;" .. quest_card .. "&lt;/td&gt;&lt;td valign='top'&gt;" .. resources .. "&lt;br&gt;&lt;br&gt;" .. conditions .. "&lt;br&gt;&lt;br&gt;" .. loadout .. "&lt;br&gt;&lt;br&gt;" .. group_card .. "&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;" end
  safe(widget("overview_card"), "echo", body)
end

local function render_character()
  local base, vitals, maximum = value("base"), value("vitals"), value("maxstats")
  local stats, worth, status, quest = value("stats"), value("worth"), value("status"), value("quest")
  local badge = status_badge("base")
  local identity = key_value({
    {"Name", base.name or base.character}, {"Pretitle", base.pretitle}, {"Race", base.race},
    {"Class", base.class}, {"Subclass", base.subclass}, {"Clan", base.clan},
    {"Level", base.level or status.level}, {"Class count", base.classes}, {"Tier", base.tier},
    {"Remorts", base.remorts or base.remort}, {"Redos", base.redos}, {"Pups", base.pups},
    {"Total pups", base.totpups}, {"Per level", base.perlevel},
  })
  local attributes = key_value({{"Strength", display(stats.str) .. " / " .. display(maximum.maxstr)}, {"Intelligence", display(stats.int) .. " / " .. display(maximum.maxint)}, {"Wisdom", display(stats.wis) .. " / " .. display(maximum.maxwis)}, {"Dexterity", display(stats.dex) .. " / " .. display(maximum.maxdex)}, {"Constitution", display(stats.con) .. " / " .. display(maximum.maxcon)}, {"Luck", display(stats.luck) .. " / " .. display(maximum.maxluck)}})
  local state_number = finite(status.state)
  local state_name = state_number and constants and constants.character_states and constants.character_states[state_number]
  local enemy = clean(status.enemy, 80)
  if not enemy or enemy == "" then enemy = "None" end
  local enemy_percent = finite(status.enemypct)
  local combat = key_value({
    {"HP", display(vitals.hp) .. " / " .. display(maximum.maxhp or vitals.maxhp)},
    {"Mana", display(vitals.mana) .. " / " .. display(maximum.maxmana or vitals.maxmana)},
    {"Moves", display(vitals.moves) .. " / " .. display(maximum.maxmoves or vitals.maxmoves)},
    {"Hitroll", stats.hr or stats.hitroll}, {"Damroll", stats.dr or stats.damroll},
    {"Saves", stats.saves}, {"Alignment", status.align}, {"Position", status.pos or status.position},
    {"State", state_name or status.state}, {"Enemy", enemy .. (enemy_percent and (" (" .. display(enemy_percent) .. "%)") or "")},
  })
  local progression = key_value({
    {"TNL", status.tnl}, {"Gold", worth.gold}, {"Bank", worth.bank},
    {"QP", worth.qp or worth.questpoints}, {"QP earned", worth.qpearned}, {"TP", worth.tp},
    {"Practices", worth.pracs or stats.practices}, {"Trains", worth.trains or stats.trains},
    {"Quest", quest.status or quest.state}, {"Quest action", quest.action},
    {"Quest target", quest.target or quest.targ}, {"Quest room", quest.room},
    {"Quest area", quest.area}, {"Quest timer", quest.timer or quest.time},
  })
  local conditions = "&lt;b&gt;Conditions&lt;/b&gt;&lt;br&gt;Position " .. escape(display(status.pos or status.position))
    .. " &amp;middot; state " .. escape(display(state_name or status.state))
    .. " &amp;middot; hunger " .. escape(display(status.hunger))
    .. " &amp;middot; thirst " .. escape(display(status.thirst))
    .. " &amp;middot; enemy " .. escape(enemy)
  local heading = "&lt;b&gt;CHARACTER &amp;middot; " .. escape(badge) .. "&lt;/b&gt;&lt;br&gt;"
  local body
  if not ui.scroll_capable and (ui.page or 1) == 1 then
    body = "&lt;b&gt;Identity&lt;/b&gt;" .. identity .. "&lt;br&gt;&lt;b&gt;Attributes&lt;/b&gt;" .. attributes
  elseif not ui.scroll_capable then
    body = "&lt;b&gt;Combat&lt;/b&gt;" .. combat .. "&lt;br&gt;&lt;b&gt;Progression &amp;amp; currencies&lt;/b&gt;" .. progression .. "&lt;br&gt;" .. conditions
  else
    body = "&lt;table width='100%'&gt;&lt;tr&gt;&lt;td valign='top' width='50%'&gt;&lt;b&gt;Identity&lt;/b&gt;" .. identity
      .. "&lt;br&gt;&lt;b&gt;Attributes&lt;/b&gt;" .. attributes .. "&lt;/td&gt;&lt;td valign='top'&gt;&lt;b&gt;Combat&lt;/b&gt;" .. combat
      .. "&lt;br&gt;&lt;b&gt;Progression &amp;amp; currencies&lt;/b&gt;" .. progression .. "&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;br&gt;" .. conditions
  end
  safe(widget("character_card"), "echo", heading .. body)
end

local function render_group()
  local group = value("group")
  local badge, envelope_status = status_badge("group")
  local members = type(group.members) == "table" and group.members or {}
  local rows = {"&lt;b&gt;GROUP &amp;middot; " .. escape(badge) .. "&lt;/b&gt;&lt;br&gt;"}
  if type(group.invitation) == "table" then
    rows[#rows + 1] = "&lt;b&gt;Invitation " .. escape(group.invitation.status or "pending") .. ":&lt;/b&gt; " .. escape(group.invitation.player or "Unknown") .. " &amp;middot; " .. escape(group.invitation.description or "") .. "&lt;br&gt;&lt;br&gt;"
  end
  if envelope_status == "unavailable" then rows[#rows + 1] = "Group GMCP is unavailable for this session."
  elseif #members == 0 then rows[#rows + 1] = "Not currently grouped."
  else
    rows[#rows + 1] = "&lt;table width='100%' cellspacing='2'&gt;&lt;tr&gt;&lt;td&gt;&lt;b&gt;Member&lt;/b&gt;&lt;/td&gt;&lt;td&gt;&lt;b&gt;Level / Align&lt;/b&gt;&lt;/td&gt;&lt;td&gt;&lt;b&gt;HP&lt;/b&gt;&lt;/td&gt;&lt;td&gt;&lt;b&gt;Mana&lt;/b&gt;&lt;/td&gt;&lt;td&gt;&lt;b&gt;Moves&lt;/b&gt;&lt;/td&gt;&lt;td&gt;&lt;b&gt;TNL / Quest&lt;/b&gt;&lt;/td&gt;&lt;/tr&gt;"
    for index = 1, math.min(#members, 20) do
      local member = members[index]; local info = member.info or member
      rows[#rows + 1] = string.format("&lt;tr&gt;&lt;td&gt;%s&lt;/td&gt;&lt;td&gt;%s / %s&lt;/td&gt;&lt;td&gt;%s/%s&lt;/td&gt;&lt;td&gt;%s/%s&lt;/td&gt;&lt;td&gt;%s/%s&lt;/td&gt;&lt;td&gt;%s / %s&lt;/td&gt;&lt;/tr&gt;", escape(member.name or info.name or "Unknown"), escape(display(info.level or info.lvl)), escape(display(info.align or info.alignment)), escape(display(info.hp)), escape(display(info.maxhp or info.mhp)), escape(display(info.mana)), escape(display(info.maxmana or info.mmn)), escape(display(info.moves)), escape(display(info.maxmoves or info.mmv)), escape(display(info.tnl)), escape(display(info.quest_timer or info.qtimer)))
    end
    rows[#rows + 1] = "&lt;/table&gt;"
  end
  safe(widget("group_card"), "echo", table.concat(rows))
end

local function item_line(item, prefix)
  return string.format("&lt;tr&gt;&lt;td&gt;%s&lt;/td&gt;&lt;td&gt;&lt;b&gt;%s&lt;/b&gt;&lt;/td&gt;&lt;td&gt;Lvl %s &amp;middot; type %s &amp;middot; %s%s%s&lt;/td&gt;&lt;/tr&gt;", escape(prefix or ""), escape(item.name or "Unknown item"), escape(display(item.level)), escape(display(item.item_type or item.type)), escape(item.flags or "no flags"), item.unique and " &amp;middot; unique" or "", finite(item.timer) and (" &amp;middot; timer " .. escape(display(item.timer))) or "")
end

local function render_inventory()
  local details = value("details")
  local badge = status_badge("details")
  set_button_text(widget("inventory_equipment"), "Equipment")
  set_button_text(widget("inventory_bags"), "Bags")
  set_button_text(widget("inventory_empty"), data().show_empty_slots and "Hide empty" or "Show empty")
  set_button_text(widget("inventory_refresh"), details.refreshing and "Refreshing" or "Refresh")
  local rows = {"&lt;b&gt;INVENTORY &amp;middot; " .. escape(badge) .. "&lt;/b&gt; &amp;middot; " .. escape(details.error or (details.stale and "stale" or "current")) .. "&lt;br&gt;&lt;table width='100%' cellspacing='2'&gt;"}
  if data().inventory_tab == "bags" then
    local bags=type(details.bags)=="table" and details.bags or {}
    local first,last=1,#bags
    if not ui.scroll_capable then first=(ui.page-1)*12+1; last=math.min(#bags,first+11) end
    for index=first,last do local bag=bags[index]
      local used, maximum = finite(bag.used_weight), finite(bag.max_weight)
      local percent = used and maximum and maximum &gt; 0 and math.floor(used / maximum * 100) or nil
      rows[#rows + 1] = string.format("&lt;tr&gt;&lt;td&gt;&lt;b&gt;%s&lt;/b&gt;&lt;/td&gt;&lt;td&gt;%s / %s weight%s &amp;middot; %s items&lt;/td&gt;&lt;td&gt;%s&lt;/td&gt;&lt;/tr&gt;", escape(bag.name or "Container"), escape(display(used)), escape(display(maximum)), percent and (" (" .. percent .. "%%)") or "", escape(#(bag.items or {})), escape(bag.error or (bag.pending and "pending" or bag.status or "current")))
    end
    if #bags == 0 then rows[#rows + 1] = "&lt;tr&gt;&lt;td&gt;No bag data.&lt;/td&gt;&lt;/tr&gt;" end
    rows[#rows + 1] = "&lt;/table&gt;&lt;br&gt;&lt;b&gt;Carried inventory&lt;/b&gt;&lt;table width='100%' cellspacing='2'&gt;"
    local inventory=type(details.inventory)=="table" and details.inventory or {}
    for index=1,math.min(#inventory,40) do rows[#rows + 1]=item_line(inventory[index],"") end
    if #inventory==0 then rows[#rows + 1]="&lt;tr&gt;&lt;td&gt;No carried inventory data.&lt;/td&gt;&lt;/tr&gt;" end
  else
    local equipment = type(details.equipment) == "table" and details.equipment or {}
    local wear = constants and constants.wear_locations or {}
    local first,last=0,32
    if not ui.scroll_capable then first=(ui.page-1)*17; last=math.min(32,first+16) end
    for location = first, last do
      local item = equipment[location] or equipment[tostring(location)]
      if item then rows[#rows + 1] = item_line(item, wear[location] or ("Slot " .. location))
      elseif data().show_empty_slots then rows[#rows + 1] = "&lt;tr&gt;&lt;td&gt;" .. escape(wear[location] or ("Slot " .. location)) .. "&lt;/td&gt;&lt;td colspan='2'&gt;&lt;font color='" .. theme().muted .. "'&gt;Empty&lt;/font&gt;&lt;/td&gt;&lt;/tr&gt;" end
    end
  end
  rows[#rows + 1] = "&lt;/table&gt;"
  safe(widget("inventory_card"), "echo", table.concat(rows))
end

local function render_chat()
  local chat = aardwolf_mudlet.chat
  local tabs = chat and chat.tabs and chat.tabs() or {}
  local active = chat and chat.active_name and chat.active_name() or "All"
  for index = 1, 5 do
    local button, tab = widget("chat_tab_" .. index), tabs[index]
    if tab then
      local buffer = chat.buffers and chat.buffers[tab.name] or {}
      local unread = tonumber(buffer.unread) or 0
      set_button_text(button, tab.name .. (unread &gt; 0 and (" (" .. unread .. ")") or ""))
      safe(button, "show")
    else safe(button, "hide") end
  end
  set_button_text(widget("chat_clear"), "Clear")
  set_button_text(widget("chat_copy"), "Copy")
  local header = "&lt;b&gt;" .. escape(active) .. "&lt;/b&gt; &amp;middot; messages remain in the main console by default&lt;br&gt;&lt;br&gt;"
  safe(widget("chat_card"), "echo", header .. (chat and chat.render and chat.render(80) or "Chat capture unavailable."))
end

local function render_dock_header()
  local active = data().active_tab or "overview"
  local section = ({character = "base", group = "group", inventory = "details"})[active] or "base"
  local badge, received
  if active == "chat" then
    local buffer = aardwolf_mudlet.chat and aardwolf_mudlet.chat.active_buffer and aardwolf_mudlet.chat.active_buffer() or {messages={}}
    badge = "BUFFERED " .. tostring(#(buffer.messages or {}))
  elseif active == "overview" then
    local distinct = {}
    for _, name in ipairs({"base", "status", "worth", "quest", "group", "details"}) do
      local item = envelope(name); local item_status = clean(item.status, 20) or "unavailable"
      distinct[item_status] = true
      local timestamp = finite(item.received_at); if timestamp and (not received or timestamp &gt; received) then received = timestamp end
    end
    local count = 0; local only
    for name in pairs(distinct) do count = count + 1; only = name end
    badge = count == 1 and only:upper() or "MIXED"
  else
    badge = status_badge(section)
    received = finite(envelope(section).received_at)
  end
  local updated = received and os.date("%H:%M:%S", received) or "--"
  safe(widget("dock_header"), "echo", "&lt;b&gt;" .. escape((TAB_LABELS[active] or active):upper()) .. "&lt;/b&gt; &amp;middot; " .. escape(badge) .. "&lt;br&gt;&lt;font color='" .. theme().muted .. "'&gt;Updated " .. escape(updated) .. "&lt;/font&gt;")
  set_button_text(widget("pin_toggle"), data().inspector_pinned == true and "Unpin" or "Pin")
end

local function render_drawer()
  local custom = actions and actions.custom and actions.custom() or {}
  local combined = type(custom) == "table" and custom or {}
  local pages = math.max(1, math.ceil(#combined / 8))
  ui.action_page = math.max(1, math.min(ui.action_page or 1, pages))
  local first = (ui.action_page - 1) * 8 + 1
  for index = 1, 8 do
    local action = combined[first + index - 1]
    local button = widget("drawer_action_" .. index)
    if action then
      set_button_text(button, (action.category or "General") .. ": " .. (action.label or action.id))
      safe(button, "setClickCallback", execute_action(action.id)); tooltip(button, action.description or action.command or action.label or action.id); safe(button, "show")
    else safe(button, "hide") end
  end
  safe(widget("drawer_header"), "echo", "&lt;b&gt;QUICKBAR&lt;/b&gt;"); set_button_text(widget("drawer_close"), "X"); set_button_text(widget("drawer_add"), "Add custom action")
  safe(widget("drawer_actions"), "echo", "Eight slots per page &amp;middot; up to 24 profile-local commands &amp;middot; left-click sends only while connected")
  set_button_text(widget("drawer_prev"), "&lt;"); set_button_text(widget("drawer_next"), "&gt;")
  safe(widget("drawer_page"), "echo", "&lt;div align='center'&gt;Page " .. ui.action_page .. " / " .. pages .. "&lt;/div&gt;")
  if data().palette_open == true then safe(widget("drawer"), "show") else safe(widget("drawer"), "hide") end
end

local function render_overlays()
  if ui.editor_open then
    safe(widget("editor"), "show"); safe(widget("editor_title"), "echo", "&lt;b&gt;CUSTOM ACTION EDITOR&lt;/b&gt;")
    safe(widget("editor_help"), "echo", "Enter: Label | Category | command&lt;br&gt;Label: up to 32 characters; category: up to 24&lt;br&gt;Command: one printable line, up to 200 characters.&lt;br&gt;Save validates and confirms the exact command shown here.")
    set_button_text(widget("editor_save"), "Save"); set_button_text(widget("editor_cancel"), "Cancel")
  else safe(widget("editor"), "hide") end
  local pending = pending_action()
  if type(pending) == "table" then
    safe(widget("confirm"), "show")
    safe(widget("confirm_text"), "echo", "&lt;b&gt;CONFIRM CUSTOM COMMAND&lt;/b&gt;&lt;br&gt;&lt;br&gt;" .. escape(pending.label or "Custom action") .. "&lt;br&gt;&lt;font color='" .. theme().warning .. "'&gt;" .. escape(pending.command or "") .. "&lt;/font&gt;&lt;br&gt;&lt;br&gt;Send this command once?")
    set_button_text(widget("confirm_send"), "Send"); set_button_text(widget("confirm_cancel"), "Cancel")
  else safe(widget("confirm"), "hide") end
end

function ui.render()
  if not ui.root then return end
  ui.render_pending = false
  render_header(); render_context(); render_map(); render_overview(); render_character(); render_group(); render_inventory(); render_chat(); render_dock_header(); render_drawer(); render_overlays()
  local active = data().active_tab or "overview"
  for _, tab in ipairs(TABS) do if tab == active then safe(widget(tab .. "_content"), "show") else safe(widget(tab .. "_content"), "hide") end; set_button_text(widget("tab_" .. tab), TAB_LABELS[tab]) end
  local mapper_visible = not data().palette_open and not ui.editor_open and type(pending_action()) ~= "table"
  if mapper_visible then safe(widget("mapper"), "show") else safe(widget("mapper"), "hide") end
  if widget("pager") then
    local pages = (active == "overview" or active == "inventory" or active == "character") and 2 or 1; ui.page = math.max(1, math.min(ui.page or 1, pages))
    set_button_text(widget("page_prev"), "&lt;"); set_button_text(widget("page_next"), "&gt;"); safe(widget("page_status"), "echo", "Page " .. ui.page .. " / " .. pages)
  end
  local vitals, maximum, worth, base, status = value("vitals"), value("maxstats"), value("worth"), value("base"), value("status")
  set_gauge("hp", "HP", vitals.hp, maximum.maxhp or vitals.maxhp)
  set_gauge("mana", "Mana", vitals.mana, maximum.maxmana or vitals.maxmana)
  set_gauge("moves", "Moves", vitals.moves, maximum.maxmoves or vitals.maxmoves)
  set_gauge("tnl", "TNL", status.tnl, base.perlevel or 1000)
  set_target_gauge()
  set_gauge("hunger", "Hunger", status.hunger, nil, status.hunger)
  set_gauge("thirst", "Thirst", status.thirst, nil, status.thirst)
  ui.apply_theme()
end

function ui.request_render()
  if ui.render_pending then return end
  ui.render_pending = true
  if type(registerNamedTimer) == "function" and type(deleteNamedTimer) == "function" then
    -- Mudlet's fifth registerNamedTimer argument means "repeating".  This is
    -- a debounce, so it must fire once; registering it as repeating redraws
    -- the entire command deck roughly every 80 ms forever.
    deleteNamedTimer(TIMER_USER, RENDER_TIMER); registerNamedTimer(TIMER_USER, RENDER_TIMER, 0.08, ui.render, false)
  else ui.render() end
end

function ui.show(persist_setting)
  if not ui.root and not ui.build() then ui.message("Geyser is unavailable. Use 'aard status' for text output."); return false end
  if not data().border_claim then ui.base_right = apply_right_claim(layout().total) end
  if not data().bottom_border_claim then local l = layout(); ui.base_bottom = apply_bottom_claim(hud_height(math.max(0, l.window_width - l.base - l.total))) end
  if persist_setting ~= false then data().visible = true; persist() end
  safe(ui.root, "show"); safe(ui.bottom_root, "show"); ui.reflow(); ui.render()
  if data().active_tab == "inventory" and aardwolf_mudlet.details and aardwolf_mudlet.details.start then aardwolf_mudlet.details.start() end
  return true
end

function ui.hide(persist_setting)
  if persist_setting ~= false then data().visible = false; persist() end
  if aardwolf_mudlet.details and aardwolf_mudlet.details.stop then aardwolf_mudlet.details.stop(true) end
  safe(ui.root, "hide"); safe(ui.bottom_root, "hide"); release_bottom_claim(); release_right_claim()
end

function ui.destroy()
  ui.hide(false)
  if type(deleteNamedTimer) == "function" then deleteNamedTimer(TIMER_USER, RENDER_TIMER) end
  if ui.root and type(ui.root.delete) == "function" then pcall(ui.root.delete, ui.root) end
  if ui.bottom_root and type(ui.bottom_root.delete) == "function" then pcall(ui.bottom_root.delete, ui.bottom_root) end
  ui.root, ui.bottom_root, ui.widgets, ui.layout = nil, nil, nil, nil
end

function ui.set_tab(tab)
  if tab == "map" then tab = "overview" end
  local valid = {overview = true, character = true, group = true, inventory = true, chat = true}
  if not valid[tab] then return false, "Unknown tab" end
  local current_uses_details = data().active_tab == "inventory" or data().active_tab == "overview"
  if current_uses_details and tab ~= data().active_tab and aardwolf_mudlet.details and aardwolf_mudlet.details.stop then aardwolf_mudlet.details.stop(true) end
  data().active_tab = tab; ui.page = 1; persist()
  if tab == "inventory" and aardwolf_mudlet.details and aardwolf_mudlet.details.start then aardwolf_mudlet.details.start() end
  if tab == "chat" and aardwolf_mudlet.chat and aardwolf_mudlet.chat.select then aardwolf_mudlet.chat.select(aardwolf_mudlet.chat.active_name()) end
  ui.request_render(); return true
end

function ui.toggle_palette(force)
  data().palette_open = type(force) == "boolean" and force or not data().palette_open
  if not data().palette_open then ui.editor_open = false end
  persist(); ui.request_render(); return data().palette_open
end

function ui.prompt_custom_editor(action_id)
  ui.editor_open = true; ui.editing_action_id = action_id
  data().palette_open = true; persist()
  local command_line = widget("editor_command")
  if command_line then
    safe(command_line, "setText", "")
    safe(command_line, "setFocus")
  end
  ui.request_render(); return true
end

function ui.editor_value()
  local editor=widget("editor_command")
  if not editor then return nil end
  local raw
  if type(editor.getText)=="function" then local ok,value=pcall(editor.getText,editor); if ok then raw=value end end
  if type(raw)~="string" then return nil end
  local label,category,command=raw:match("^%s*([^|]-)%s*|%s*([^|]-)%s*|%s*(.-)%s*$")
  return label,category,command
end

-- Compatibility callback used by the command module after theme changes.
function ui.theme()
  ui.apply_theme()
  ui.request_render()
end
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.help</name><script>aardwolf_mudlet.help = aardwolf_mudlet.help or {}

function aardwolf_mudlet.help.summary()
  return [[aardwolf-mudlet 1.0.0
aard status|help
aard ui show|hide|status|theme &lt;obsidian|high-contrast&gt;|density &lt;compact|comfortable&gt;|scale &lt;90|100|115|130&gt;
aard map import|import cancel|status|palette &lt;source|obsidian|high-contrast&gt;|center|zoom &lt;in|out&gt;
aard chat show|hide|status|clear|tab ...|filter ...|preserve &lt;on|off&gt;|log &lt;on|off&gt;|limit &lt;1-5000&gt;|copy|open &lt;https-url&gt;
aard action add &lt;label&gt;|&lt;category&gt;|&lt;command&gt; | edit|remove|move|list
aard inventory refresh|status
aard capture &lt;command&gt;|copy
aard tts on|off|say &lt;text&gt;|clear|rate &lt;-1..1&gt;|voice &lt;name&gt;|status
aard sound on|off|map &lt;event&gt;|&lt;relative media file&gt;|&lt;volume&gt;|remove &lt;event&gt;|status
aard data export|import|backup|backup &lt;on|off&gt;|status

The package preserves the main console, never polls equipment, never replaces a profile map, and never executes persisted Lua.]]
end

function aardwolf_mudlet.help.show()
  aardwolf_mudlet.ui.message(aardwolf_mudlet.help.summary())
end
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script><Script isActive="yes" isFolder="no"><name>aardwolf_mudlet.lifecycle</name><script>aardwolf_mudlet.lifecycle = aardwolf_mudlet.lifecycle or {}
local legacy_base_pending = aardwolf_mudlet.lifecycle.initialized == nil
  and aardwolf_mudlet.ui and aardwolf_mudlet.ui.root ~= nil
local legacy_base_right = legacy_base_pending and tonumber(aardwolf_mudlet.ui.base_right) or nil
local legacy_base_bottom = legacy_base_pending and tonumber(aardwolf_mudlet.ui.base_bottom) or nil
aardwolf_mudlet.lifecycle.handlers = aardwolf_mudlet.lifecycle.handlers or {}
aardwolf_mudlet.lifecycle.initialized = aardwolf_mudlet.lifecycle.initialized == true
aardwolf_mudlet.lifecycle.quest_requested = aardwolf_mudlet.lifecycle.quest_requested == true
aardwolf_mudlet.lifecycle.character_requested = aardwolf_mudlet.lifecycle.character_requested == true
local USER="aardwolf-mudlet"
local HEARTBEAT="aardwolf-mudlet::heartbeat"
local CONNECT_TIMER="aardwolf-mudlet::connect-hydrate"
local LEGACY_USER="aardwolf_mudlet"
local LEGACY_EVENT_PREFIX="aardwolf-mudlet::event::"
local LEGACY_EVENTS={
  "char-base","char-vitals","char-maxstats","char-status","char-stats","char-worth",
  "group","room-info","tick","config","install","uninstall","load","resize",
  "connect","disconnect","mapper-loaded","map-import-finished","exit",
}
local LEGACY_TIMERS={
  "aardwolf-mudlet::timer::render","aardwolf-mudlet::timer::start",
  "aardwolf-mudlet::timer::tick-countdown","aardwolf-mudlet::timer::details-queue",
  "aardwolf-mudlet::timer::details-capture","aardwolf-mudlet::timer::details-debounce",
}
local events={
  {"base","gmcp.char.base",aardwolf_mudlet.protocol.base},{"vitals","gmcp.char.vitals",aardwolf_mudlet.protocol.vitals},
  {"maxstats","gmcp.char.maxstats",aardwolf_mudlet.protocol.maxstats},{"status","gmcp.char.status",aardwolf_mudlet.protocol.status},
  {"stats","gmcp.char.stats",aardwolf_mudlet.protocol.stats},{"worth","gmcp.char.worth",aardwolf_mudlet.protocol.worth},
  {"group","gmcp.group",aardwolf_mudlet.protocol.group},{"room","gmcp.room.info",aardwolf_mudlet.protocol.room},
  {"quest","gmcp.comm.quest",aardwolf_mudlet.protocol.quest},{"tick","gmcp.comm.tick",aardwolf_mudlet.protocol.tick},
  {"channel","gmcp.comm.channel",aardwolf_mudlet.protocol.channel},
  {"map-status","aardwolf-mudlet::map-status-changed",aardwolf_mudlet.protocol.map_status},
}
local function legacy_conflict()
  local conflicts={}
  for _,candidate in ipairs({"aardwolf_interface","aardwolf_map","aardwolf_character","aardwolf_chat"}) do
    if type(rawget(_G,candidate))=="table" then conflicts[#conflicts+1]=candidate end
  end
  return #conflicts&gt;0,conflicts
end
local function connected()
  if type(getConnectionInfo)~="function" then return false end
  local ok,info,port,is_connected=pcall(getConnectionInfo); if not ok then return false end
  if type(is_connected)=="boolean" then return is_connected end
  if type(info)=="table" then return info.connected==true or info.state=="connected" or info[1]==true end
  return info==true
end
function aardwolf_mudlet.lifecycle.center_map()
  if aardwolf_mudlet.map and aardwolf_mudlet.map.center_current then return aardwolf_mudlet.map.center_current() end
  return false,"The packaged map importer is unavailable."
end
function aardwolf_mudlet.lifecycle.request_quest(force)
  if aardwolf_mudlet.state.connection~="connected" or (aardwolf_mudlet.lifecycle.quest_requested and not force) then return false end
  aardwolf_mudlet.lifecycle.quest_requested=true
  if sendGMCP then sendGMCP("request quest"); return true end
  return false
end
function aardwolf_mudlet.lifecycle.request_character(force)
  if aardwolf_mudlet.state.connection~="connected" or (aardwolf_mudlet.lifecycle.character_requested and not force) then return false end
  aardwolf_mudlet.lifecycle.character_requested=true
  if sendGMCP then sendGMCP("request char"); return true end
  return false
end
function aardwolf_mudlet.lifecycle.on_connect()
  aardwolf_mudlet.state.reset_session("connected"); aardwolf_mudlet.lifecycle.quest_requested=false; aardwolf_mudlet.lifecycle.character_requested=false
  if deleteNamedTimer then pcall(deleteNamedTimer,USER,CONNECT_TIMER) end
  registerNamedTimer(USER,CONNECT_TIMER,0.5,function() aardwolf_mudlet.lifecycle.request_character(false); aardwolf_mudlet.lifecycle.request_quest(false) end,false)
  aardwolf_mudlet.ui.request_render()
end
function aardwolf_mudlet.lifecycle.on_disconnect()
  if deleteNamedTimer then pcall(deleteNamedTimer,USER,CONNECT_TIMER) end
  aardwolf_mudlet.details.stop("Disconnected")
  if aardwolf_mudlet.capture then aardwolf_mudlet.capture.cancel("Disconnected") end
  aardwolf_mudlet.state.reset_session("disconnected")
  aardwolf_mudlet.lifecycle.quest_requested=false
  aardwolf_mudlet.lifecycle.character_requested=false
  aardwolf_mudlet.ui.request_render()
end
function aardwolf_mudlet.lifecycle.on_resize() aardwolf_mudlet.ui.reflow() end
function aardwolf_mudlet.lifecycle.on_heartbeat()
  local now=os.time()
  for name,envelope in pairs(aardwolf_mudlet.state.sections or {}) do
    if name~="details" and envelope.status=="current" and envelope.received_at and now-envelope.received_at&gt;180 then envelope.status="stale"; envelope.error="No update received for 180 seconds" end
  end
  if aardwolf_mudlet.capture and aardwolf_mudlet.capture.expire then aardwolf_mudlet.capture.expire() end
  aardwolf_mudlet.ui.request_render()
end
function aardwolf_mudlet.lifecycle.on_exit() aardwolf_mudlet.lifecycle.shutdown(true) end
function aardwolf_mudlet.lifecycle.on_uninstall(_,name) if tostring(name)=="aardwolf-mudlet" then aardwolf_mudlet.lifecycle.shutdown(false) end end
function aardwolf_mudlet.lifecycle.on_profile_save()
  if aardwolf_mudlet.data and aardwolf_mudlet.data.on_profile_save then pcall(aardwolf_mudlet.data.on_profile_save) end
end
function aardwolf_mudlet.lifecycle.hydrate()
  if not connected() then return end
  aardwolf_mudlet.state.connection="connected"
  for _,event in ipairs(events) do if event[2]:match("^gmcp%.") and event[2]~="gmcp.comm.tick" then pcall(event[3]) end end
  pcall(aardwolf_mudlet.protocol.map_status)
end
function aardwolf_mudlet.lifecycle.register()
  for _,event in ipairs(events) do registerNamedEventHandler(USER,"aardwolf-mudlet::"..event[1],event[2],event[3]) end
  registerNamedEventHandler(USER,"aardwolf-mudlet::resize","sysWindowResizeEvent",aardwolf_mudlet.lifecycle.on_resize)
  registerNamedEventHandler(USER,"aardwolf-mudlet::connect","sysConnectionEvent",aardwolf_mudlet.lifecycle.on_connect)
  registerNamedEventHandler(USER,"aardwolf-mudlet::disconnect","sysDisconnectionEvent",aardwolf_mudlet.lifecycle.on_disconnect)
  registerNamedEventHandler(USER,"aardwolf-mudlet::exit","sysExitEvent",aardwolf_mudlet.lifecycle.on_exit)
  registerNamedEventHandler(USER,"aardwolf-mudlet::uninstall","sysUninstallPackage",aardwolf_mudlet.lifecycle.on_uninstall)
  registerNamedEventHandler(USER,"aardwolf-mudlet::profile-save","sysProfileSaveEvent",aardwolf_mudlet.lifecycle.on_profile_save)
  -- The heartbeat is the one intentionally repeating named timer. Mudlet's
  -- fifth argument is `repeating`, not `one_shot`.
  registerNamedTimer(USER,HEARTBEAT,1,aardwolf_mudlet.lifecycle.on_heartbeat,true)
end
function aardwolf_mudlet.lifecycle.unregister()
  if deleteNamedEventHandler then
    for _,event in ipairs(events) do pcall(deleteNamedEventHandler,USER,"aardwolf-mudlet::"..event[1]) end
    for _,name in ipairs({"resize","connect","disconnect","exit","uninstall","profile-save"}) do pcall(deleteNamedEventHandler,USER,"aardwolf-mudlet::"..name) end
    -- Mudlet can retain static objects from a pre-1.5 package during an
    -- in-place upgrade. Remove that generation's handlers as well so it
    -- cannot reclaim the viewport after the command deck has rebuilt.
    for _,name in ipairs(LEGACY_EVENTS) do pcall(deleteNamedEventHandler,LEGACY_USER,LEGACY_EVENT_PREFIX..name) end
  end
  if deleteNamedTimer then
    pcall(deleteNamedTimer,USER,HEARTBEAT)
    pcall(deleteNamedTimer,USER,CONNECT_TIMER)
    for _,name in ipairs(LEGACY_TIMERS) do pcall(deleteNamedTimer,LEGACY_USER,name) end
  end
end
function aardwolf_mudlet.lifecycle.shutdown(keep_visibility)
  -- This must be idempotent even when the loaded namespace came from the
  -- legacy monolith, which had no `initialized` flag. Returning early here
  -- leaves its Geyser roots and border claims alive and can hide the console.
  if aardwolf_mudlet.details and aardwolf_mudlet.details.stop then pcall(aardwolf_mudlet.details.stop) end
  if aardwolf_mudlet.capture and aardwolf_mudlet.capture.cancel then pcall(aardwolf_mudlet.capture.cancel,"Package stopped") end
  if aardwolf_mudlet.chat and aardwolf_mudlet.chat.shutdown then pcall(aardwolf_mudlet.chat.shutdown) end
  if aardwolf_mudlet.sound and aardwolf_mudlet.sound.shutdown then pcall(aardwolf_mudlet.sound.shutdown) end
  if aardwolf_mudlet.map and aardwolf_mudlet.map.shutdown then pcall(aardwolf_mudlet.map.shutdown,"unloaded") end
  aardwolf_mudlet.lifecycle.unregister()
  if aardwolf_mudlet.ui and aardwolf_mudlet.ui.destroy then pcall(aardwolf_mudlet.ui.destroy) end
  -- A legacy root can retain its in-memory base even if an interrupted
  -- upgrade already erased the settings-file claim. Use that package-owned
  -- evidence once; never guess at or repeatedly reduce a foreign border.
  if legacy_base_pending then
    if legacy_base_right and type(getBorderRight)=="function" and type(setBorderRight)=="function" then
      local ok,current=pcall(getBorderRight)
      if ok and tonumber(current) and tonumber(current)&gt;=legacy_base_right then pcall(setBorderRight,legacy_base_right) end
    end
    if legacy_base_bottom and type(getBorderBottom)=="function" and type(setBorderBottom)=="function" then
      local ok,current=pcall(getBorderBottom)
      if ok and tonumber(current) and tonumber(current)&gt;=legacy_base_bottom then pcall(setBorderBottom,legacy_base_bottom) end
    end
    legacy_base_pending=false
  end
  if aardwolf_mudlet.actions and aardwolf_mudlet.actions.cancel then pcall(aardwolf_mudlet.actions.cancel) end
  aardwolf_mudlet.lifecycle.initialized=false
  if not keep_visibility and aardwolf_mudlet.settings.data then aardwolf_mudlet.settings.data.visible=false end
end
function aardwolf_mudlet.lifecycle.initialize()
  local conflict,names=legacy_conflict()
  if conflict then
    aardwolf_mudlet.lifecycle.deferred=true
    if type(echo)=="function" then echo("\n[aardwolf-mudlet] Initialization deferred: disable legacy Aardwolf packages ("..table.concat(names,", ")..") and reload. Trusted legacy map ownership markers remain reusable.\n") end
    return false
  end
  aardwolf_mudlet.lifecycle.deferred=false
  aardwolf_mudlet.lifecycle.shutdown(true); aardwolf_mudlet.settings.load(); aardwolf_mudlet.state.reset_session("disconnected")
  if aardwolf_mudlet.chat and aardwolf_mudlet.chat.initialize then aardwolf_mudlet.chat.initialize() end
  if aardwolf_mudlet.accessibility and aardwolf_mudlet.accessibility.apply then aardwolf_mudlet.accessibility.apply() end
  if aardwolf_mudlet.map and aardwolf_mudlet.map.initialize then aardwolf_mudlet.map.initialize() end
  -- A saved claim can survive a Mudlet/profile restart. Restore it only when
  -- the live border still exactly matches what this package applied, then
  -- clear the stale ownership record before calculating the new layout.
  if aardwolf_mudlet.ui and aardwolf_mudlet.ui.release_saved_claims then aardwolf_mudlet.ui.release_saved_claims() end
  aardwolf_mudlet.ui.build(); aardwolf_mudlet.lifecycle.register(); aardwolf_mudlet.lifecycle.initialized=true
  aardwolf_mudlet.lifecycle.hydrate(); if aardwolf_mudlet.state.connection=="connected" then aardwolf_mudlet.lifecycle.request_character(false); aardwolf_mudlet.lifecycle.request_quest(false) end
  if aardwolf_mudlet.settings.is_visible() then aardwolf_mudlet.ui.show() else aardwolf_mudlet.ui.hide() end
end
function aardwolf_mudlet.lifecycle.repair()
  aardwolf_mudlet.lifecycle.shutdown(true)
  aardwolf_mudlet.settings.load()
  if aardwolf_mudlet.ui and aardwolf_mudlet.ui.release_saved_claims then aardwolf_mudlet.ui.release_saved_claims() end
  aardwolf_mudlet.settings.data.visible=true
  aardwolf_mudlet.settings.data.collapsed_by_user=false
  aardwolf_mudlet.settings.save()
  aardwolf_mudlet.lifecycle.initialize()
  return aardwolf_mudlet.ui and aardwolf_mudlet.ui.root ~= nil
end
aardwolf_mudlet.lifecycle.initialize()
</script><packageName></packageName><eventHandlerList></eventHandlerList></Script></ScriptPackage><KeyPackage></KeyPackage><VariablePackage></VariablePackage></MudletPackage>
